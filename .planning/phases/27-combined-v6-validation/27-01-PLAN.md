---
phase: 27-combined-v6-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - analysis/validate_combined_v6.py
  - docs/rules_mdm_hybrid.md
autonomous: true
requirements: [VAL-08, VAL-09, VAL-10]

must_haves:
  truths:
    - "A/B report compares HybridEngine baseline vs HybridEngine+fail-safe on VN30"
    - "Walk-forward validation uses train pre-2022, test 2022-2026"
    - "Walk-forward degradation < 10% out-of-sample vs in-sample"
    - "Dashboard is deployed with updated metrics"
  artifacts:
    - path: "analysis/validate_combined_v6.py"
      provides: "Combined A/B + walk-forward validation script"
      contains: "def run_ab_comparison"
    - path: "output/v6_combined_validation.txt"
      provides: "Validation results text report"
      contains: "Walk-Forward"
---

<objective>
Create combined v6.0 validation: A/B comparison of HybridEngine baseline vs HybridEngine+fail-safe,
walk-forward validation (train pre-2022, test 2022-2026), and confirm dashboard deployment.

Context: Fail-safe was wired into HybridEngine this session. Results:
- Baseline (fail-safe OFF): +192.1%, CAGR 10.0%, DD -31.8%
- Fail-safe ON: +238.8%, CAGR 11.5%, DD -28.2%

Dashboard already deployed with latest data. This plan creates the formal validation script
and walk-forward test to satisfy VAL-08, VAL-09, VAL-10.
</objective>

<execution_context>
@C:/Users/trant/projects/mdm/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/trant/projects/mdm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
From strategies/mdm_hybrid/config.py:
```python
@dataclass
class MDMV2Config:
    # ... fields ...
    fail_safe_enabled: bool = True
    name: str = "default"

VN30_PRESET = MDMV2Config(
    correction_threshold=-0.06, ma10_cash_consecutive=3,
    cash_deterioration_days=20, stop_loss_pct=0.015,
    fail_safe_enabled=True, ...
)
```

From strategies/mdm_hybrid/mdm_hybrid_engine.py:
```python
class HybridEngine:
    def __init__(self, config: HybridConfig = None): ...
    def run(self, df: pd.DataFrame) -> pd.DataFrame: ...
    def get_trades(self) -> list: ...
```

From analysis/sweep_vn30_params.py:
```python
def compute_metrics(results):
    # Returns: total_return, cagr, max_dd, transitions, buy_pct
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create combined v6.0 validation script</name>
  <files>
    analysis/validate_combined_v6.py
  </files>
  <read_first>
    analysis/sweep_vn30_params.py
    strategies/mdm_hybrid/config.py
  </read_first>
  <behavior>
    Script must:
    1. Run A/B comparison: HybridEngine with fail_safe_enabled=False vs True (best config)
    2. Run walk-forward: train on 2015-2021, test on 2022-2026, compare degradation
    3. Print results to stdout and save to output/v6_combined_validation.txt
    4. Exit 0 if walk-forward degradation < 10%, exit 1 otherwise
  </behavior>
  <action>
    Create analysis/validate_combined_v6.py that:

    1. Loads VN30 data via DataLoader('vn30') + build_indicator_dataframe
    2. Defines best config: dd_cash_threshold=5, ma10_cash_consecutive=3, cash_deterioration_days=20, correction_threshold=-0.06, stop_loss_pct=0.015
    3. A/B comparison (VAL-08):
       - Baseline: HybridConfig(v2_config=MDMV2Config(**best, fail_safe_enabled=False), two_phase_enabled=True, filter_enabled=False)
       - Treatment: same but fail_safe_enabled=True
       - Compute metrics for both using compute_metrics from sweep_vn30_params pattern
       - Print side-by-side comparison
    4. Walk-forward (VAL-09):
       - Train: 2015-01-01 to 2021-12-31
       - Test: 2022-01-01 to 2026-03-31
       - Run treatment config on both periods
       - Compute degradation = (train_return - test_return) / train_return
       - PASS if degradation < 0.10 (10%)
    5. Dashboard check (VAL-10):
       - Verify dashboard/data/mdm_v2.json exists and contains signal_stats
       - Print confirmation
    6. Save full report to output/v6_combined_validation.txt
  </action>
  <verify>
    <automated>uv run python analysis/validate_combined_v6.py</automated>
  </verify>
  <acceptance_criteria>
    - Script runs without error
    - A/B comparison shows fail-safe ON beats baseline
    - Walk-forward degradation < 10%
    - output/v6_combined_validation.txt exists
  </acceptance_criteria>
  <done>
    Combined v6.0 validation complete. A/B and walk-forward both pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update docs and requirements</name>
  <files>
    docs/rules_mdm_hybrid.md
  </files>
  <read_first>
    docs/rules_mdm_hybrid.md
  </read_first>
  <behavior>
    - Add fail-safe section to rules_mdm_hybrid.md documenting the feature
    - Update backtest results in Section XIII with new 238.8% figure
  </behavior>
  <action>
    1. In docs/rules_mdm_hybrid.md Section XIII backtest results table, update VN30 preset row to show +239% return
    2. Add fail-safe documentation section after Section XII (same content as in rules_mdm_v2.md)
  </action>
  <verify>
    <automated>grep "239\|238" docs/rules_mdm_hybrid.md</automated>
  </verify>
</task>

</tasks>

<verification>
- `uv run python analysis/validate_combined_v6.py` exits 0
- `test -f output/v6_combined_validation.txt` passes
- Walk-forward degradation < 10%
</verification>

<success_criteria>
- Combined A/B shows fail-safe improves return, DD, and CAGR
- Walk-forward validation passes (< 10% degradation)
- Dashboard deployed with signal_stats
- Docs updated with new results
</success_criteria>

<output>
After completion, create `.planning/phases/27-combined-v6-validation/27-01-SUMMARY.md`
</output>
