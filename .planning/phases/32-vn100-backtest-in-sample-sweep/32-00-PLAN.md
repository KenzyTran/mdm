---
phase: 32-vn100-backtest-in-sample-sweep
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/phase32/__init__.py
  - tests/phase32/conftest.py
  - tests/phase32/test_scaffold.py
  - pyproject.toml
autonomous: true
requirements: [BT-01, BT-02]
must_haves:
  truths:
    - "pytest tests/phase32 runs and passes scaffold"
    - "pyarrow and tqdm import successfully"
  artifacts:
    - path: "tests/phase32/conftest.py"
      provides: "Synthetic OHLC + universe fixtures"
    - path: "tests/phase32/test_scaffold.py"
      provides: "Env import check"
  key_links:
    - from: "tests/phase32/test_scaffold.py"
      to: "pyarrow, tqdm, multiprocessing"
      via: "import statements"
      pattern: "import (pyarrow|tqdm|multiprocessing)"
---

<objective>
Wave 0 scaffolding: create test directory, env-check tests, shared fixtures, and ensure pyarrow + tqdm are installed. Unblocks all later plans which reference these tests.
</objective>

<context>
@.planning/phases/32-vn100-backtest-in-sample-sweep/32-CONTEXT.md
@.planning/phases/32-vn100-backtest-in-sample-sweep/32-VALIDATION.md
@pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tests/phase32 scaffold + env check</name>
  <files>tests/phase32/__init__.py, tests/phase32/conftest.py, tests/phase32/test_scaffold.py, pyproject.toml</files>
  <read_first>
    - pyproject.toml (current dependencies)
    - tests/ (any existing test layout, if any)
  </read_first>
  <action>
    1. Create empty `tests/phase32/__init__.py`.
    2. Create `tests/phase32/conftest.py` with two pytest fixtures:
       - `synthetic_ohlc()` returning a tiny pandas DataFrame with columns `[stockcode, tradingdate, openprice, highprice, lowprice, closeprice, volume]` covering 30 trading days for 3 fake tickers (AAA, BBB, CCC).
       - `synthetic_universe()` returning dict `{date: [stockcode,...]}` with constant membership = [AAA, BBB, CCC] across 30 days.
    3. Create `tests/phase32/test_scaffold.py` with three tests:
       - `test_imports_pyarrow()` — `import pyarrow; assert pyarrow.__version__`
       - `test_imports_tqdm()` — `import tqdm; assert tqdm.__version__`
       - `test_multiprocessing_pool()` — spin up `multiprocessing.Pool(2)`, map `lambda x: x*2` over `[1,2,3]`, assert result `[2,4,6]`. Use a top-level helper function (not lambda) so it works on Windows spawn.
    4. Add `pyarrow>=14.0.0` and `tqdm>=4.66.0` to `[project] dependencies` in `pyproject.toml` if not already present. Do not touch other deps.
  </action>
  <verify>
    <automated>uv run pytest tests/phase32/test_scaffold.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/phase32/__init__.py`, `conftest.py`, `test_scaffold.py` exist
    - `uv run pytest tests/phase32/test_scaffold.py -x -q` exits 0 with 3 passed
    - `grep -q pyarrow pyproject.toml` returns 0
    - `grep -q tqdm pyproject.toml` returns 0
  </acceptance_criteria>
  <done>Scaffold green; downstream plans can write tests under tests/phase32/.</done>
</task>

</tasks>

<verification>
`uv run pytest tests/phase32 -x -q` passes.
</verification>

<success_criteria>
Test infra ready, env deps installed, fixtures available for plans 01-03.
</success_criteria>

<output>
Create `.planning/phases/32-vn100-backtest-in-sample-sweep/32-00-SUMMARY.md` after completion.
</output>
</content>
