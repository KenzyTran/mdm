---
phase: 32-vn100-backtest-in-sample-sweep
plan: 03
type: execute
wave: 3
depends_on: ["32-02"]
files_modified:
  - analysis/select_top3_vn100.py
  - tests/phase32/test_top3.py
  - docs/audits/phase32/locked_params_top3.json
  - docs/audits/phase32-backtest-sweep.md
autonomous: false
requirements: [BT-02]
must_haves:
  truths:
    - "Top-3 configs by Sharpe_rf3 selected from sweep_results.csv (BT-02 / SC4)"
    - "locked_params_top3.json written for Phase 33 handoff (D-14)"
    - "Phase 32 audit report describes methodology + single-run + sweep + top-3 (D-16)"
    - "Sanity gate enforced on top-3 (D-13: abort if any flagged)"
  artifacts:
    - path: "analysis/select_top3_vn100.py"
      provides: "Top-3 selection script with tie-breakers"
    - path: "docs/audits/phase32/locked_params_top3.json"
      provides: "JSON handoff for Phase 33 (D-14 schema)"
    - path: "docs/audits/phase32-backtest-sweep.md"
      provides: "Phase 32 audit report"
  key_links:
    - from: "analysis/select_top3_vn100.py"
      to: "docs/audits/phase32/sweep_results.csv"
      via: "pandas.read_csv input"
      pattern: "sweep_results.csv"
    - from: "analysis/select_top3_vn100.py"
      to: "docs/audits/phase32/locked_params_top3.json"
      via: "json.dump output"
      pattern: "locked_params_top3.json"
---

<objective>
Close BT-02: read `sweep_results.csv` from plan 02, sort by `Sharpe_rf3` desc with tie-breakers (CAGR desc, MaxDD asc per D-12), write top-3 to `locked_params_top3.json` per D-14 schema, enforce sanity gate (D-13: if any top-3 row has `sanity_flag != "OK"`, abort and require manual review), and produce the Phase 32 audit report markdown.
</objective>

<context>
@.planning/phases/32-vn100-backtest-in-sample-sweep/32-CONTEXT.md
@docs/audits/phase32/sweep_results.csv
@docs/audits/phase32/single_run_nav.csv
@docs/audits/phase32/single_run_trades.csv
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement select_top3_vn100.py + write locked_params JSON</name>
  <files>analysis/select_top3_vn100.py, tests/phase32/test_top3.py, docs/audits/phase32/locked_params_top3.json</files>
  <read_first>
    - docs/audits/phase32/sweep_results.csv (column layout)
    - .planning/phases/32-vn100-backtest-in-sample-sweep/32-CONTEXT.md (D-10..D-14)
  </read_first>
  <behavior>
    - test_select_top3_returns_three_rows: given a synthetic DataFrame of 10 rows with varying Sharpe_rf3, returns 3 rows.
    - test_select_top3_sorted_by_sharpe_desc: result[0].Sharpe_rf3 >= result[1] >= result[2].
    - test_tie_breaker_cagr: when two rows tie on Sharpe, higher CAGR wins (D-12).
    - test_tie_breaker_maxdd: when Sharpe + CAGR tie, lower MaxDD (less negative — i.e. abs() smaller, but MaxDD stored as negative number, so MaxDD closer to 0) wins.
    - test_sanity_gate_aborts: if any top-3 row has sanity_flag != "OK", `select_top3` raises `RuntimeError` (D-13).
    - test_json_schema: written JSON contains keys `selected_at, selection_metric, period, universe, configs`; configs is list[3]; each config has `rank, c_yoy, a_cagr, n_proximity, hard_stop, slots, entry_option, metrics`.
  </behavior>
  <action>
    Create `analysis/select_top3_vn100.py`:

    1. Module docstring citing BT-02 SC4 + D-10..D-14.
    2. Constants:
       ```python
       SWEEP_CSV = Path("docs/audits/phase32/sweep_results.csv")
       OUTPUT_JSON = Path("docs/audits/phase32/locked_params_top3.json")
       PERIOD = "2014-01-01..2018-12-31"
       UNIVERSE = "current-VN100"
       SELECTION_METRIC = "sharpe_rf3"
       ```

    3. `select_top3(df: pd.DataFrame) -> pd.DataFrame`:
       - Filter out rows with `sanity_flag == "ERROR"` (failed runs from D-07).
       - Sort by `["Sharpe_rf3", "CAGR", "MaxDD"]` with `ascending=[False, False, False]`.
         (Note: MaxDD is a negative number; "less severe" means closer to 0, i.e. larger value, so `False` ascending = bigger first = correct per D-12.)
       - Take top 3 rows.
       - If any of the 3 has `sanity_flag != "OK"`, raise `RuntimeError(f"Top-3 contains sanity-flagged config: {flagged}")` per D-13.
       - Return the 3-row DataFrame.

    4. `to_json_payload(top3: pd.DataFrame) -> dict`:
       ```python
       {
         "selected_at": date.today().isoformat(),
         "selection_metric": "sharpe_rf3",
         "period": PERIOD,
         "universe": UNIVERSE,
         "configs": [
           {
             "rank": i+1,
             "c_yoy": row.c_yoy, "a_cagr": row.a_cagr, "n_proximity": row.n_proximity,
             "hard_stop": row.hard_stop, "slots": int(row.slots), "entry_option": row.entry_option,
             "metrics": {col: row[col] for col in METRIC_COLS}
           }
           for i, row in enumerate(top3.itertuples())
         ]
       }
       ```

    5. `main()`:
       - Read SWEEP_CSV.
       - Print sweep summary (total rows, OK count, flagged count, error count).
       - Call `select_top3(df)` (raises if flagged).
       - Build payload and write to OUTPUT_JSON with `indent=2`.
       - Print top-3 table to stdout.

    6. `if __name__ == "__main__": main()`.

    Then create `tests/phase32/test_top3.py` implementing the 6 behaviors above. Use small synthetic DataFrames (no real CSV).
  </action>
  <verify>
    <automated>uv run pytest tests/phase32/test_top3.py -x -q && uv run python analysis/select_top3_vn100.py && test -f docs/audits/phase32/locked_params_top3.json</automated>
  </verify>
  <acceptance_criteria>
    - `analysis/select_top3_vn100.py` exists with `select_top3`, `to_json_payload`, `main`
    - `uv run pytest tests/phase32/test_top3.py -x -q` exits 0
    - `docs/audits/phase32/locked_params_top3.json` exists
    - `python -c "import json; d=json.load(open('docs/audits/phase32/locked_params_top3.json')); assert len(d['configs'])==3; assert d['selection_metric']=='sharpe_rf3'"` exits 0
    - JSON `configs[*]` has keys `rank, c_yoy, a_cagr, n_proximity, hard_stop, slots, entry_option, metrics`
  </acceptance_criteria>
  <done>BT-02 SC4 met. Phase 33 handoff JSON ready.</done>
</task>

<task type="auto">
  <name>Task 2: Write Phase 32 audit report (D-16)</name>
  <files>docs/audits/phase32-backtest-sweep.md</files>
  <read_first>
    - docs/audits/phase32/sweep_results.csv (summary stats)
    - docs/audits/phase32/locked_params_top3.json (top-3 details)
    - docs/audits/phase32/single_run_nav.csv (baseline NAV final value)
    - docs/audits/phase32/single_run_trades.csv (baseline trade count)
    - .planning/phases/32-vn100-backtest-in-sample-sweep/32-CONTEXT.md (D-16)
    - docs/audits/phase31-*.md if present (precedent layout)
  </read_first>
  <action>
    Create `docs/audits/phase32-backtest-sweep.md` with these sections:

    1. **Header**: `# Phase 32 — VN100 Backtest + In-Sample Sweep` and metadata block (date, period 2014-01-01..2018-12-31, universe current-VN100, requirements BT-01, BT-02).

    2. **Methodology**:
       - Pipeline: universe (Phase 29) → CANSLIM (Phase 29) → entry (Phase 30) → portfolio (Phase 31) → costs.
       - Period: 2014-01-01 to 2018-12-31 hard-coded (D-09).
       - Universe: current-VN100 (D-01), survivorship bias acknowledged.
       - MDM gate: HybridEngine v6 (Phase 31 D-07).
       - Costs: 0.25% commission + 0.10% tax + 0.10% slippage (Phase 31 D-22).
       - Sharpe: rf=3% (D-10).

    3. **Single-Run Baseline (BT-01 / SC1)**:
       - Config: Phase 31 defaults (max_slots=8, hard_stop=0.08, entry_option=A, c_yoy=0.20, a_cagr=0.15, n_proximity=0.15).
       - Read final NAV from `single_run_nav.csv` and compute CAGR/Sharpe/MaxDD; report as table.
       - Trade count from `single_run_trades.csv`.
       - Artifacts: link `single_run_nav.csv`, `single_run_trades.csv`, `single_run_positions.csv`.

    4. **Sweep Results (BT-02 / SC2-SC3)**:
       - Grid: 4×4×4×4×3×2 = 1,536 configs.
       - Read `sweep_results.csv`; report total configs, success count, error count, flagged count.
       - Sharpe distribution: min, p25, median, p75, max.
       - CAGR distribution: min, p25, median, p75, max.
       - MaxDD distribution.

    5. **Top-3 Configs (BT-02 / SC4)**:
       - Read `locked_params_top3.json`.
       - Render markdown table:
         ```
         | Rank | c_yoy | a_cagr | n_proximity | hard_stop | slots | entry | CAGR | Sharpe | MaxDD | num_trades |
         ```
       - Selection metric: Sharpe_rf3, tie-breakers CAGR desc → MaxDD asc (D-12).

    6. **Sanity Gate (SC5)**:
       - Report number of CAGR_TOO_HIGH rows.
       - Confirm none of top-3 are flagged (or note abort).

    7. **Artifacts**:
       - `docs/audits/phase32/single_run_*.csv`
       - `docs/audits/phase32/sweep_results.csv`
       - `docs/audits/phase32/locked_params_top3.json`
       - `docs/audits/phase32/cache/*.parquet`

    8. **Handoff to Phase 33**:
       - `locked_params_top3.json` will be loaded by Phase 33 helper (D-15).
       - OOS period 2019-2025, sensitivity across 3 universes (BT-03, BT-04).

    9. **Notes & Caveats**:
       - Survivorship bias from current-VN100 (deferred to Phase 33 sensitivity).
       - Failed configs (if any) listed at end of sweep_results.csv with `error` column populated.

    Use real numbers from the CSVs. Run small inline pandas snippets while writing to extract distributions — do not invent values.
  </action>
  <verify>
    <automated>test -f docs/audits/phase32-backtest-sweep.md && grep -q "Top-3" docs/audits/phase32-backtest-sweep.md && grep -q "BT-01" docs/audits/phase32-backtest-sweep.md && grep -q "BT-02" docs/audits/phase32-backtest-sweep.md && grep -q "Sharpe_rf3\|Sharpe" docs/audits/phase32-backtest-sweep.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/audits/phase32-backtest-sweep.md` exists
    - Sections: Methodology, Single-Run Baseline, Sweep Results, Top-3 Configs, Sanity Gate, Artifacts, Handoff, Notes
    - Mentions BT-01 and BT-02
    - Top-3 table has all 6 sweep params + key metrics
    - Numbers come from CSV/JSON files (not invented)
  </acceptance_criteria>
  <done>Phase 32 audit report complete; all BT-01/BT-02 deliverables documented.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Final user review of top-3 + report</name>
  <what-built>
    Top-3 selection JSON + Phase 32 audit report. User should confirm the locked params look reasonable before Phase 33 inherits them.
  </what-built>
  <how-to-verify>
    1. Open `docs/audits/phase32/locked_params_top3.json` — verify 3 configs, all `sanity_flag = OK`.
    2. Open `docs/audits/phase32-backtest-sweep.md` — read top-3 table.
    3. Sanity check: are top-3 Sharpe values plausible (e.g. 0.5–2.5 range)? CAGR < 100% (else flag suggests look-ahead)?
    4. Check error count from sweep: should be << 1,536.
    5. Confirm `docs/audits/phase32/cache/*.parquet` exists (precompute worked).
  </how-to-verify>
  <acceptance_criteria>
    - User reviews JSON + report
    - User confirms top-3 are reasonable or requests rerun
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe needed reruns</resume-signal>
</task>

</tasks>

<verification>
- `uv run pytest tests/phase32/test_top3.py -x -q` green
- `docs/audits/phase32/locked_params_top3.json` valid JSON with 3 configs
- `docs/audits/phase32-backtest-sweep.md` exists with all sections
- User approves
</verification>

<success_criteria>
BT-02 SC4 + SC5 closed. Phase 32 deliverables complete.
</success_criteria>

<output>
Create `.planning/phases/32-vn100-backtest-in-sample-sweep/32-03-SUMMARY.md`.
</output>
</content>
