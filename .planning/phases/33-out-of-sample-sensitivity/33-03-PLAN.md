---
phase: 33-out-of-sample-sensitivity
plan: 03
type: execute
wave: 3
depends_on: ["33-01", "33-02"]
files_modified:
  - strategies/canslim/universe.py
  - analysis/sensitivity_vn100.py
  - docs/audits/phase33/sensitivity_matrix.csv
  - docs/audits/phase33/baselines.json
  - docs/audits/phase33/verdict.md
autonomous: true
requirements: [BT-03, BT-04, BT-08]
gap_closure: true

must_haves:
  truths:
    - "liquidity-reconstructed universe mode returns valid ticker sets (not empty/error)"
    - "Sensitivity matrix has 9 rows with non-NaN metrics for all 3 universe modes"
    - "CANSLIM-only baseline produces >0 trades and non-zero Sharpe_rf3"
    - "verdict.md failure attribution uses valid canslim_only baseline data"
  artifacts:
    - path: "strategies/canslim/universe.py"
      provides: "Fixed SQL query using closeprice instead of closeindex"
      contains: "closeprice"
    - path: "analysis/sensitivity_vn100.py"
      provides: "Fixed canslim_only baseline with CASH->BUY transition"
      contains: "CASH"
    - path: "docs/audits/phase33/sensitivity_matrix.csv"
      provides: "Full 9-row matrix with non-NaN liquidity-reconstructed rows"
    - path: "docs/audits/phase33/baselines.json"
      provides: "canslim_only baseline with non-zero metrics"
    - path: "docs/audits/phase33/verdict.md"
      provides: "Updated failure attribution using valid baseline data"
      contains: "Failure Attribution"
  key_links:
    - from: "strategies/canslim/universe.py"
      to: "stock_eod table"
      via: "SQL query AVG(closeprice * totalvol)"
      pattern: "closeprice.*totalvol"
    - from: "analysis/sensitivity_vn100.py"
      to: "strategies/entry/window.py"
      via: "CASH->BUY transition triggers compute_buy_windows"
      pattern: "CASH.*BUY"
---

<objective>
Close two verification gaps from Phase 33: (1) fix the SQL column bug in UniverseLoader._liquidity_reconstructed() so the sensitivity matrix produces valid data for all 3 universe modes, and (2) fix the canslim_only baseline so it actually produces trades by providing a proper CASH->BUY transition for EntryEngine.

Purpose: BT-04 requires valid sensitivity data across all 3 universes. The failure attribution in verdict.md must use valid baseline measurements, not hollow 0-trade results.
Output: Fixed universe.py, fixed sensitivity script, regenerated sensitivity_matrix.csv, baselines.json, and verdict.md.
</objective>

<execution_context>
@C:/Users/trant/projects/mdm/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/trant/projects/mdm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-out-of-sample-sensitivity/33-CONTEXT.md
@.planning/phases/33-out-of-sample-sensitivity/33-VERIFICATION.md
@.planning/phases/33-out-of-sample-sensitivity/33-02-SUMMARY.md

<interfaces>
From strategies/canslim/universe.py (lines 74-92):
```python
def _liquidity_reconstructed(self, as_of_date: date) -> Set[str]:
    rebalance = self._last_rebalance_date(as_of_date)
    sql = """
        SELECT stockcode,
               AVG(closeindex * totalvol) AS adv   # BUG: closeindex does not exist in stock_eod
        FROM stock_eod
        WHERE tradingdate BETWEEN %(start)s AND %(end)s
        GROUP BY stockcode
        ORDER BY adv DESC
        LIMIT 100
    """
```

stock_eod actual columns (verified via DB query):
  stockcode, tradingdate, openprice, closeprice, highestprice, lowestprice,
  totalvol, totalval, totaladjustrate, basicprice, ceilingprice, floorprice, ...

The column `closeindex` exists in `index_eod` (for index data), NOT in `stock_eod`.
Fix: change `closeindex` to `closeprice`.

From strategies/entry/window.py (lines 29-59):
```python
def compute_buy_windows(state: pd.Series, window_days: int = 20):
    prev = state.shift(1)
    is_transition = (state == "BUY") & (prev.isin(["CASH", "SELL"]))
    # ...returns windows starting at each CASH/SELL -> BUY transition
```

The canslim_only baseline injects a constant all-"BUY" Series. Since there is
no CASH/SELL -> BUY transition, compute_buy_windows returns 0 windows, EntryEngine
produces 0 fills, PortfolioEngine produces 0 trades. Fix: set index[0] to "CASH"
so bar 1 is a CASH->BUY transition.

From analysis/sensitivity_vn100.py (lines 222-248):
```python
def run_canslim_only_baseline(configs):
    precomputed = precompute_static(PERIOD, mode="current-vn100")
    all_buy = pd.Series("BUY", index=precomputed["mdm_gate"].index, name="mdm_state")
    precomputed_override = dict(precomputed)
    precomputed_override["mdm_gate"] = all_buy
    result = run_vn100_backtest(canslim_cfg, portfolio_cfg, "C", PERIOD, precomputed_override)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix SQL column bug + canslim_only baseline transition, re-run sensitivity</name>
  <files>strategies/canslim/universe.py, analysis/sensitivity_vn100.py</files>
  <read_first>
    - strategies/canslim/universe.py
    - analysis/sensitivity_vn100.py
    - strategies/entry/window.py
    - docs/audits/phase33/sensitivity_matrix.csv
    - docs/audits/phase33/baselines.json
  </read_first>
  <action>
**Fix 1: SQL column name in `strategies/canslim/universe.py`**

In `_liquidity_reconstructed()` method (line 78), change the SQL query from:
```sql
AVG(closeindex * totalvol) AS adv
```
to:
```sql
AVG(closeprice * totalvol) AS adv
```

This is the only change needed in this file. The column `closeindex` does not exist in the `stock_eod` table (it exists in `index_eod`). The correct column for stock close prices is `closeprice`.

**Fix 2: CANSLIM-only baseline gate override in `analysis/sensitivity_vn100.py`**

In `run_canslim_only_baseline()` (around line 229), the current code creates an all-BUY gate:
```python
all_buy = pd.Series("BUY", index=precomputed["mdm_gate"].index, name="mdm_state")
```

The problem: `compute_buy_windows()` in `strategies/entry/window.py` detects CASH/SELL -> BUY transitions. A constant all-BUY series has NO transitions (shift(1) on first bar is NaN, not "CASH"/"SELL"), so EntryEngine produces 0 fills and PortfolioEngine produces 0 trades.

Fix: After creating `all_buy`, set the first bar to "CASH" so that bar index[1] is a CASH->BUY transition:
```python
all_buy = pd.Series("BUY", index=precomputed["mdm_gate"].index, name="mdm_state")
all_buy.iloc[0] = "CASH"  # Force a CASH->BUY transition on bar 1
```

This gives EntryEngine exactly one buy window covering essentially the entire period, which is the correct semantic for "no MDM gate" (always available to buy).

**After both fixes, re-run the sensitivity script:**
```bash
cd C:/Users/trant/projects/mdm && uv run python analysis/sensitivity_vn100.py
```

This regenerates:
- `docs/audits/phase33/sensitivity_matrix.csv` (liquidity-reconstructed rows now have real data)
- `docs/audits/phase33/baselines.json` (canslim_only now has non-zero metrics)
- `docs/audits/phase33/diem_canslim_oos.json` (unchanged, but re-validated)
- `docs/audits/phase33/verdict.md` (failure attribution now uses real canslim_only data)

The script takes several minutes due to DB queries for the 3 liquidity-reconstructed runs that were previously failing. Expect 9 sensitivity rows + baselines + diem + verdict output.

**Important:** Do NOT change locked parameters, OOS period, or any other config. BT-03 results (oos_nav.csv, oos_metrics.json) are NOT regenerated — they remain as-is.
  </action>
  <verify>
    <automated>cd C:/Users/trant/projects/mdm && uv run python -c "
import json, pandas as pd, numpy as np

# Check 1: SQL fix
with open('strategies/canslim/universe.py') as f:
    src = f.read()
assert 'closeprice' in src, 'closeprice not found in universe.py'
assert 'closeindex' not in src, 'closeindex still present in universe.py'

# Check 2: canslim_only baseline gate fix
with open('analysis/sensitivity_vn100.py') as f:
    src = f.read()
assert 'CASH' in src, 'CASH transition fix not found in sensitivity_vn100.py'

# Check 3: sensitivity matrix has no NaN rows
df = pd.read_csv('docs/audits/phase33/sensitivity_matrix.csv')
assert len(df) == 9, f'expected 9 rows, got {len(df)}'
liq = df[df['mode'] == 'liquidity-reconstructed']
assert not liq['Sharpe_rf3'].isna().any(), f'liquidity-reconstructed still has NaN: {liq[\"Sharpe_rf3\"].tolist()}'
print(f'sensitivity_matrix: 9 rows, no NaN. liq-recon Sharpe: {liq[\"Sharpe_rf3\"].tolist()}')

# Check 4: canslim_only baseline has non-zero metrics
b = json.load(open('docs/audits/phase33/baselines.json'))
co = b['canslim_only']
assert co['num_trades'] > 0, f'canslim_only still has 0 trades: {co}'
assert co['Sharpe_rf3'] != 0.0, f'canslim_only Sharpe still 0: {co}'
print(f'canslim_only: {co[\"num_trades\"]} trades, Sharpe={co[\"Sharpe_rf3\"]:.3f}, MaxDD={co[\"MaxDD\"]:.3f}')

print('ALL CHECKS PASSED')
"</automated>
  </verify>
  <acceptance_criteria>
    - `strategies/canslim/universe.py` contains `closeprice * totalvol` (not `closeindex * totalvol`) in _liquidity_reconstructed SQL
    - `strategies/canslim/universe.py` does NOT contain the string `closeindex` anywhere
    - `analysis/sensitivity_vn100.py` run_canslim_only_baseline contains `all_buy.iloc[0] = "CASH"` or equivalent CASH->BUY transition logic
    - `docs/audits/phase33/sensitivity_matrix.csv` has 9 rows where ALL Sharpe_rf3 values are non-NaN (including the 3 liquidity-reconstructed rows)
    - `docs/audits/phase33/baselines.json` canslim_only entry has `num_trades > 0` and `Sharpe_rf3 != 0.0`
  </acceptance_criteria>
  <done>SQL column bug fixed (closeprice replaces closeindex), canslim_only baseline produces real trades via CASH->BUY transition, sensitivity matrix and baselines regenerated with valid data for all 9 cells and all 3 baselines.</done>
</task>

<task type="auto">
  <name>Task 2: Validate regenerated verdict.md failure attribution</name>
  <files>docs/audits/phase33/verdict.md</files>
  <read_first>
    - docs/audits/phase33/verdict.md
    - docs/audits/phase33/baselines.json
    - docs/audits/phase33/oos_metrics.json
    - docs/audits/phase33/sensitivity_matrix.csv
  </read_first>
  <action>
After Task 1 re-ran the full sensitivity script (which calls `write_verdict()` at the end), verdict.md should already be regenerated with the updated canslim_only and sensitivity data.

**Verify the regenerated verdict.md is internally consistent:**

1. Read `docs/audits/phase33/verdict.md` and confirm:
   - Section 3 (BT-08 Pass/Fail): Sharpe uplift and MaxDD reduction values match oos_metrics.json vs baselines.json vnindex_bh
   - Section 4 (Sensitivity): liquidity-reconstructed rows show actual Sharpe values (not "N/A")
   - Section 5 (Baseline Comparison): canslim_only row shows non-zero CAGR, Sharpe, MaxDD
   - Section 7 (Failure Attribution): uses the REAL canslim_only Sharpe (not 0.000) in its attribution logic

2. If the regenerated verdict.md failure attribution logic is now different because canslim_only Sharpe is non-zero, verify the new attribution is logically correct:
   - If canslim_only Sharpe > B&H Sharpe (0.383): attribution should NOT blame CANSLIM scoring
   - If canslim_only Sharpe < B&H Sharpe (0.383): attribution correctly identifies CANSLIM scoring as weak
   - If strategy Sharpe (0.448) < canslim_only Sharpe: attribution identifies MDM gate as hurting
   - If MDM-only Sharpe (0.508) > strategy Sharpe (0.448): attribution identifies stock selection/entry as bottleneck

3. If verdict.md needs any corrections after validation (e.g., the auto-generated text is inconsistent), fix by calling:
   ```bash
   cd C:/Users/trant/projects/mdm && uv run python -c "from analysis.sensitivity_vn100 import write_verdict; write_verdict()"
   ```
   This re-reads the JSON/CSV data files and regenerates the verdict from scratch.

**Do NOT change any numbers in oos_metrics.json or the BT-08 pass/fail logic. Only validate that the verdict text accurately reflects the data.**
  </action>
  <verify>
    <automated>cd C:/Users/trant/projects/mdm && uv run python -c "
import json

# Load data sources
oos = json.load(open('docs/audits/phase33/oos_metrics.json'))
baselines = json.load(open('docs/audits/phase33/baselines.json'))
verdict_text = open('docs/audits/phase33/verdict.md').read()

# Verify verdict contains real canslim_only data (not 0.000)
co_sharpe = baselines['canslim_only']['Sharpe_rf3']
assert co_sharpe != 0.0, 'canslim_only Sharpe is still 0.0 in baselines.json'

# Verify verdict.md mentions actual Sharpe values
assert '0.000' not in verdict_text or 'canslim' not in verdict_text.split('0.000')[0][-50:], \
    'verdict.md still references canslim_only Sharpe=0.000'

# Verify key sections present
assert 'Failure Attribution' in verdict_text, 'Missing Failure Attribution section'
assert 'Sharpe uplift' in verdict_text, 'Missing Sharpe uplift reference'
assert 'liquidity-reconstructed' in verdict_text, 'Missing liquidity-reconstructed in sensitivity'

# Verify verdict has substantial content (not truncated)
assert len(verdict_text) > 1000, f'verdict.md too short: {len(verdict_text)} chars'

print(f'verdict.md: {len(verdict_text)} chars, canslim_only Sharpe={co_sharpe:.3f}')
print('VERDICT VALIDATION PASSED')
"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/audits/phase33/verdict.md` exists and is >1000 characters
    - verdict.md Section 5 (Baseline Comparison) shows canslim_only with non-zero Sharpe (not "0.000")
    - verdict.md Section 4 (Sensitivity) shows liquidity-reconstructed rows with actual numeric values (not "N/A" for all 3)
    - verdict.md Section 7 (Failure Attribution) references the real canslim_only Sharpe value from baselines.json, not 0.000
    - The BT-08 overall verdict (PASS/FAIL) is consistent with oos_metrics.json Sharpe=0.448 vs vnindex_bh Sharpe=0.383 (uplift=+0.065, still FAIL for >0.20 threshold)
  </acceptance_criteria>
  <done>verdict.md is regenerated with valid canslim_only baseline data and accurate liquidity-reconstructed sensitivity results. Failure attribution correctly uses real baseline measurements instead of hollow 0-trade results.</done>
</task>

</tasks>

<verification>
- `strategies/canslim/universe.py` uses `closeprice` (not `closeindex`) in _liquidity_reconstructed SQL
- `analysis/sensitivity_vn100.py` canslim_only baseline creates a CASH->BUY transition
- `docs/audits/phase33/sensitivity_matrix.csv` has 9 non-NaN rows across all 3 universe modes
- `docs/audits/phase33/baselines.json` canslim_only has num_trades > 0 and non-zero Sharpe
- `docs/audits/phase33/verdict.md` failure attribution uses real canslim_only metrics
</verification>

<success_criteria>
Both verification gaps closed:
- Gap 1 (BT-04): liquidity-reconstructed universe mode produces valid sensitivity metrics (all 9 matrix cells non-NaN)
- Gap 2 (BT-04/BT-08): canslim_only baseline produces real trades and non-zero metrics; verdict.md failure attribution is based on valid data
</success_criteria>

<output>
After completion, create `.planning/phases/33-out-of-sample-sensitivity/33-03-SUMMARY.md`
</output>
