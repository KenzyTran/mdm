---
phase: 999.1-fix-mdm-sell-reduce-slots
plan: 02
type: execute
wave: 2
depends_on: ["999.1-01"]
files_modified:
  - docs/rules_mdm_hybrid.md
autonomous: false
requirements:
  - SELL-REDUCE-03

must_haves:
  truths:
    - "Full OOS backtest runs without errors after partial-liquidation change"
    - "Attribution report shows mdm_sell exits are fewer than before (partial, not full liquidation)"
    - "docs/rules_mdm_hybrid.md documents the new SELL behavior"
  artifacts:
    - path: "docs/rules_mdm_hybrid.md"
      provides: "Updated SELL rule documentation"
      contains: "sell_retain_pct"
  key_links:
    - from: "docs/rules_mdm_hybrid.md"
      to: "strategies/portfolio/engine.py"
      via: "Documents MDM SELL partial liquidation behavior"
      pattern: "sell_retain_pct"
---

<objective>
Verify partial-liquidation change in full backtest and update documentation.

Purpose: Confirm the fix works end-to-end on real data (not just synthetic tests) and document the new behavior per Code-Docs Sync Rule.
Output: Verified backtest results showing correct partial liquidation, updated rule docs.
</objective>

<execution_context>
@C:/Users/trant/projects/mdm/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/trant/projects/mdm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/999.1-fix-mdm-sell-reduce-slots/999.1-01-SUMMARY.md
@strategies/portfolio/engine.py
@docs/rules_mdm_hybrid.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full OOS backtest and capture attribution diff</name>
  <files>docs/rules_mdm_hybrid.md</files>
  <read_first>
    - strategies/portfolio/engine.py (lines 440-480 to confirm partial liquidation is in place)
    - docs/rules_mdm_hybrid.md (current SELL documentation to update)
  </read_first>
  <action>
    1. Run the full OOS backtest (same command used in Phase 33):
       ```bash
       uv run python -m strategies.portfolio.run_oos 2>&1 | tail -40
       ```
       If `run_oos` does not exist, find the correct entry point:
       ```bash
       grep -rl "PortfolioEngine" scripts/ analysis/ --include="*.py" | head -5
       ```
       Run whatever script produces the OOS backtest with PortfolioEngine.

    2. Capture the exit_reason breakdown from the result. Count how many trades have exit_reason="mdm_sell". Compare mentally with Phase 33 OOS result (62 trades total). The mdm_sell count should be lower than before (since we now only close bottom 50%).

    3. Update `docs/rules_mdm_hybrid.md` — find the section describing MDM SELL behavior and update it:
       - Old behavior: "MDM SELL liquidates all open positions"
       - New behavior: "MDM SELL ranks open positions by RS (Relative Strength). Closes the bottom N% weakest positions (default: bottom 50%, configurable via `sell_retain_pct`). Retains the top N% strongest positions. Positions with missing/NaN RS are treated as weakest and closed first. Rounding uses ceiling (favors retaining). No new entries while gate=SELL."
       - Add the config parameter: `sell_retain_pct: float = 0.5` in the parameters section.
       - If there is no explicit SELL section, add one under the appropriate heading.

    4. Per CLAUDE.md Code-Docs Sync Rule: strategy logic changed in `strategies/portfolio/engine.py`, corresponding docs updated in `docs/rules_mdm_hybrid.md` in same plan.
  </action>
  <verify>
    <automated>cd C:/Users/trant/projects/mdm && grep -c "sell_retain_pct" docs/rules_mdm_hybrid.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/rules_mdm_hybrid.md contains "sell_retain_pct"
    - docs/rules_mdm_hybrid.md contains "bottom 50%" or "weakest" describing partial liquidation
    - docs/rules_mdm_hybrid.md contains "Relative Strength" or "RS" in SELL context
    - Full OOS backtest completes without errors (exit code 0)
  </acceptance_criteria>
  <done>OOS backtest runs cleanly with partial liquidation. Rule docs updated to reflect new SELL behavior.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify backtest results and attribution accuracy</name>
  <what-built>
    MDM SELL now closes only the weakest 50% of positions by RS instead of all positions.
    - Config: `sell_retain_pct = 0.5` in PortfolioConfig
    - Engine: RS-ranked partial liquidation in MDM SELL path
    - Tests: 5 new tests + 1 updated test, all passing
    - Docs: rules_mdm_hybrid.md updated
  </what-built>
  <how-to-verify>
    1. Review the OOS backtest output from Task 1 — check that mdm_sell exit count is reasonable (should be lower than total positions that were open during SELL periods)
    2. Confirm CAGR/Sharpe/MaxDD are in a reasonable range (may differ from Phase 33 OOS: CAGR=6.23%, Sharpe=0.448, MaxDD=-10.22% since we now retain positions)
    3. Run: `uv run python -m pytest tests/strategies/portfolio/ -v` to see all tests pass
    4. Review `docs/rules_mdm_hybrid.md` SELL section for accuracy
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. `grep "sell_retain_pct" docs/rules_mdm_hybrid.md` — documentation updated
2. Full OOS backtest exits cleanly
3. User approves attribution results
</verification>

<success_criteria>
- Full backtest runs without errors post-change
- Rule documentation reflects new partial-liquidation SELL behavior
- User verifies attribution accuracy is improved
</success_criteria>

<output>
After completion, create `.planning/phases/999.1-fix-mdm-sell-reduce-slots/999.1-02-SUMMARY.md`
</output>
