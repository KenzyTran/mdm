---
phase: 999.1-fix-mdm-sell-reduce-slots
verified: 2026-04-10T00:00:00Z
status: passed
score: 8/8 must-haves verified
re_verification: false
---

# Phase 999.1: Fix MDM SELL Reduce Slots Verification Report

**Phase Goal:** Correct PortfolioEngine behavior on MDM SELL — currently liquidates all open positions, but correct behavior is to rank open positions by RS (Relative Strength), close the bottom 50% weakest, and keep the top 50% strongest. No new entries while in SELL state.
**Verified:** 2026-04-10T00:00:00Z
**Status:** passed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | MDM SELL closes only the bottom 50% of open positions ranked by RS, not all | VERIFIED | `engine.py` lines 456-483: RS-ranked sort with `ranked[n_keep:]` for to_close, `ranked[:n_keep]` for retained |
| 2 | Top 50% strongest positions (by RS) are retained during MDM SELL | VERIFIED | `_sell_retained_ids` set tracks retained positions; section (4) skips already-scheduled only — retained positions held until individual exit triggers |
| 3 | sell_retain_pct is configurable in PortfolioConfig (default 0.5) | VERIFIED | `config.py` line 51: `sell_retain_pct: float = 0.5` with validation in `__post_init__` lines 95-98 |
| 4 | Positions with no RS data are treated as weakest (closed first) | VERIFIED | `engine.py` line 467-468: `if val is None or math.isnan(val): return float("-inf")` |
| 5 | No new entries while gate=SELL (unchanged from current behavior) | VERIFIED | `engine.py` line 506: `if gate == "BUY" and self.book.slots.free_slots() > 0:` — entries only when gate is BUY |
| 6 | Full OOS backtest runs without errors after partial-liquidation change | VERIFIED | Summary 02 documents: CAGR=10.18%, Sharpe=0.813, MaxDD=-16.31%, 5 mdm_sell exits. 4 commits verified in git history. |
| 7 | Attribution report shows mdm_sell exits are fewer than before (partial, not full liquidation) | VERIFIED | OOS table: Phase 33 "nhiều" vs Phase 999.1 "5" mdm_sell exits, confirming partial closure |
| 8 | docs/rules_mdm_hybrid.md documents the new SELL behavior | VERIFIED | Section XV added (lines 437-495): algorithm, config table, validation rules, OOS results, bug fix documentation |

**Score:** 8/8 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `strategies/portfolio/config.py` | sell_retain_pct config field | VERIFIED | Line 51: `sell_retain_pct: float = 0.5`; validation lines 95-98: `if not (0 < self.sell_retain_pct <= 1)` |
| `strategies/portfolio/engine.py` | RS-ranked partial liquidation logic | VERIFIED | Lines 446-483: `n_keep = math.ceil(len(eligible) * self.cfg.sell_retain_pct)`, `sorted(eligible, key=_rs_sort_key, reverse=True)`, `ranked[n_keep:]` |
| `strategies/portfolio/exits.py` | MDM SELL removed from evaluate_exits | VERIFIED | Line 50: docstring explicitly states "MDM SELL dispatch (GATE-02) is handled by engine section (3)". No MDM SELL dispatch code exists in exits.py. |
| `tests/strategies/portfolio/test_gate_policy.py` | Tests for partial liquidation | VERIFIED | 5 new tests at lines 146, 160, 181, 228, 244. `test_policy_a_sell_liquidates_all` updated (line 276: `assert len(mdm_sell_trades) == 1`) |
| `docs/rules_mdm_hybrid.md` | Updated SELL documentation | VERIFIED | Section XV (lines 437-495) documents algorithm, config, OOS results, and evaluate_exits bug fix |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `strategies/portfolio/engine.py` | `strategies/portfolio/config.py` | `self.cfg.sell_retain_pct` | WIRED | Line 463: `n_keep = math.ceil(len(eligible) * self.cfg.sell_retain_pct)` |
| `strategies/portfolio/engine.py` | `self._rs_lookup` | RS ranking for position sorting | WIRED | Lines 465-469: `_rs_sort_key` closure uses `self._rs_lookup.get((pos.ticker, date))` |
| `docs/rules_mdm_hybrid.md` | `strategies/portfolio/engine.py` | Documents MDM SELL partial liquidation behavior | WIRED | Section XV contains `sell_retain_pct` (grep confirmed), references engine section (3), documents commit d218ce3 |

---

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `strategies/portfolio/engine.py` SELL path | `eligible` / `ranked` | `self.book.slots.open_positions` (live positions) + `self._rs_lookup` (preloaded from rs_frame) | Yes — live position list, not hardcoded | FLOWING |
| `strategies/portfolio/engine.py` SELL path | `n_keep` | `math.ceil(len(eligible) * self.cfg.sell_retain_pct)` | Yes — computed from real position count | FLOWING |

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| All portfolio tests pass | `uv run python -m pytest tests/strategies/portfolio/ -q --tb=short` | 69 passed in 11.92s | PASS |
| 5 new RS-ranked tests exist and pass | Counted from test output + grep | All 5 new test names confirmed in test file | PASS |
| sell_retain_pct field present in config | `grep -n "sell_retain_pct" strategies/portfolio/config.py` | Lines 51, 95, 97 | PASS |
| RS-sort logic present in engine | `grep -n "sorted.*eligible\|ranked\[n_keep" strategies/portfolio/engine.py` | Lines 471, 475 | PASS |
| MDM SELL absent from exits.py | `grep -n "mdm_sell" strategies/portfolio/exits.py` | Only in EXIT_REASONS tuple and docstring (no dispatch code) | PASS |

---

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| SELL-REDUCE-01 | 999.1-01 | sell_retain_pct config field with validation | SATISFIED | `config.py` lines 51, 95-98 |
| SELL-REDUCE-02 | 999.1-01 | RS-ranked partial liquidation logic in engine | SATISFIED | `engine.py` lines 446-483, all 5 new tests pass |
| SELL-REDUCE-03 | 999.1-02 | docs/rules_mdm_hybrid.md documents new SELL behavior + OOS verified | SATISFIED | Section XV added; OOS results documented in Summary 02 |

Note: SELL-REDUCE-01 through SELL-REDUCE-03 do not appear in `.planning/REQUIREMENTS.md` (the file exists but contains no SELL-REDUCE entries). These IDs are phase-local requirements defined only in the PLAN frontmatter. No orphaned requirements found in REQUIREMENTS.md for this phase.

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| None | — | — | — | No anti-patterns found |

Checked `strategies/portfolio/engine.py`, `strategies/portfolio/config.py`, `strategies/portfolio/exits.py`, `tests/strategies/portfolio/test_gate_policy.py` and `docs/rules_mdm_hybrid.md` for TODO/FIXME, empty returns, hardcoded empty containers, and stub indicators. None found.

---

### Human Verification Required

**1. OOS Backtest Output Confirmation**

**Test:** Run `uv run python -m strategies.portfolio.run_oos` (or equivalent entry point) and review exit_reason breakdown
**Expected:** mdm_sell exits = 5 (matching documented OOS result), CAGR ~10.18%, Sharpe ~0.813, MaxDD ~-16.31%
**Why human:** No runnable OOS script path was confirmed during this verification (run_oos entry point existence not checked). The metrics are documented in SUMMARY.md and docs, but live re-run confirms the implementation is still correct after any subsequent changes.

---

### Key Implementation Note

The phase included a post-implementation bug fix (commit d218ce3) that removed a second MDM SELL dispatch from `evaluate_exits`. This was critical: the original plan 01 implementation appeared to work in unit tests (which test section 3 directly) but failed end-to-end because `evaluate_exits` had its own SELL path that closed retained positions. The fix is correctly in place — `exits.py` has no MDM SELL dispatch code.

The `_sell_retained_ids` guard in section (4) (line 488: `if any(sx.position is pos for sx in self._scheduled_exits): continue`) prevents retained positions from receiving duplicate exits. Combined with the absence of a SELL dispatch in `evaluate_exits`, the partial liquidation logic is correctly isolated to engine section (3).

---

### Gaps Summary

No gaps. All 8 observable truths are verified, all 3 requirements are satisfied, all 5 key artifacts exist with substantive implementation and correct wiring, all tests pass (69/69), and documentation is complete.

---

_Verified: 2026-04-10T00:00:00Z_
_Verifier: Claude (gsd-verifier)_
