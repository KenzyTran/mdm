---
phase: 30-stock-level-entry-confirmation
plan: 02
type: execute
wave: 2
depends_on: ["30-01"]
files_modified:
  - strategies/entry/option_a.py
  - strategies/entry/option_c.py
  - strategies/entry/window.py
  - strategies/entry/__init__.py
  - tests/entry/test_option_a.py
  - tests/entry/test_option_c.py
  - tests/entry/test_window.py
autonomous: true
requirements: [ENTRY-01, ENTRY-02, ENTRY-03]
must_haves:
  truths:
    - "detect_option_a(df, cfg) returns bool Series; fires iff all four D-03 clauses hold on bar t"
    - "detect_option_c(df, cfg) returns bool Series; fires iff all four D-05 clauses hold; fail-closed on zero-down-days in last pocket_lookback sessions"
    - "compute_buy_windows(state, window_days) returns list of (start_date, end_date_inclusive) tuples, one per CASH/SELL→BUY transition; day-1 inclusive; no extension on continuous BUY; series-starts-in-BUY does NOT open a window"
    - "All detector tests and window tests are GREEN (importorskip converted to real assertions)"
    - "Option A 52wk high test uses close.shift(1).rolling(252).max() — off-by-one guard test passes"
  artifacts:
    - path: "strategies/entry/option_a.py"
      provides: "detect_option_a(df: pd.DataFrame, cfg: EntryConfig) -> pd.Series[bool]"
      min_lines: 25
      contains: "def detect_option_a"
    - path: "strategies/entry/option_c.py"
      provides: "detect_option_c(df: pd.DataFrame, cfg: EntryConfig) -> pd.Series[bool]"
      min_lines: 30
      contains: "def detect_option_c"
    - path: "strategies/entry/window.py"
      provides: "compute_buy_windows(state, window_days) + find_window(signal_date, windows) helpers"
      min_lines: 30
      contains: "def compute_buy_windows"
  key_links:
    - from: "strategies/entry/option_a.py"
      to: "adj_close.shift(1).rolling(cfg.high_lookback).max()"
      via: "vectorized pandas rolling with explicit shift(1)"
      pattern: "shift\\(1\\).*rolling"
    - from: "strategies/entry/option_c.py"
      to: "down_day_vols filtered via .where(is_down_day), then .shift(1).rolling(pocket_lookback).max()"
      via: "NaN propagation for fail-closed"
      pattern: "where.*shift\\(1\\).*rolling"
    - from: "strategies/entry/window.py"
      to: "state.shift(1).isin(['CASH','SELL']) & (state == 'BUY')"
      via: "transition detection via prior-bar compare"
      pattern: "isin.*CASH.*SELL"
---

<objective>
Build the three pure-function building blocks of Phase 30: Option A detector, Option C (Pocket Pivot) detector, and the MDM-BUY window tracker. All three are stateless and per-ticker (detectors) or per-state-series (window), enabling the engine in 30-03 to be a thin orchestrator.

Purpose: Implements ENTRY-01, ENTRY-02, ENTRY-03. These are the locked formulas from CONTEXT.md D-03..D-10; no design latitude remains — plan executor must produce the exact semantics in the RED tests from 30-01.

Output: Three new modules under `strategies/entry/`, green tests for all three.
</objective>

<execution_context>
@C:/Users/trant/projects/mdm/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/trant/projects/mdm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/30-stock-level-entry-confirmation/30-CONTEXT.md
@.planning/phases/30-stock-level-entry-confirmation/30-RESEARCH.md
@.planning/phases/30-stock-level-entry-confirmation/30-01-NOTES.md
@.planning/phases/30-stock-level-entry-confirmation/30-01-SUMMARY.md
@strategies/entry/config.py
@strategies/entry/__init__.py
@tests/entry/conftest.py
@tests/entry/test_option_a.py
@tests/entry/test_option_c.py
@tests/entry/test_window.py
@strategies/mdm_hybrid/position_manager.py

<interfaces>
Per 30-RESEARCH.md §Pattern 1 + §Code Examples, locked function signatures:

```python
# strategies/entry/option_a.py
def detect_option_a(df: pd.DataFrame, cfg: EntryConfig) -> pd.Series:
    """Fire on 52wk high + vol surge + up bar + upper-half close.
    df: single-ticker adjusted OHLCV, sorted ascending by date,
        columns: adj_open, adj_high, adj_low, adj_close, totalvol.
    Returns: bool Series aligned to df.index.
    """

# strategies/entry/option_c.py
def detect_option_c(df: pd.DataFrame, cfg: EntryConfig) -> pd.Series:
    """Fire on pocket pivot (D-05). Fail-closed when zero down days in last cfg.pocket_lookback sessions."""

# strategies/entry/window.py
def compute_buy_windows(state: pd.Series, window_days: int = 20) -> list[tuple]:
    """Return list of (start_date, end_date_inclusive) per CASH/SELL->BUY transition.
    state: pd.Series indexed by date, values in {"BUY","CASH","SELL"}.
    Day 1 = transition bar itself. 20-day window inclusive => bars [t, t+19].
    No extension on continuous BUY. series-starts-in-BUY => NO initial window.
    """

def find_window(signal_date, windows: list[tuple]) -> tuple[int | None, int | None]:
    """Return (window_index, days_since_buy) if signal_date falls within any window, else (None, None).
    days_since_buy is 1-indexed (1 = transition bar itself).
    """
```

Skeleton implementations provided verbatim in 30-RESEARCH.md §Code Examples — executor should adapt and extend with full docstrings.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Option A detector (ENTRY-01)</name>
  <files>strategies/entry/option_a.py, strategies/entry/__init__.py, tests/entry/test_option_a.py</files>
  <behavior>
    - All six test cases from 30-01 test_option_a.py become GREEN:
      * test_option_a_fires_on_full_breakout
      * test_option_a_fails_not_new_high (strict > required)
      * test_option_a_fails_volume_below_1_5x
      * test_option_a_fails_down_bar
      * test_option_a_fails_lower_half_close
      * test_option_a_excludes_today_from_52wk_max (shift(1) off-by-one guard)
  </behavior>
  <action>
Implement `strategies/entry/option_a.py` per the skeleton in 30-RESEARCH.md §Pattern 1. Key points (D-03, D-04, pitfall #1):

1. Extract `c, o, h, lo, v = df["adj_close"], df["adj_open"], df["adj_high"], df["adj_low"], df["totalvol"]`.
2. `hi_ref = c.shift(1).rolling(cfg.high_lookback).max()` — **MUST include `shift(1)`** to exclude today's close from the 252-window reference (off-by-one pitfall). Guard test asserts this.
3. `avg_vol = v.shift(1).rolling(cfg.vol_lookback).mean()` — also shift(1) to exclude today from volume baseline.
4. Four conditions ANDed:
   - `c > hi_ref` (strict greater — test asserts equal-to-max does NOT fire)
   - `v >= cfg.vol_mult * avg_vol`
   - `c > o`
   - `c >= (h + lo) / 2.0`
5. `return (cond_a & cond_b & cond_c & cond_d).fillna(False)` — NaN warmup bars become False, not NaN.
6. Google-style docstring citing D-03 semantics.
7. Update `strategies/entry/__init__.py` to re-export `detect_option_a`.
8. Convert `pytest.importorskip` gating in `tests/entry/test_option_a.py` to a plain import (remove the skip guard) — tests now run live against the real module.

Per CLAUDE.md: snake_case, 4-space indent, Google docstring with Args/Returns.
  </action>
  <verify>
    <automated>uv run pytest tests/entry/test_option_a.py -x -q</automated>
  </verify>
  <done>test_option_a.py all GREEN including the shift(1) off-by-one guard; detect_option_a importable from strategies.entry.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Option C (Pocket Pivot) detector (ENTRY-02)</name>
  <files>strategies/entry/option_c.py, strategies/entry/__init__.py, tests/entry/test_option_c.py</files>
  <behavior>
    - All five test cases from 30-01 test_option_c.py become GREEN:
      * test_option_c_fires_pocket_pivot
      * test_option_c_fails_closed_zero_down_days (explicit NaN-propagation fail-closed)
      * test_option_c_fails_below_ma50
      * test_option_c_fails_below_base_tolerance (close at 84% of 50d high)
      * test_option_c_fails_down_bar
  </behavior>
  <action>
Implement `strategies/entry/option_c.py` using the exact idiom from 30-RESEARCH.md §Code Examples (Option C "down-day vol max"). Critical rules (D-05, D-06, pitfall #2):

1. `prev_close = c.shift(1); is_down_day = c < prev_close`
2. `down_vols = v.where(is_down_day)` — NaN on up days, volume on down days.
3. `max_down_vol = down_vols.shift(1).rolling(cfg.pocket_lookback, min_periods=1).max()` — shift(1) excludes today; min_periods=1 allows partial warmup BUT all-NaN windows still produce NaN (pandas default) → `vol > NaN` evaluates False → **fail-closed naturally**. Do NOT `fillna(0)` — that would be a free-pass bug (pitfall #2).
4. `ma50 = c.rolling(cfg.ma_length).mean()` — simple MA on adjusted close (D-06).
5. `hi_50 = h.shift(1).rolling(cfg.ma_length).max()` — 50-day high reference, shift(1) to exclude today.
6. Four conditions:
   - `c > o` (up bar)
   - `c >= ma50` (above MA50)
   - `v > max_down_vol` (pocket pivot volume; NaN propagates False)
   - `c >= (1.0 - cfg.base_tolerance) * hi_50` (within 15% of 50d high)
7. `return (...).fillna(False)`.
8. Update `strategies/entry/__init__.py` to re-export `detect_option_c`.
9. Remove `pytest.importorskip` from test_option_c.py.

Include an inline comment on the `max_down_vol` line explaining "NaN propagation = fail-closed per D-05 pitfall #2".
  </action>
  <verify>
    <automated>uv run pytest tests/entry/test_option_c.py -x -q</automated>
  </verify>
  <done>test_option_c.py all GREEN; zero-down-days test explicitly asserts no fire via NaN propagation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: MDM-BUY window tracker (ENTRY-03)</name>
  <files>strategies/entry/window.py, strategies/entry/__init__.py, tests/entry/test_window.py</files>
  <behavior>
    - All seven test cases from 30-01 test_window.py become GREEN:
      * test_window_day_1_inclusive
      * test_window_day_20_inclusive
      * test_window_day_21_rejected
      * test_window_no_extension_on_continuous_buy
      * test_window_reset_on_cash_to_buy_transition
      * test_window_sell_to_buy_is_transition
      * test_window_series_starts_in_buy_no_initial_window (per 30-01-NOTES.md locked decision)
  </behavior>
  <action>
Implement `strategies/entry/window.py` with two public functions (D-07..D-10, pitfall #3):

1. `compute_buy_windows(state: pd.Series, window_days: int = 20) -> list[tuple[pd.Timestamp, pd.Timestamp]]`:
   - `prev = state.shift(1)` — NaN on first bar.
   - `is_transition = (state == "BUY") & (prev.isin(["CASH", "SELL"]))` — note: NaN.isin returns False, so series-starts-in-BUY does NOT match (implementing the 30-01-NOTES.md locked decision naturally).
   - Iterate `state.index[is_transition]`; for each transition bar `t`, compute end bar as the 20th bar inclusive: `idx.get_loc(t) + window_days - 1`, clamped to `len(idx) - 1`. Append `(idx[i], idx[j])`.
   - Window is [start, end] inclusive — day 1 = transition bar itself (pitfall #3: do not off-by-one).
   - No extension: only the transition bar anchors; continuous BUY does NOT reset. Test `test_window_no_extension_on_continuous_buy` proves this.
   - Reset on new transition: each CASH/SELL→BUY pair emits its own window (independent 20-day clocks, possibly overlapping if a new transition occurs mid-window — record both).

2. `find_window(signal_date, windows) -> tuple[int | None, int]`:
   - Linear scan (windows list is small — tens, not thousands).
   - If signal_date in [start, end] of any window, return `(window_index, days_since_buy)` where `days_since_buy` is 1-indexed: `bars_between(start, signal_date) + 1`. Use `signal_date - start + 1` if `state.index` is contiguous trading days OR pass the state's index to compute bar offset (safer; implementer picks — recommend accepting `trading_days_index: pd.DatetimeIndex | None = None` parameter so engine can pass it).
   - If not in any window, return `(None, None)`.

3. Google-style docstrings on both functions citing D-07..D-10 and the 30-01-NOTES.md decision on series-starts-in-BUY.

4. Update `strategies/entry/__init__.py` to re-export `compute_buy_windows, find_window`.

5. Remove `pytest.importorskip` from test_window.py.

Note on implementation style: researcher suggested either generator or materialized list — go with **materialized list** (simpler, matches test shape, windows are few).
  </action>
  <verify>
    <automated>uv run pytest tests/entry/test_window.py -x -q</automated>
  </verify>
  <done>test_window.py all GREEN including the series-starts-in-BUY no-initial-window case per 30-01-NOTES.md.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/entry/test_option_a.py tests/entry/test_option_c.py tests/entry/test_window.py -q` — all GREEN
- `uv run pytest tests/entry/ -q` — config + detectors + window all pass; engine + ab_report still SKIP (30-03 pending)
- `python -c "from strategies.entry import EntryConfig, detect_option_a, detect_option_c, compute_buy_windows, find_window"` — all imports succeed
- `grep -n "shift(1).rolling" strategies/entry/option_a.py` — confirms off-by-one fix present
- `grep -n "fillna(0)" strategies/entry/option_c.py` — should return NOTHING (free-pass bug absent)
</verification>

<success_criteria>
1. Three detector/tracker modules implement locked semantics from D-03..D-10
2. Option A uses `close.shift(1).rolling(252).max()` (off-by-one guard test green)
3. Option C fails-closed on zero-down-days via NaN propagation (no `fillna(0)` anti-pattern)
4. Window tracker correctly handles: day-1 inclusive, day-21 rejected, no extension on continuous BUY, SELL→BUY transition, series-starts-in-BUY no-initial-window
5. `uv run pytest tests/entry/` shows only 30-03 files skipping (engine + ab_report); everything else green
</success_criteria>

<output>
After completion, create `.planning/phases/30-stock-level-entry-confirmation/30-02-SUMMARY.md` per template. Include:
- Function signatures and semantics for detect_option_a, detect_option_c, compute_buy_windows, find_window
- Which pitfalls the implementation explicitly guards against (reference pitfall #1, #2, #3)
- Columns/dtypes the detectors expect on input DataFrames
- Integration note for 30-03: how the engine should chain `compute_buy_windows` → `find_window` → dedup key `(ticker, window_id, detector_name)`
</output>