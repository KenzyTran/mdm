---
phase: 30-stock-level-entry-confirmation
plan: 03
type: execute
wave: 3
depends_on: ["30-01", "30-02"]
files_modified:
  - strategies/entry/engine.py
  - strategies/entry/ab_report.py
  - strategies/entry/__init__.py
  - tests/entry/test_engine.py
  - tests/entry/test_ab_report.py
  - docs/rules_entry.md
  - docs/audits/phase30-entry-ab.md
  - docs/audits/phase30/entry_ab_raw_vn100.csv
  - docs/audits/phase30/entry_ab_canslim.csv
autonomous: true
requirements: [ENTRY-04, ENTRY-05]
must_haves:
  truths:
    - "EntryEngine orchestrates per-ticker detection → window gate → dedup → next-day-open fill"
    - "Fill price = adj_open at bar t+1 for signal at bar t; last-bar signals recorded as unfilled with reason 'no next bar'"
    - "A and C are independent streams — same ticker can contribute one fill to each stream in same window"
    - "Dedup key (ticker, window_id, detector_name) — within-stream first-fire-only"
    - "No look-ahead: shuffling post-signal-bar MDM state leaves fills unchanged (state[i-1] discipline)"
    - "A/B report runs twice — raw VN100 + CANSLIM-qualified — with all D-19 metrics side-by-side"
    - "docs/rules_entry.md documents Option A/C formulas + window + fill model in same commit (Code-Docs Sync Rule)"
  artifacts:
    - path: "strategies/entry/engine.py"
      provides: "EntryEngine(mdm_state, universe_tickers, canslim_scores, price_loader, config) + run() → Fills dataframe"
      min_lines: 80
      contains: "class EntryEngine"
    - path: "strategies/entry/ab_report.py"
      provides: "Script producing docs/audits/phase30-entry-ab.md + CSVs"
      min_lines: 60
    - path: "docs/rules_entry.md"
      provides: "Locked rules doc per Code-Docs Sync Rule"
      contains: "Option A"
    - path: "docs/audits/phase30-entry-ab.md"
      provides: "A/B report on VN100 2014-2025"
      contains: "Option A"
  key_links:
    - from: "strategies/entry/engine.py"
      to: "strategies/entry/option_a.detect_option_a + option_c.detect_option_c"
      via: "per-ticker call in main loop"
      pattern: "detect_option_a"
    - from: "strategies/entry/engine.py"
      to: "strategies/entry/window.compute_buy_windows + find_window"
      via: "window gate on signal bar"
      pattern: "compute_buy_windows"
    - from: "strategies/entry/engine.py"
      to: "connectors/adjust.adjust_ohlc"
      via: "all raw OHLCV frames pass through adjust_ohlc before detectors (D-02)"
      pattern: "adjust_ohlc"
    - from: "strategies/entry/ab_report.py"
      to: "strategies/canslim.CanslimScorer output per-letter booleans"
      via: "left-join on (date, ticker), filter c_pass & a_pass & n_pass & s_pass & l_pass & i_pass & liq_pass (D-16)"
      pattern: "c_pass.*a_pass.*n_pass"
---

<objective>
Wire the detectors and window tracker from 30-02 into `EntryEngine`, build the A/B helper that runs on VN100 2014-2025 twice (raw + CANSLIM-qualified), emit the Markdown+CSV audit report, and create `docs/rules_entry.md` per Code-Docs Sync Rule.

Purpose: Closes ENTRY-04 and ENTRY-05 — the two remaining requirements. This is the phase's deliverable to Phase 31: a reproducible stream of `(signal_date, fill_date, ticker, detector, fill_price, window_id, days_since_buy)` records, plus side-by-side A vs C comparison on the real VN100 universe.

Output:
- `EntryEngine` orchestrator + engine tests GREEN
- `ab_report.py` script + tests GREEN
- `docs/audits/phase30-entry-ab.md` + CSVs (real VN100 run 2014-2025)
- `docs/rules_entry.md` (Code-Docs Sync)
</objective>

<execution_context>
@C:/Users/trant/projects/mdm/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/trant/projects/mdm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/30-stock-level-entry-confirmation/30-CONTEXT.md
@.planning/phases/30-stock-level-entry-confirmation/30-RESEARCH.md
@.planning/phases/30-stock-level-entry-confirmation/30-01-NOTES.md
@.planning/phases/30-stock-level-entry-confirmation/30-01-SUMMARY.md
@.planning/phases/30-stock-level-entry-confirmation/30-02-SUMMARY.md
@strategies/entry/config.py
@strategies/entry/option_a.py
@strategies/entry/option_c.py
@strategies/entry/window.py
@strategies/canslim/scorer.py
@strategies/canslim/config.py
@connectors/adjust.py
@connectors/postgres.py
@strategies/mdm_hybrid/mdm_hybrid_engine.py
@tests/entry/test_engine.py
@tests/entry/test_ab_report.py

<interfaces>
Per 30-RESEARCH.md §Code Context and §Pattern 3, locked engine signature:

```python
# strategies/entry/engine.py
from dataclasses import dataclass
import pandas as pd

@dataclass
class Fill:
    signal_date: pd.Timestamp
    fill_date: pd.Timestamp | None     # None if unfilled
    ticker: str
    detector: str                       # "A" or "C"
    fill_price: float | None            # None if unfilled
    window_id: int
    days_since_buy: int                 # 1-indexed
    status: str                         # "filled" or "unfilled: no next bar"

class EntryEngine:
    def __init__(
        self,
        mdm_state: pd.Series,                     # indexed by date, values in {"BUY","CASH","SELL"}
        universe_tickers: list[str],
        price_loader,                             # callable: ticker -> raw OHLCV DataFrame
        config: EntryConfig,
        canslim_scores: pd.DataFrame | None = None,  # None = raw universe (D-16 raw table)
    ):
        ...

    def run(self) -> pd.DataFrame:
        """Return Fills DataFrame with columns:
        signal_date, fill_date, ticker, detector, fill_price, window_id, days_since_buy, status
        Per D-11..D-15.
        """
```

Fill record reference — matches 30-RESEARCH.md §Pattern 3 pseudocode.

Report metrics per D-19:
- total_fills
- unique_tickers
- windows_with_fills (count of windows producing ≥1 fill)
- mean_fills_per_window
- days_since_buy_histogram (dict: 1..20 → count)
- ab_overlap (set of tickers filled by BOTH A and C within the same window)
</interfaces>

**Canslim join semantics (D-16):** Filter is `c_pass & a_pass & n_pass & s_pass & l_pass & i_pass & liq_pass` on the **signal bar** (not the fill bar). Per 30-01-NOTES.md resolution of OQ #2, forward-fill the per-letter booleans onto the daily price index before joining (if scorer is sparse) or left-join directly (if dense). The notes file is authoritative.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: EntryEngine orchestrator + engine tests (ENTRY-04)</name>
  <files>strategies/entry/engine.py, strategies/entry/__init__.py, tests/entry/test_engine.py</files>
  <behavior>
    - All five test cases from 30-01 test_engine.py become GREEN:
      * test_fill_next_open — signal at bar t → fill_price == df.loc[t+1, "adj_open"]
      * test_last_bar_unfilled — signal on last bar → Fill.status == "unfilled: no next bar", fill_date/fill_price None
      * test_no_lookahead_state_shuffle — shuffling state bars AFTER signal bars leaves fills unchanged
      * test_ac_streams_independent — same ticker, same bar, both detectors fire → two Fill records
      * test_within_stream_dedup_first_fire_only — same ticker, same window, detector A fires twice → one Fill
  </behavior>
  <action>
Implement `strategies/entry/engine.py`:

1. `Fill` dataclass per interfaces block above (frozen=True for safety).

2. `EntryEngine` class following 30-RESEARCH.md §Pattern 3 pseudocode:
   - `__init__` stores inputs, calls `compute_buy_windows(mdm_state, config.window_days)` once.
   - `run()` iterates `universe_tickers`:
     - `raw = self.price_loader(ticker)` → raw OHLCV DataFrame with date index.
     - `df = adjust_ohlc(raw)` — **D-02: mandatory**. Do NOT call detectors on raw frame.
     - Align to `mdm_state.index` intersection (drop bars outside MDM state coverage).
     - `fires_a = detect_option_a(df, self.config)`
     - `fires_c = detect_option_c(df, self.config)`
     - For each detector in `[("A", fires_a), ("C", fires_c)]`:
       - Iterate signal dates (`df.index[fires]`).
       - `window_id, days_since_buy = find_window(signal_date, self.windows)`
       - If `window_id is None` → skip (out of window).
       - Apply CANSLIM filter if `self.canslim_scores is not None`: look up per-letter booleans for `(signal_date, ticker)`; if any of `c_pass, a_pass, n_pass, s_pass, l_pass, i_pass, liq_pass` is False/missing → skip.
       - Dedup key `(ticker, window_id, detector_name)` — maintain a set; if already seen, skip (D-15 first-fire-only).
       - Compute fill bar: `next_bar = df.index[df.index.get_loc(signal_date) + 1]` if index position < len-1, else None.
       - If `next_bar is None` → append `Fill(..., fill_date=None, fill_price=None, status="unfilled: no next bar")` (D-12).
       - Else → append `Fill(..., fill_date=next_bar, fill_price=df.loc[next_bar, "adj_open"], status="filled")` (D-11).
   - Return `pd.DataFrame([asdict(f) for f in fills])`.

3. Update `strategies/entry/__init__.py` to re-export `EntryEngine, Fill`.

4. Remove `pytest.importorskip` from test_engine.py. The test_no_lookahead_state_shuffle test should construct an engine, call .run(), mutate the state series values AT bars strictly after each signal bar to random garbage, construct a second engine with the mutated state, call .run() again, and assert the two Fills DataFrames are equal. (Since compute_buy_windows is called once at __init__, this test verifies the engine doesn't peek at post-signal state mid-run.)

Style per CLAUDE.md: PascalCase class, snake_case methods, Google docstrings with Args/Returns. Keep `run()` under 80 lines; extract helpers if needed.
  </action>
  <verify>
    <automated>uv run pytest tests/entry/test_engine.py -x -q</automated>
  </verify>
  <done>test_engine.py all GREEN; EntryEngine and Fill importable from strategies.entry.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: A/B report script + test + live VN100 run (ENTRY-05)</name>
  <files>strategies/entry/ab_report.py, strategies/entry/__init__.py, tests/entry/test_ab_report.py, docs/audits/phase30-entry-ab.md, docs/audits/phase30/entry_ab_raw_vn100.csv, docs/audits/phase30/entry_ab_canslim.csv</files>
  <behavior>
    - test_ab_report_runs_twice_and_produces_required_columns GREEN: tiny 2-ticker synthetic run produces two metric rows (raw + canslim) each with all D-19 columns.
    - `python -m strategies.entry.ab_report` (or equivalent CLI entry) produces the two CSVs under docs/audits/phase30/ and the Markdown report at docs/audits/phase30-entry-ab.md.
  </behavior>
  <action>
Build the A/B helper per D-16..D-19, D-21.

1. `strategies/entry/ab_report.py`:
   - Function `summarize_fills(fills_df: pd.DataFrame, windows: list) -> dict` — computes the six D-19 metrics (total_fills, unique_tickers, windows_with_fills, mean_fills_per_window, days_since_buy_histogram, ab_overlap). ab_overlap computed per (window_id) — set of tickers appearing in both detector streams within the same window; report cardinality.
   - Function `build_ab_table(raw_fills, canslim_fills, windows) -> pd.DataFrame` — emits side-by-side table: rows = {total_fills, unique_tickers, ...}, columns = {raw_A, raw_C, canslim_A, canslim_C}.
   - Function `run_ab_report(mdm_state, universe, price_loader, canslim_scores, config, output_dir)`:
     - Instantiate `EntryEngine` twice — once with `canslim_scores=None`, once with the scorer frame.
     - Collect fills from both runs.
     - Call `summarize_fills` per (run × detector).
     - Write two CSVs under `output_dir/phase30/`: `entry_ab_raw_vn100.csv` (per-fill detail of raw run), `entry_ab_canslim.csv` (per-fill detail of canslim run).
     - Write `output_dir/phase30-entry-ab.md` with a header stating:
       * Phase 30 source commit
       * Actual first/last dates from MDM state (D-18)
       * Universe size (raw + canslim-qualified counts)
       * The side-by-side metrics table (markdown)
       * D-13 simplification note: "Phase 30 assumes every next-day open is tradable. T+2 / 7% ceiling-floor lock handling is deferred to Phase 31 per D-13."
       * The "days_since_buy" histogram per detector (small table 1..20)
   - `if __name__ == "__main__":` block that:
     - Loads raw VN100 prices 2014-2025 via `connectors/postgres.py` batch loader (path from 30-01-NOTES.md OQ #3 resolution). If 30-01-NOTES.md flagged that no batch loader exists, first add a thin helper `connectors/postgres.py::load_vn100_prices(start, end) -> dict[ticker -> DataFrame]`.
     - Loads HybridEngine VN30 state series (instantiate HybridEngine with v6 best config, call `.run(vn30_df)`, extract `['state']` column). **Do NOT re-run HybridEngine inside EntryEngine** (D-10) — the state series is computed once here and passed in.
     - Loads CanslimScorer output for same window.
     - Calls `run_ab_report(...)`.

2. `tests/entry/test_ab_report.py` — convert `importorskip` to real import. Test `test_ab_report_runs_twice_and_produces_required_columns`:
   - Build 2-ticker synthetic OHLCV via conftest helpers.
   - Build synthetic MDM state with one CASH→BUY transition.
   - Build synthetic canslim scores (pass for ticker1 on signal bar, fail for ticker2).
   - Provide an in-memory `price_loader = lambda t: prices[t]`.
   - Call `run_ab_report(..., output_dir=tmp_path)`.
   - Assert tmp_path/phase30-entry-ab.md exists, contains "Option A" and "Option C" headings, contains D-13 simplification note.
   - Assert the two CSVs exist.
   - Assert the summarize_fills output includes all six D-19 metric keys.

3. Update `strategies/entry/__init__.py` to re-export `run_ab_report, summarize_fills`.

4. **Execute the live run** against real VN100 2014-2025 data. Produce the actual `docs/audits/phase30-entry-ab.md` + CSVs. If the live run fails due to data coverage gaps, DO NOT fabricate — instead record the actual failure mode in the report and leave a clearly marked TODO for 30-02-followup. The synthetic unit test must still pass.

Per Code-Docs Sync Rule: `docs/rules_entry.md` created in Task 3 of THIS plan must land in the same commit as ab_report.py changes. The 30-03 executor should stage all three tasks together before committing.
  </action>
  <verify>
    <automated>uv run pytest tests/entry/test_ab_report.py -x -q &amp;&amp; test -f docs/audits/phase30-entry-ab.md &amp;&amp; test -f docs/audits/phase30/entry_ab_raw_vn100.csv</automated>
  </verify>
  <done>test_ab_report.py GREEN; real markdown + CSV artifacts exist with D-19 metrics side-by-side for raw and canslim runs on VN100 2014-2025.</done>
</task>

<task type="auto">
  <name>Task 3: docs/rules_entry.md (Code-Docs Sync)</name>
  <files>docs/rules_entry.md</files>
  <action>
Create `docs/rules_entry.md` documenting the Phase 30 locked rules. Per CLAUDE.md Code-Docs Sync Rule this MUST land in the same commit as the code (stage together with Task 1 + Task 2 outputs).

Sections:

1. **Purpose** — one paragraph: stock-level entry confirmation gated by MDM BUY window, filling next-day open. Cite ENTRY-01..ENTRY-05 and link to `.planning/phases/30-stock-level-entry-confirmation/30-CONTEXT.md`.

2. **Option A: 52-Week High Breakout (ENTRY-01)** — Formula as a bulleted list citing D-03:
   - `close[t] > max(close[t-252..t-1])` (strict; exclude today via shift(1))
   - `vol[t] >= 1.5 * mean(vol[t-50..t-1])`
   - `close[t] > open[t]`
   - `close[t] >= (high[t] + low[t]) / 2`
   - Note tunable parameters: `high_lookback=252`, `vol_lookback=50`, `vol_mult=1.5` (D-04).
   - Off-by-one pitfall note.

3. **Option C: Pocket Pivot (ENTRY-02)** — Formula per D-05:
   - `close[t] > open[t]`
   - `close[t] >= MA50[t]` (simple MA on adjusted close, D-06)
   - `vol[t] > max(down_day_vols[t-10..t-1])` where down day = `close < prior close`
   - **Fail-closed**: if fewer than 1 down day in last 10 sessions, detector does NOT fire (NaN propagation idiom).
   - `close[t] >= 0.85 * max(high[t-50..t-1])` (within 15% of 50d high)
   - Tunables: `ma_length=50`, `pocket_lookback=10`, `base_tolerance=0.15` (D-06).

4. **Entry Timing Window (ENTRY-03)** — Per D-07..D-10:
   - 20 trading days from most recent CASH/SELL→BUY transition of MDM state
   - Day 1 = transition bar itself; day 21+ rejected even if MDM still BUY
   - No extension on continuous BUY (clock keeps running from original transition)
   - Reset on each new CASH/SELL→BUY transition
   - Series-starts-in-BUY: NO initial window (locked by 30-01-NOTES.md)
   - MDM state source: HybridEngine v6 best (D-10) — Phase 30 consumes, does NOT re-run.

5. **Fill Model (ENTRY-04)** — Per D-11..D-13:
   - `fill_price = adj_open[t+1]` (ATO)
   - Last-bar signals: recorded as unfilled with reason "no next bar"
   - **Deferred to Phase 31**: T+2 settlement, 7% ceiling/floor lock handling

6. **Duplicate Handling** — Per D-14/D-15:
   - A and C are independent streams
   - Within-stream dedup key: `(ticker, window_id, detector_name)` — first fire only

7. **Output Location** — Per D-21: `docs/audits/phase30-entry-ab.md` + CSVs under `docs/audits/phase30/`.

8. **References** — Links to code files (`strategies/entry/option_a.py`, etc.) and tests (`tests/entry/`).

Keep doc under 200 lines. Match tone and structure of existing `docs/rules_mdm_hybrid.md`.
  </action>
  <verify>
    <automated>test -f docs/rules_entry.md &amp;&amp; grep -q "Option A" docs/rules_entry.md &amp;&amp; grep -q "Option C" docs/rules_entry.md &amp;&amp; grep -q "20 trading days" docs/rules_entry.md</automated>
  </verify>
  <done>docs/rules_entry.md exists with all eight sections; Code-Docs Sync Rule satisfied when committed alongside code changes from Tasks 1-2.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/entry/ -q` — entire phase test suite GREEN (config + detectors + window + engine + ab_report)
- `uv run pytest -q` — full project suite GREEN (no regressions in other phases)
- `test -f docs/rules_entry.md` — Code-Docs Sync artifact present
- `test -f docs/audits/phase30-entry-ab.md` — A/B report present with D-18 date range and D-13 simplification note
- `test -f docs/audits/phase30/entry_ab_raw_vn100.csv && test -f docs/audits/phase30/entry_ab_canslim.csv` — CSVs present
- `python -c "from strategies.entry import EntryEngine, EntryConfig, detect_option_a, detect_option_c, compute_buy_windows, find_window, Fill, run_ab_report"` — all exports importable
</verification>

<success_criteria>
1. **ENTRY-04**: EntryEngine produces next-day-open fills with last-bar unfilled edge handled; no look-ahead (state shuffle test green)
2. **ENTRY-05**: A/B report runs twice (raw VN100 + CANSLIM-qualified) on real 2014-2025 data, producing side-by-side fill counts with all D-19 metrics
3. **Code-Docs Sync**: `docs/rules_entry.md` committed in the same commit as `strategies/entry/engine.py` and `ab_report.py`
4. All phase 30 tests GREEN; no regressions in full project suite
5. A/B audit artifacts (.md + CSVs) exist under `docs/audits/phase30*`
</success_criteria>

<output>
After completion, create `.planning/phases/30-stock-level-entry-confirmation/30-03-SUMMARY.md` per template. Include:
- EntryEngine + Fill signatures and the exact main-loop pipeline
- Headline fill counts from the live A/B run (raw A, raw C, canslim A, canslim C) — actual numbers
- Days-since-buy distribution shape (one-line summary: "most fills in days 1-5" or similar)
- AB overlap cardinality (tickers filled by both A and C in same window)
- Any data-coverage caveats observed during the live VN100 run
- Integration note for Phase 31: Fills DataFrame schema is the entry stream into the portfolio engine
- Memory update reminder per the "best model docs rule": if this A/B run surfaces a clearly-superior variant, update `.planning` memory in the same commit
</output>