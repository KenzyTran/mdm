---
phase: 30-stock-level-entry-confirmation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - strategies/entry/__init__.py
  - strategies/entry/config.py
  - tests/entry/__init__.py
  - tests/entry/conftest.py
  - tests/entry/test_config.py
  - tests/entry/test_option_a.py
  - tests/entry/test_option_c.py
  - tests/entry/test_window.py
  - tests/entry/test_engine.py
  - tests/entry/test_ab_report.py
autonomous: true
requirements: [ENTRY-01, ENTRY-02, ENTRY-03, ENTRY-04, ENTRY-05]
must_haves:
  truths:
    - "Empty strategies/entry/ package exists with EntryConfig dataclass importable"
    - "tests/entry/ scaffold exists with stub tests that fail meaningfully (RED state) for ENTRY-01..ENTRY-05"
    - "Synthetic OHLCV fixture builder produces 260+ bar single-ticker frames with adj_open/high/low/close/totalvol columns"
    - "EntryConfig rejects out-of-range thresholds at construction (fail-loud)"
    - "Open questions from 30-RESEARCH.md (CanslimScorer cadence, VN100 batch loader, series-starts-in-BUY) resolved and documented"
  artifacts:
    - path: "strategies/entry/config.py"
      provides: "EntryConfig dataclass with D-03..D-06 thresholds + window_days, __post_init__ validation"
      contains: "class EntryConfig"
    - path: "strategies/entry/__init__.py"
      provides: "Package marker + EntryConfig re-export"
    - path: "tests/entry/conftest.py"
      provides: "Synthetic OHLCV + synthetic MDM state series fixtures"
    - path: "tests/entry/test_config.py"
      provides: "EntryConfig validation tests (green on day 1)"
  key_links:
    - from: "tests/entry/conftest.py"
      to: "adj_open/adj_high/adj_low/adj_close/totalvol schema"
      via: "fixture builder returns pd.DataFrame with these columns"
      pattern: "adj_close"
---

<objective>
Wave 0 scaffolding for Phase 30. Creates the empty `strategies/entry/` package with `EntryConfig` (the only non-stub artifact this plan produces), plus the full `tests/entry/` test scaffold with stub/RED tests keyed to ENTRY-01..ENTRY-05. Also resolves the three open questions from 30-RESEARCH.md via a short code audit so plans 30-02 and 30-03 can execute without interpretation.

Purpose: Per 30-VALIDATION.md this is the Wave 0 gap closure — every downstream task in 30-02/30-03 references a test file that MUST exist first (Nyquist compliance). Resolving the open questions here prevents rework.

Output:
- `strategies/entry/config.py` with validated `EntryConfig` dataclass
- `tests/entry/` test scaffold (all files, stubs fail for the right reason)
- Resolutions for the 3 research open questions embedded in the SUMMARY
</objective>

<execution_context>
@C:/Users/trant/projects/mdm/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/trant/projects/mdm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/30-stock-level-entry-confirmation/30-CONTEXT.md
@.planning/phases/30-stock-level-entry-confirmation/30-RESEARCH.md
@.planning/phases/30-stock-level-entry-confirmation/30-VALIDATION.md
@strategies/canslim/config.py
@strategies/canslim/scorer.py
@strategies/mdm_hybrid/mdm_hybrid_engine.py
@strategies/mdm_hybrid/position_manager.py
@connectors/adjust.py
@tests/canslim/conftest.py

<interfaces>
Per 30-RESEARCH.md §Standard Stack and §Architecture Patterns, the package must match these shapes:

```python
# strategies/entry/config.py — mirrors strategies/canslim/config.py style
from dataclasses import dataclass

@dataclass
class EntryConfig:
    # Option A (D-03/D-04)
    high_lookback: int = 252
    vol_lookback: int = 50
    vol_mult: float = 1.5
    # Option C (D-05/D-06)
    ma_length: int = 50
    pocket_lookback: int = 10
    base_tolerance: float = 0.15
    # Window (D-07..D-09, D-20)
    window_days: int = 20

    def __post_init__(self) -> None:
        # Fail-loud validation per 30-RESEARCH.md + Phase 29 D-08 pattern
        ...
```

MDM state series values (from `strategies/mdm_hybrid/position_manager.py::V2MarketState`):
`"BUY"`, `"CASH"`, `"SELL"` — string-valued enum. Window tracker will compare string literals.

CanslimScorer output columns (from `strategies/canslim/scorer.py`):
`date, ticker, c_pass, c_plus_pass, a_pass, a_plus_pass, n_pass, s_pass, l_pass, i_pass, liq_pass, rs_rating, score`

`connectors/adjust.py::adjust_ohlc(df)` — produces `adj_open/adj_high/adj_low/adj_close`; volume column unchanged (`totalvol`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve open questions via code audit</name>
  <files>.planning/phases/30-stock-level-entry-confirmation/30-01-NOTES.md</files>
  <action>
Resolve the three open questions from 30-RESEARCH.md §Open Questions. Spend max 15 minutes total. Create a short NOTES.md file in the phase directory capturing the answers — 30-02 and 30-03 executors will read this.

1. **CanslimScorer output cadence** (OQ #2, research pitfall #7): Open `strategies/canslim/scorer.py`. Determine whether scorer output is dense (per trading day, per ticker) or sparse (only rebalance dates / only signal-bar dates). Grep for the main public method (likely `score()` or similar) and inspect its iteration. Record: (a) cadence, (b) whether Phase 30 will need to forward-fill per-letter booleans before joining on price bars for D-16.

2. **VN100 batch price loader** (OQ #3): Inspect `connectors/postgres.py` for any existing `load_prices(tickers, start, end)` or equivalent batch loader. Also check `strategies/canslim/` for a price loader it uses (Phase 29 must have loaded prices somewhere). Record: (a) whether a batch loader exists and its import path, or (b) that Phase 30 plan 30-03 needs to add a thin wrapper under `connectors/`.

3. **Series-starts-in-BUY edge case** (OQ #1): Per researcher's recommendation, **lock the decision as "no initial window"** — if the VN30 MDM state series begins on a bar where state is already BUY with no observable prior CASH/SELL bar, that is NOT treated as a transition and does NOT open a window. First window only opens on an actually-observed CASH/SELL→BUY transition. Record this as a locked decision in NOTES.md so 30-02 window.py implementer has no ambiguity.

Format NOTES.md as a short Markdown file with three H2 sections, one per question, each with "Answer:" and (where relevant) "Implementation note for 30-02/30-03:".
  </action>
  <verify>
    <automated>test -f .planning/phases/30-stock-level-entry-confirmation/30-01-NOTES.md &amp;&amp; grep -q "Series-starts-in-BUY" .planning/phases/30-stock-level-entry-confirmation/30-01-NOTES.md</automated>
  </verify>
  <done>NOTES.md exists with resolved answers for all 3 open questions; series-starts-in-BUY locked to "no initial window".</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create strategies/entry package skeleton + EntryConfig</name>
  <files>strategies/entry/__init__.py, strategies/entry/config.py, tests/entry/__init__.py, tests/entry/test_config.py</files>
  <behavior>
    - EntryConfig() with defaults constructs successfully; all seven attributes readable with locked defaults (high_lookback=252, vol_lookback=50, vol_mult=1.5, ma_length=50, pocket_lookback=10, base_tolerance=0.15, window_days=20).
    - EntryConfig(high_lookback=1) raises ValueError ("high_lookback must be >= 2").
    - EntryConfig(vol_lookback=0) raises ValueError.
    - EntryConfig(vol_mult=0.9) raises ValueError ("vol_mult must be >= 1.0").
    - EntryConfig(ma_length=1) raises ValueError.
    - EntryConfig(pocket_lookback=0) raises ValueError.
    - EntryConfig(base_tolerance=0.0) raises ValueError; EntryConfig(base_tolerance=1.0) raises ValueError ("base_tolerance must be in (0, 1)").
    - EntryConfig(window_days=0) raises ValueError.
    - `from strategies.entry import EntryConfig` works.
  </behavior>
  <action>
Follow the skeleton from 30-RESEARCH.md §Code Examples (EntryConfig skeleton). Implements per D-03..D-06, D-20.

1. Create `strategies/entry/__init__.py` — minimal package marker; re-export `EntryConfig` from `.config`. Leave a `# EntryEngine will be re-exported by plan 30-03` comment. No other exports.
2. Create `strategies/entry/config.py` with `EntryConfig` dataclass per the interfaces block above. Implement `__post_init__` with all fail-loud validations from 30-RESEARCH.md. Google-style docstring on the class. Reference D-03..D-06, D-20 in the docstring.
3. Create `tests/entry/__init__.py` — empty package marker.
4. Create `tests/entry/test_config.py` with one test function per behavior bullet above. Use `pytest.raises(ValueError)` for the failure cases.

Per CLAUDE.md: snake_case functions, PascalCase class, 4-space indent, Google-style docstring.
  </action>
  <verify>
    <automated>uv run pytest tests/entry/test_config.py -x -q</automated>
  </verify>
  <done>test_config.py all green; `python -c "from strategies.entry import EntryConfig; EntryConfig()"` succeeds.</done>
</task>

<task type="auto">
  <name>Task 3: Test scaffold with RED stubs for ENTRY-01..ENTRY-05</name>
  <files>tests/entry/conftest.py, tests/entry/test_option_a.py, tests/entry/test_option_c.py, tests/entry/test_window.py, tests/entry/test_engine.py, tests/entry/test_ab_report.py</files>
  <action>
Create the rest of the Wave 0 test scaffold per 30-VALIDATION.md §Wave 0 Requirements and 30-RESEARCH.md §Phase Requirements → Test Map. These tests MUST exist now (RED state) so downstream tasks in 30-02/30-03 can reference them in their `<verify>` blocks.

**`tests/entry/conftest.py`** — Shared fixtures. Build synthetic OHLCV fixture helpers per 30-VALIDATION.md:
  - `make_ohlcv(n_bars=260, start_date="2020-01-01", seed=42)` — returns pd.DataFrame indexed by business-day DatetimeIndex with columns `adj_open, adj_high, adj_low, adj_close, totalvol`. Deterministic random walk close, OHLC derived sanely, volume baseline around 1_000_000 with noise. This is the raw-material fixture; individual tests will copy + mutate specific bars to force detector clauses on/off.
  - `make_buy_breakout_bar(df, idx)` — mutates bar at `idx` to satisfy all four Option A clauses: close above previous 252-max, volume ≥ 1.5× 50-day avg, close > open, close in upper half. Returns mutated df.
  - `make_mdm_state(dates, transitions)` — helper that accepts a DatetimeIndex and a list of `(date, new_state)` tuples and materializes a pd.Series[str] with CASH/SELL/BUY values forward-filled between transitions. Default start: "CASH".
  - Mark all fixtures with `@pytest.fixture`.

**`tests/entry/test_option_a.py`** — Stubs for ENTRY-01 (one test per clause + positive case). All tests start with:
```python
import pytest
pytest.importorskip("strategies.entry.option_a", reason="30-02 Task 1 not yet implemented")
from strategies.entry.option_a import detect_option_a
```
Tests (all xfail via `pytest.importorskip` until 30-02 lands):
- `test_option_a_fires_on_full_breakout` — positive case using `make_buy_breakout_bar`.
- `test_option_a_fails_not_new_high` — mutate close to equal 252-max (strict > required).
- `test_option_a_fails_volume_below_1_5x` — volume = 1.49× average.
- `test_option_a_fails_down_bar` — close < open.
- `test_option_a_fails_lower_half_close` — close just below (high+low)/2.
- `test_option_a_excludes_today_from_52wk_max` — off-by-one guard: ensure the detector uses `close.shift(1).rolling(252).max()` not `rolling(252).max()`. Construct a fixture where today's close IS the max but equals yesterday's prior-252 max; assert fires (without shift(1) it would not).

**`tests/entry/test_option_c.py`** — Stubs for ENTRY-02:
- `test_option_c_fires_pocket_pivot` — positive case.
- `test_option_c_fails_closed_zero_down_days` — explicit fixture where last 10 sessions are all up days; assert NO fire (fail-closed per D-05, pitfall #2).
- `test_option_c_fails_below_ma50`.
- `test_option_c_fails_below_base_tolerance` — close at 84% of 50d high (outside 15%).
- `test_option_c_fails_down_bar`.

**`tests/entry/test_window.py`** — Stubs for ENTRY-03:
- `test_window_day_1_inclusive` — signal on transition bar itself fires.
- `test_window_day_20_inclusive` — signal exactly 20 bars after transition still fires.
- `test_window_day_21_rejected` — signal 21 bars after transition rejected.
- `test_window_no_extension_on_continuous_buy` — state BUY for 50 bars with one transition at bar 0 → only bars 0..19 in window.
- `test_window_reset_on_cash_to_buy_transition` — two transitions, each opens fresh 20-day window.
- `test_window_sell_to_buy_is_transition` — SELL→BUY treated same as CASH→BUY per D-07.
- `test_window_series_starts_in_buy_no_initial_window` — per 30-01-NOTES.md locked decision: series that starts in BUY with no prior CASH/SELL does NOT open a window.

**`tests/entry/test_engine.py`** — Stubs for ENTRY-04:
- `test_fill_next_open` — signal at bar t → fill_price == df.loc[t+1, "adj_open"].
- `test_last_bar_unfilled` — signal on final bar → recorded as unfilled with reason "no next bar".
- `test_no_lookahead_state_shuffle` — mutate MDM state AFTER each signal bar to garbage; assert fills unchanged (state[i-1] discipline per CLAUDE.md memory).
- `test_ac_streams_independent` — same ticker, same bar fires both A and C → two fills recorded.
- `test_within_stream_dedup_first_fire_only` — same ticker fires A twice in one window → only first recorded.

**`tests/entry/test_ab_report.py`** — Stubs for ENTRY-05:
- `test_ab_report_runs_twice_and_produces_required_columns` — tiny 2-ticker synthetic run with synthetic MDM state; assert report object has both raw and canslim-qualified tables with all D-19 metrics as columns: `total_fills, unique_tickers, windows_with_fills, mean_fills_per_window, days_since_buy_histogram, ab_overlap_count`.

All test files that import from yet-unbuilt modules use `pytest.importorskip(...)` at module level so they **skip** rather than error during scaffold commit. The skips convert to real RED/GREEN in 30-02 and 30-03.
  </action>
  <verify>
    <automated>uv run pytest tests/entry/ -q</automated>
  </verify>
  <done>`pytest tests/entry/` runs without collection errors; test_config.py tests PASS; all other test files SKIP cleanly via importorskip (expected — modules not yet built). Zero hard failures.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/entry/ -q` — runs cleanly, no collection errors
- `uv run pytest tests/entry/test_config.py -x -q` — all pass
- `python -c "from strategies.entry import EntryConfig; c = EntryConfig(); print(c)"` — prints dataclass repr
- `test -f .planning/phases/30-stock-level-entry-confirmation/30-01-NOTES.md`
- `grep -q "importorskip" tests/entry/test_option_a.py` — confirms RED-stub pattern in place
</verification>

<success_criteria>
1. `strategies/entry/` package importable with `EntryConfig` dataclass (locked defaults from D-03..D-06, D-20)
2. `EntryConfig.__post_init__` fails-loud on 7 out-of-range scenarios (test_config.py green)
3. `tests/entry/` scaffold complete: conftest.py + 6 test files, all referenced by downstream plans' `<verify>` blocks
4. 30-01-NOTES.md resolves CanslimScorer cadence, VN100 batch loader status, and series-starts-in-BUY edge
5. `uv run pytest tests/entry/` exits 0 (config tests pass, others skip via importorskip)
</success_criteria>

<output>
After completion, create `.planning/phases/30-stock-level-entry-confirmation/30-01-SUMMARY.md` per template. Include:
- EntryConfig signature (field names + defaults + validation rules)
- Fixture helper signatures from conftest.py (so 30-02/30-03 know what's available)
- Resolved open questions (inline repeat of 30-01-NOTES.md content for convenience)
- Test files created and their RED-stub pattern (importorskip)
</output>