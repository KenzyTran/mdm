---
phase: 01-data-integrity
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - core/signal_loader.py
  - data/signals/tecl_signals.csv
  - data/signals/nasdaq_signals.csv
  - tests/test_signal_fixtures.py
autonomous: true
requirements:
  - DATA-04

must_haves:
  truths:
    - "load_signal_fixture('data/signals/tecl_signals.csv') returns a DataFrame with columns [date, signal, gain_loss_pct]"
    - "load_signal_fixture('data/signals/nasdaq_signals.csv') returns a DataFrame with the same schema"
    - "All signal values are exactly one of: Buy, Sell, Cash"
    - "Signal dates are valid datetime objects sorted ascending"
    - "TECL fixture contains signals spanning 2017-2026"
    - "NASDAQ fixture contains signals spanning 2017-2026"
    - "Invalid signal types in a CSV cause a ValueError"
  artifacts:
    - path: "core/signal_loader.py"
      provides: "Signal fixture loading and validation function"
      exports: ["load_signal_fixture"]
      min_lines: 20
    - path: "data/signals/tecl_signals.csv"
      provides: "TECL published signal history fixture"
      contains: "date,signal,gain_loss_pct"
    - path: "data/signals/nasdaq_signals.csv"
      provides: "NASDAQ published signal history fixture"
      contains: "date,signal,gain_loss_pct"
    - path: "tests/test_signal_fixtures.py"
      provides: "Tests for signal fixture loading and validation"
      min_lines: 30
  key_links:
    - from: "core/signal_loader.py"
      to: "data/signals/tecl_signals.csv"
      via: "pd.read_csv"
      pattern: "pd\\.read_csv"
    - from: "tests/test_signal_fixtures.py"
      to: "core/signal_loader.py"
      via: "import load_signal_fixture"
      pattern: "from core\\.signal_loader import load_signal_fixture"
---

<objective>
Create structured CSV test fixtures for Dr. K's published TECL and NASDAQ signal histories, plus a signal fixture loader with validation. These fixtures are the ground truth for Phase 3's signal comparison engine.

Purpose: The published signal history is what we validate the reverse-engineered MDM against. Without structured fixtures, Phase 3 cannot score model accuracy.
Output: `data/signals/tecl_signals.csv`, `data/signals/nasdaq_signals.csv`, `core/signal_loader.py`, `tests/test_signal_fixtures.py`.
</objective>

<execution_context>
@C:/Users/trant/projects/mdm/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/trant/projects/mdm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-integrity/01-CONTEXT.md
@.planning/phases/01-data-integrity/01-RESEARCH.md
@.planning/phases/01-data-integrity/01-01-SUMMARY.md

<interfaces>
<!-- Plan 01 creates core/ package. This plan adds core/signal_loader.py to it. -->

From core/__init__.py (created by Plan 01):
- Empty init, core package exists

Signal fixture CSV format (per D-08):
```csv
date,signal,gain_loss_pct
2020-04-02,Buy,
2020-06-24,Sell,15.3
2020-06-29,Buy,
2020-09-04,Sell,22.1
```

Columns:
- date: YYYY-MM-DD format, must be valid trading day
- signal: exactly one of Buy, Sell, Cash
- gain_loss_pct: numeric or empty (optional field, filled on Sell signals)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create signal fixture loader and tests</name>
  <files>core/signal_loader.py, tests/test_signal_fixtures.py</files>
  <read_first>
    - C:/Users/trant/projects/mdm/core/__init__.py
    - C:/Users/trant/projects/mdm/.planning/phases/01-data-integrity/01-RESEARCH.md
  </read_first>
  <behavior>
    - test_load_valid_fixture: load_signal_fixture(path) returns DataFrame with columns ['date', 'signal', 'gain_loss_pct'].
    - test_date_dtype: date column has dtype datetime64[ns].
    - test_date_sorted_ascending: dates are monotonically increasing.
    - test_valid_signal_types: all signal values are in {'Buy', 'Sell', 'Cash'}.
    - test_invalid_signal_raises: A fixture CSV containing signal type 'Hold' raises ValueError.
    - test_gain_loss_numeric: gain_loss_pct column has dtype float64 (NaN for missing values).
    - test_whitespace_stripped: signal values with leading/trailing whitespace are stripped (e.g., ' Buy ' becomes 'Buy').
  </behavior>
  <action>
    **Step 1: Write tests first (RED phase)**
    Create `tests/test_signal_fixtures.py`:
    - Import: `from core.signal_loader import load_signal_fixture`
    - Use `tmp_path` pytest fixture to create small test CSV files for validation tests.
    - For the invalid signal test, create a temp CSV with a 'Hold' signal type.
    - For the valid tests, create a temp CSV with 3-4 rows of valid Buy/Sell signals.

    Run: `uv run pytest tests/test_signal_fixtures.py -x` -- must FAIL (RED).

    **Step 2: Implement signal loader (GREEN phase)**
    Create `core/signal_loader.py` with function:

    ```python
    def load_signal_fixture(filepath: str) -> pd.DataFrame:
        """Load a published signal history CSV into a structured DataFrame.

        Args:
            filepath: Path to signal CSV with columns date, signal, gain_loss_pct

        Returns:
            DataFrame with parsed dates, validated signal types, numeric gain_loss_pct

        Raises:
            ValueError: If any signal type is not in {Buy, Sell, Cash}
        """
    ```

    Implementation:
    1. `pd.read_csv(filepath)`
    2. `pd.to_datetime(df['date'])` -- parse date column
    3. `df['signal'] = df['signal'].str.strip()` -- strip whitespace (per pitfall 5)
    4. Validate signal types: `valid_signals = {'Buy', 'Sell', 'Cash'}`. If any value not in set, raise `ValueError(f"Invalid signal types found: {invalid}")`
    5. `df['gain_loss_pct'] = pd.to_numeric(df['gain_loss_pct'], errors='coerce')` -- NaN for empty/missing
    6. `df.sort_values('date').reset_index(drop=True)` -- sort ascending
    7. Return df

    Run: `uv run pytest tests/test_signal_fixtures.py -x` -- must PASS (GREEN).
  </action>
  <verify>
    <automated>uv run pytest tests/test_signal_fixtures.py -v</automated>
  </verify>
  <acceptance_criteria>
    - core/signal_loader.py contains `def load_signal_fixture(`
    - core/signal_loader.py contains `{'Buy', 'Sell', 'Cash'}`
    - core/signal_loader.py contains `raise ValueError`
    - tests/test_signal_fixtures.py contains `from core.signal_loader import load_signal_fixture`
    - `uv run pytest tests/test_signal_fixtures.py -v` exits with code 0
  </acceptance_criteria>
  <done>
    Signal loader function loads CSV fixtures, validates signal types, parses dates, handles optional gain_loss_pct. All unit tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create TECL and NASDAQ signal fixture CSV files</name>
  <files>data/signals/tecl_signals.csv, data/signals/nasdaq_signals.csv</files>
  <read_first>
    - C:/Users/trant/projects/mdm/core/signal_loader.py
    - C:/Users/trant/projects/mdm/.planning/phases/01-data-integrity/01-CONTEXT.md
  </read_first>
  <action>
    **Per D-07:** Signal data must be manually entered from virtueofselfishinvesting.com. Per D-09: Create fixtures for both TECL and NASDAQ signal histories (2017-2026, 100+ signals each).

    **Step 1: Create data/signals/ directory**
    `mkdir -p data/signals`

    **Step 2: Create TECL signal fixture**
    Create `data/signals/tecl_signals.csv` with header: `date,signal,gain_loss_pct`

    Research Dr. K's published TECL signals from virtueofselfishinvesting.com. Enter all available Buy/Sell/Cash signals from 2017 to present. Each row:
    - date: YYYY-MM-DD of signal date
    - signal: Buy, Sell, or Cash
    - gain_loss_pct: percentage gain/loss at exit (empty for Buy entries, filled for Sell/Cash exits if available)

    If exact gain_loss_pct is not available for some signals, leave the field empty (the loader handles NaN).

    **Step 3: Create NASDAQ signal fixture**
    Create `data/signals/nasdaq_signals.csv` with same format.
    Research Dr. K's published NASDAQ/composite signals from the same source.

    **Step 4: Validate fixtures load correctly**
    Run the signal loader on both files to confirm they parse without error:
    ```python
    from core.signal_loader import load_signal_fixture
    tecl = load_signal_fixture('data/signals/tecl_signals.csv')
    nasdaq = load_signal_fixture('data/signals/nasdaq_signals.csv')
    print(f"TECL: {len(tecl)} signals, {tecl['date'].min()} to {tecl['date'].max()}")
    print(f"NASDAQ: {len(nasdaq)} signals, {nasdaq['date'].min()} to {nasdaq['date'].max()}")
    ```

    **Step 5: Add integration tests**
    Add to `tests/test_signal_fixtures.py`:
    - `test_tecl_fixture_loads`: load_signal_fixture('data/signals/tecl_signals.csv') returns DataFrame with >10 rows (at minimum, more expected per D-09).
    - `test_nasdaq_fixture_loads`: load_signal_fixture('data/signals/nasdaq_signals.csv') returns DataFrame with >10 rows.
    - `test_tecl_fixture_date_range`: first date >= 2017-01-01, last date <= 2026-12-31.
    - `test_nasdaq_fixture_date_range`: same range check.
    - `test_no_duplicate_dates_tecl`: no duplicate dates in TECL fixture.
    - `test_no_duplicate_dates_nasdaq`: no duplicate dates in NASDAQ fixture.

    NOTE: If the website is not accessible or signals cannot be found, create skeleton fixtures with at least 10-20 representative signals from known public MDM history (major Buy/Sell signals around COVID crash 2020, 2022 bear market, etc.) and document the gaps. The fixtures can be expanded later.
  </action>
  <verify>
    <automated>uv run pytest tests/test_signal_fixtures.py -v</automated>
  </verify>
  <acceptance_criteria>
    - data/signals/tecl_signals.csv exists and first line is `date,signal,gain_loss_pct`
    - data/signals/nasdaq_signals.csv exists and first line is `date,signal,gain_loss_pct`
    - data/signals/tecl_signals.csv has more than 10 data rows (excluding header)
    - data/signals/nasdaq_signals.csv has more than 10 data rows (excluding header)
    - Both files contain only Buy, Sell, or Cash in the signal column
    - `uv run pytest tests/test_signal_fixtures.py -v` exits with code 0
  </acceptance_criteria>
  <done>
    TECL and NASDAQ signal fixture CSVs exist with published signal data from 2017-2026. Both load successfully through the signal loader with no validation errors. Integration tests confirm fixtures parse correctly, have valid date ranges, no duplicate dates, and valid signal types.
  </done>
</task>

</tasks>

<verification>
Run full test suite: `uv run pytest tests/ -v`
All tests pass for both data loader and signal fixtures.
Signal fixtures load without errors and contain valid signal data.
</verification>

<success_criteria>
- load_signal_fixture works for both TECL and NASDAQ CSV files
- Both fixtures contain published signals spanning 2017-2026
- All signal types are valid (Buy/Sell/Cash)
- Dates are properly parsed and sorted ascending
- gain_loss_pct is numeric where present, NaN where missing
- All tests in tests/test_signal_fixtures.py pass
- Full test suite `uv run pytest tests/ -v` passes
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-integrity/01-02-SUMMARY.md`
</output>
