---
phase: 02-codebase-organization
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - scripts/run_backtest.py
  - scripts/optimize_mdm.py
  - scripts/check_date.py
  - analysis/analyze_drawdown.py
  - analysis/analyze_vsa_drawdown.py
  - analysis/diagnose_vn30.py
  - analysis/__init__.py
  - scripts/__init__.py
  - mdm_backtest.ipynb
  - sp500_backtest.ipynb
  - vn30_vsa_backtest.ipynb
autonomous: true
requirements: [ORG-01, ORG-04]

must_haves:
  truths:
    - "Scripts run from project root with updated imports pointing to strategies/"
    - "Analysis tools run from project root with updated imports pointing to strategies/"
    - "Notebooks have updated import cells pointing to strategies/"
    - "Old models/ and vn30_vsa/ directories are deleted (per D-03)"
    - "Regression tests pass confirming identical output (per D-10)"
  artifacts:
    - path: "scripts/run_backtest.py"
      provides: "MDM backtest runner"
      contains: "from strategies.mdm_classic"
    - path: "scripts/optimize_mdm.py"
      provides: "MDM parameter optimizer"
      contains: "from strategies.mdm_classic"
    - path: "analysis/analyze_drawdown.py"
      provides: "MDM drawdown analysis"
      contains: "from strategies.mdm_classic"
    - path: "analysis/analyze_vsa_drawdown.py"
      provides: "VSA drawdown analysis"
      contains: "from strategies.vsa"
  key_links:
    - from: "scripts/run_backtest.py"
      to: "strategies/mdm_classic/mdm_engine.py"
      via: "import"
      pattern: "from strategies.mdm_classic"
    - from: "analysis/analyze_vsa_drawdown.py"
      to: "strategies/vsa/vsa_engine.py"
      via: "import"
      pattern: "from strategies.vsa"
---

<objective>
Move entry-point scripts and analysis tools to their new directories, update all imports (including notebooks), delete old directories, and run regression tests to confirm zero behavioral change.

Purpose: Completes the three-layer architecture (ORG-01) and validates the entire migration (ORG-04) by running regression tests that compare post-migration output to golden baselines.
Output: Clean project structure with scripts/, analysis/, strategies/, core/. Old models/ and vn30_vsa/ removed. Regression tests green.
</objective>

<execution_context>
@C:/Users/trant/projects/mdm/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/trant/projects/mdm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-codebase-organization/02-CONTEXT.md
@.planning/phases/02-codebase-organization/02-RESEARCH.md

Depends on Plan 02 SUMMARY for confirmed import paths:
@.planning/phases/02-codebase-organization/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move scripts and analysis files, update all imports</name>
  <files>
    scripts/run_backtest.py,
    scripts/optimize_mdm.py,
    scripts/check_date.py,
    scripts/__init__.py,
    analysis/analyze_drawdown.py,
    analysis/analyze_vsa_drawdown.py,
    analysis/diagnose_vn30.py,
    analysis/__init__.py,
    mdm_backtest.ipynb,
    sp500_backtest.ipynb,
    vn30_vsa_backtest.ipynb
  </files>
  <read_first>
    run_backtest.py,
    optimize_mdm.py,
    check_date.py,
    analyze_drawdown.py,
    analyze_vsa_drawdown.py,
    analysis/diagnose_vn30.py,
    mdm_backtest.ipynb,
    sp500_backtest.ipynb,
    vn30_vsa_backtest.ipynb
  </read_first>
  <action>
    Per D-04, D-05, D-06, D-07:

    **Step 1: Create scripts/ directory**
    - Create `scripts/__init__.py` (empty, just package marker -- optional, but consistent)

    **Step 2: Move and update scripts (per D-04, D-06)**

    For each script, copy to scripts/ and update imports:

    `run_backtest.py` -> `scripts/run_backtest.py`:
    - Change `from models import MDMEngine, DataLoader` to `from strategies.mdm_classic import MDMEngine`
    - Change `from models.data_loader import DataLoader` to `from strategies.mdm_classic.data_loader import DataLoader`
    - Change any other `from models.` imports to `from strategies.mdm_classic.`
    - Add `import sys; from pathlib import Path; sys.path.insert(0, str(Path(__file__).resolve().parent.parent))` at the top if not already present, so it can be run as `python scripts/run_backtest.py` from project root

    `optimize_mdm.py` -> `scripts/optimize_mdm.py`:
    - Change `from models.config import MDMConfig` to `from strategies.mdm_classic.config import MDMConfig`
    - Change `from models.mdm_engine import MDMEngine` to `from strategies.mdm_classic.mdm_engine import MDMEngine`
    - Change all other `from models` imports to `from strategies.mdm_classic`
    - Add sys.path fix as above

    `check_date.py` -> `scripts/check_date.py`:
    - Change `from models` imports to `from strategies.mdm_classic`
    - Add sys.path fix as above

    **Step 3: Update analysis files (per D-05)**

    `analysis/` directory already exists with `diagnose_vn30.py`. Create `analysis/__init__.py` (empty).

    `analyze_drawdown.py` -> `analysis/analyze_drawdown.py`:
    - Change `from models` imports to `from strategies.mdm_classic`
    - Add sys.path fix
    - Remove any existing sys.path hacks that reference `models`

    `analyze_vsa_drawdown.py` -> `analysis/analyze_vsa_drawdown.py`:
    - Change `from vn30_vsa` imports to `from strategies.vsa`
    - Add sys.path fix

    `analysis/diagnose_vn30.py` (already in analysis/):
    - Change `from models.data_loader import DataLoader` to `from strategies.mdm_classic.data_loader import DataLoader`
    - Remove any `sys.path.append` hacks that reference parent directories for models access
    - Add clean sys.path fix pointing to project root

    **Step 4: Update notebook imports (per D-05)**

    Notebooks stay at project root. For each .ipynb file, load as JSON, find code cells with import statements, and update:

    `mdm_backtest.ipynb`:
    - `from models import (...)` -> `from strategies.mdm_classic import (...)`
    - `from models.` -> `from strategies.mdm_classic.`

    `sp500_backtest.ipynb`:
    - `from models.mdm_engine import MDMEngine` -> `from strategies.mdm_classic.mdm_engine import MDMEngine`
    - All other `from models` -> `from strategies.mdm_classic`

    `vn30_vsa_backtest.ipynb`:
    - `from vn30_vsa.vsa_engine import VSAEngine` -> `from strategies.vsa.vsa_engine import VSAEngine`
    - `from vn30_vsa.` -> `from strategies.vsa.`
    - All other `from vn30_vsa` -> `from strategies.vsa`

    For notebook editing: Read each .ipynb with the Read tool, parse the JSON structure, update the source lines in code cells that contain import statements, then write back with the Write tool. Be careful to preserve JSON structure.

    **Step 5: Delete old root-level scripts**

    After copying to new locations, delete:
    - run_backtest.py (now at scripts/run_backtest.py)
    - optimize_mdm.py (now at scripts/optimize_mdm.py)
    - check_date.py (now at scripts/check_date.py)
    - analyze_drawdown.py (now at analysis/analyze_drawdown.py)
    - analyze_vsa_drawdown.py (now at analysis/analyze_vsa_drawdown.py)
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -c "import sys; sys.path.insert(0, '.'); exec(open('scripts/run_backtest.py').read().split('if __name__')[0]); print('Script imports OK')" 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - scripts/run_backtest.py exists and contains `from strategies.mdm_classic`
    - scripts/optimize_mdm.py exists and contains `from strategies.mdm_classic`
    - scripts/check_date.py exists and contains `from strategies.mdm_classic`
    - analysis/analyze_drawdown.py exists and contains `from strategies.mdm_classic`
    - analysis/analyze_vsa_drawdown.py exists and contains `from strategies.vsa`
    - analysis/diagnose_vn30.py contains `from strategies.mdm_classic` (not `from models`)
    - Root-level run_backtest.py, optimize_mdm.py, check_date.py, analyze_drawdown.py, analyze_vsa_drawdown.py are deleted
    - mdm_backtest.ipynb contains `strategies.mdm_classic` in at least one cell
    - vn30_vsa_backtest.ipynb contains `strategies.vsa` in at least one cell
  </acceptance_criteria>
  <done>All scripts, analysis tools, and notebooks updated with new import paths; old root scripts removed</done>
</task>

<task type="auto">
  <name>Task 2: Delete old directories and run regression tests</name>
  <files>
    models/ (DELETE),
    vn30_vsa/ (DELETE)
  </files>
  <read_first>
    tests/test_mdm_regression.py,
    tests/test_vsa_regression.py,
    tests/fixtures/mdm_signals_baseline.csv
  </read_first>
  <action>
    Per D-03, D-07:

    **Step 1: Delete old directories**

    Delete the entire `models/` directory (including __pycache__). Git history preserves the originals per D-03.
    Delete the entire `vn30_vsa/` directory (including __pycache__). Git history preserves the originals per D-03.

    Use `rm -rf models/ vn30_vsa/` or equivalent.

    **Step 2: Verify clean import state**

    Run: `.venv/Scripts/python.exe -c "from strategies.mdm_classic import MDMEngine; from strategies.vsa import VSAEngine; from core.data_loader import DataLoader; print('All imports OK')"`

    This must succeed, confirming the three-layer architecture works without old directories.

    Also verify old imports FAIL (per D-07 clean break):
    `.venv/Scripts/python.exe -c "from models import MDMEngine" 2>&1` should produce ImportError.

    **Step 3: Run regression tests**

    Run: `.venv/Scripts/python.exe -m pytest tests/test_mdm_regression.py -x -v`

    This should pass, confirming MDM output is identical to golden baseline (per D-10).

    Run: `.venv/Scripts/python.exe -m pytest tests/test_vsa_regression.py -x -v`

    This should pass (or skip if VSA data file unavailable).

    **Step 4: Run full test suite**

    Run: `.venv/Scripts/python.exe -m pytest tests/ -v`

    All tests should pass: Phase 1 tests (test_data_loader.py, test_signal_fixtures.py) and Phase 2 regression tests.

    If any test fails, diagnose and fix the issue. Common causes:
    - Import path not updated somewhere
    - File path reference in a script still points to old location
    - Data file not found (check relative paths work from project root)
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/ -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - models/ directory does not exist
    - vn30_vsa/ directory does not exist
    - `python -c "from models import MDMEngine"` fails with ImportError
    - `python -c "from strategies.mdm_classic import MDMEngine"` succeeds
    - `python -c "from strategies.vsa import VSAEngine"` succeeds
    - `.venv/Scripts/python.exe -m pytest tests/test_mdm_regression.py -v` passes (all tests green)
    - `.venv/Scripts/python.exe -m pytest tests/ -v` passes (full suite green)
  </acceptance_criteria>
  <done>Old directories deleted; regression tests confirm identical output; three-layer architecture complete (per D-03, D-10, ORG-04)</done>
</task>

</tasks>

<verification>
- Project structure matches target: core/, strategies/mdm_classic/, strategies/vsa/, analysis/, scripts/
- No models/ or vn30_vsa/ directories exist
- `from strategies.mdm_classic import MDMEngine` works
- `from strategies.vsa import VSAEngine` works
- `from core.data_loader import DataLoader` works
- `.venv/Scripts/python.exe -m pytest tests/ -v` -- ALL tests pass
- Root directory is clean: no run_backtest.py, optimize_mdm.py, check_date.py, analyze_drawdown.py, analyze_vsa_drawdown.py
- Notebooks at root have updated import cells
</verification>

<success_criteria>
Three-layer architecture fully operational. All imports updated. Old directories removed. Regression tests confirm zero behavioral change. Full test suite green.
</success_criteria>

<output>
After completion, create `.planning/phases/02-codebase-organization/02-03-SUMMARY.md`
</output>
