---
phase: 29-vn100-universe-canslim-scorer
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - strategies/canslim/__init__.py
  - strategies/canslim/config.py
  - strategies/canslim/universe.py
  - strategies/canslim/sectors.py
  - strategies/canslim/scorer.py
  - strategies/canslim/baseline.py
  - strategies/canslim/rules/__init__.py
  - strategies/canslim/rules/fundamental.py
  - strategies/canslim/rules/technical.py
  - strategies/canslim/rules/rs.py
  - strategies/canslim/rules/flow.py
  - strategies/canslim/rules/liquidity.py
  - tests/canslim/__init__.py
  - tests/canslim/conftest.py
  - tests/canslim/test_config.py
  - tests/canslim/test_universe.py
  - tests/canslim/test_sectors.py
  - tests/canslim/test_rules_fundamental.py
  - tests/canslim/test_rules_technical.py
  - tests/canslim/test_rules_rs.py
  - tests/canslim/test_rules_flow.py
  - tests/canslim/test_rules_liquidity.py
  - tests/canslim/test_scorer.py
  - scripts/introspect_canslim_schema.py
  - docs/rules_canslim.md
  - docs/audits/phase29/.gitkeep
autonomous: true
requirements: [UNIV-01, UNIV-02, UNIV-03, CANS-01, CANS-02, CANS-03, CANS-04, CANS-05, CANS-06, CANS-07, CANS-08, CANS-09, CANS-10, CANS-11, CANS-12]
must_haves:
  truths:
    - "strategies/canslim/ package is importable with all declared submodules"
    - "tests/canslim/ suite runs (all tests collected, even if skipped) via uv run pytest tests/canslim -q"
    - "scripts/introspect_canslim_schema.py runs against live Postgres+MySQL and writes a schema-lock JSON file"
    - "Locked column names for is_quarter_nonbank, is_quarter_bank, stock_list.sector are written to a committed artifact"
  artifacts:
    - path: "strategies/canslim/__init__.py"
      provides: "Package root exporting CanslimConfig and CanslimScorer names"
    - path: "scripts/introspect_canslim_schema.py"
      provides: "Live-DB column introspection CLI"
    - path: ".planning/phases/29-vn100-universe-canslim-scorer/schema_lock.json"
      provides: "Locked column names for downstream plans"
    - path: "docs/rules_canslim.md"
      provides: "Stub for Code-Docs Sync (filled in by later plans)"
  key_links:
    - from: "later plans (02-09)"
      to: "schema_lock.json"
      via: "direct read of locked column names"
      pattern: "schema_lock.json"
---

<objective>
Wave 0 scaffold: create the complete strategies/canslim/ package skeleton, pytest collection layout under tests/canslim/, a live-DB introspection script that locks column names BEFORE any rule implementation, and the docs/rules_canslim.md stub plus docs/audits/phase29/ dir. No business logic yet — stubs raise NotImplementedError. This unblocks all downstream plans (02-09) to run in parallel where possible.

Purpose: Phase 29 research explicitly requires schema introspection to run FIRST because exact column names for is_quarter_nonbank, is_quarter_bank, and stock_list.sector are NOT verified. Implementing rules against guessed column names is forbidden.
Output: Importable package, collectible test suite, schema_lock.json artifact, docs stubs.
</objective>

<execution_context>
@C:/Users/trant/projects/mdm/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/trant/projects/mdm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/29-vn100-universe-canslim-scorer/29-CONTEXT.md
@.planning/phases/29-vn100-universe-canslim-scorer/29-RESEARCH.md
@.planning/phases/29-vn100-universe-canslim-scorer/29-VALIDATION.md
@connectors/postgres.py
@connectors/mysql.py
@connectors/eps.py
@connectors/adjust.py
@vn30_vsa/vsa_engine.py
@vn30_vsa/config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create strategies/canslim package + test skeleton</name>
  <read_first>
    - vn30_vsa/__init__.py (package layout reference)
    - vn30_vsa/config.py (dataclass style reference)
    - .planning/phases/29-vn100-universe-canslim-scorer/29-VALIDATION.md (Wave 0 Requirements checklist)
    - connectors/postgres.py, connectors/mysql.py (for import patterns)
  </read_first>
  <files>
    strategies/canslim/__init__.py,
    strategies/canslim/config.py,
    strategies/canslim/universe.py,
    strategies/canslim/sectors.py,
    strategies/canslim/scorer.py,
    strategies/canslim/baseline.py,
    strategies/canslim/rules/__init__.py,
    strategies/canslim/rules/fundamental.py,
    strategies/canslim/rules/technical.py,
    strategies/canslim/rules/rs.py,
    strategies/canslim/rules/flow.py,
    strategies/canslim/rules/liquidity.py,
    tests/canslim/__init__.py,
    tests/canslim/conftest.py,
    tests/canslim/test_config.py,
    tests/canslim/test_universe.py,
    tests/canslim/test_sectors.py,
    tests/canslim/test_rules_fundamental.py,
    tests/canslim/test_rules_technical.py,
    tests/canslim/test_rules_rs.py,
    tests/canslim/test_rules_flow.py,
    tests/canslim/test_rules_liquidity.py,
    tests/canslim/test_scorer.py
  </files>
  <action>
    Create the strategies/canslim/ package mirroring vn30_vsa/ layout.

    strategies/canslim/__init__.py — module docstring + placeholder re-exports:
      from strategies.canslim.config import CanslimConfig  # noqa: F401
      from strategies.canslim.scorer import CanslimScorer  # noqa: F401
      __all__ = ["CanslimConfig", "CanslimScorer"]

    Each of config.py, universe.py, sectors.py, scorer.py, baseline.py, rules/fundamental.py, rules/technical.py, rules/rs.py, rules/flow.py, rules/liquidity.py gets:
      - Module docstring stating owning requirements (e.g. "C/C+/A/A+ rules — CANS-01..CANS-04, D-07, D-11")
      - A top-level stub class or function with signature matching the decisions in 29-CONTEXT.md (D-09 output shape, D-12 defaults) that raises NotImplementedError("Filled in by plan 29-NN")
      - strategies/canslim/config.py MUST define `@dataclass class CanslimConfig:` with fields declared but body `raise NotImplementedError` is not possible on a dataclass — instead declare all fields as per D-12 defaults (c_threshold: float = 0.20, a_threshold: float = 0.15, n_within_high: float = 0.15, s_vol_mult: float = 1.5, l_rs_threshold: float = 80.0, liquidity_min_turnover_vnd: float = 5_000_000_000, i_lookback_days: int = 20) and leave validation for plan 02.
      - strategies/canslim/scorer.py: `class CanslimScorer:` with `__init__(self, config: "CanslimConfig", pg_engine=None, mysql_engine=None)` storing attrs, and `score(self, as_of_date) -> "pd.DataFrame": raise NotImplementedError`.

    tests/canslim/conftest.py: shared fixtures returning fake OHLCV DataFrame (30 rows, 3 tickers), fake EPS DataFrame (with publish_date column), fake stock_list (10 tickers with sector), and a `canslim_config` fixture returning CanslimConfig().

    Each tests/canslim/test_*.py file: one import-smoke test + one `@pytest.mark.skip(reason="implemented in plan 29-NN")` placeholder per owning requirement, referencing the req IDs explicitly in test names (e.g. def test_cans01_c_rule_quarterly_eps_yoy(): pass).

    Must follow snake_case, 4-space indent, Google-style docstrings per CLAUDE.md conventions.
  </action>
  <verify>
    <automated>uv run python -c "import strategies.canslim; from strategies.canslim import CanslimConfig, CanslimScorer; print(CanslimConfig())" &amp;&amp; uv run pytest tests/canslim --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run python -c "import strategies.canslim"` exits 0
    - `uv run pytest tests/canslim --collect-only -q` lists at least 13 collected test items across the 10 test files
    - `grep -r "NotImplementedError" strategies/canslim/` returns at least 8 matches
    - `CanslimConfig().c_threshold == 0.20 and CanslimConfig().l_rs_threshold == 80.0` evaluates True
  </acceptance_criteria>
  <done>Package imports cleanly; pytest collects tests/canslim; every module has a docstring naming its owning requirement IDs.</done>
</task>

<task type="auto">
  <name>Task 2: Live-DB schema introspection script + schema_lock.json</name>
  <read_first>
    - connectors/postgres.py (get_engine, query helpers)
    - connectors/mysql.py (get_engine, query helpers)
    - .planning/phases/29-vn100-universe-canslim-scorer/29-RESEARCH.md (sections on is_quarter_nonbank, is_quarter_bank, stock_list.sector)
    - docs/audits/phase28-data-audit.md (Phase 28 sector-table split notes, if present)
  </read_first>
  <files>
    scripts/introspect_canslim_schema.py,
    .planning/phases/29-vn100-universe-canslim-scorer/schema_lock.json
  </files>
  <action>
    Create `scripts/introspect_canslim_schema.py` — a standalone CLI (argparse: `--output PATH`, default `.planning/phases/29-vn100-universe-canslim-scorer/schema_lock.json`).

    Steps:
      1. `from connectors.postgres import get_engine as pg_engine`
      2. `from connectors.mysql import get_engine as my_engine`
      3. For Postgres: query `information_schema.columns` for tables `stock_list`, `stock_eod`, `stock_foreign_eod`, `index_eod`, `stock_rs`. Collect column names + data types.
      4. For MySQL: query `information_schema.columns WHERE table_schema = DATABASE()` for tables `is_quarter_nonbank`, `is_quarter_bank`, `is_quarter_insurance`, `is_quarter_stock`, `ratios_stock`, `rank_top_stocks`.
      5. Additionally probe for sector-ish columns on `stock_list`: run `SELECT column_name FROM information_schema.columns WHERE table_name='stock_list' AND (column_name ILIKE '%sector%' OR column_name ILIKE '%nganh%' OR column_name ILIKE '%industry%' OR column_name ILIKE '%nhom%')`. Record all hits.
      6. For `is_quarter_nonbank` / `is_quarter_bank`: probe specifically for EPS-bearing columns (`column_name LIKE '%eps%' OR column_name LIKE '%ppop%' OR column_name LIKE '%loi_nhuan%'`) and publish/date columns (`column_name LIKE '%publish%' OR column_name LIKE '%period%' OR column_name LIKE '%date%' OR column_name LIKE '%ngay%'`).
      7. Emit JSON structured as:
         {
           "generated_at": "<ISO ts>",
           "postgres": {"<table>": [{"name": "...", "type": "..."}]},
           "mysql": {"<table>": [...]},
           "locked": {
             "stock_list_sector_column": "<best guess>",
             "is_quarter_nonbank_eps_column": "<best guess>",
             "is_quarter_bank_ppop_column": "<best guess>",
             "publish_date_column": "<best guess>"
           },
           "notes": ["Any ambiguities that humans must resolve"]
         }
      8. Print a human-readable summary to stdout and write JSON to the output path.
      9. If any "locked" field cannot be resolved unambiguously, print "AMBIGUOUS: <field>" lines and exit with code 2 (do NOT fail the Wave 0 task — downstream plans will read `notes` and hard-code the decision).

    Run the script immediately and commit the produced `schema_lock.json` alongside the script so plans 03-09 can read locked column names directly.
  </action>
  <verify>
    <automated>uv run python scripts/introspect_canslim_schema.py --output .planning/phases/29-vn100-universe-canslim-scorer/schema_lock.json &amp;&amp; uv run python -c "import json; d=json.load(open('.planning/phases/29-vn100-universe-canslim-scorer/schema_lock.json')); assert 'locked' in d and 'postgres' in d and 'mysql' in d; print(list(d['locked'].keys()))"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/introspect_canslim_schema.py` exists and runs (exit 0 or 2)
    - `.planning/phases/29-vn100-universe-canslim-scorer/schema_lock.json` exists and has keys `postgres`, `mysql`, `locked`, `notes`
    - `locked.stock_list_sector_column` is a non-empty string OR listed under `notes` as ambiguous
    - `locked.is_quarter_nonbank_eps_column` and `locked.is_quarter_bank_ppop_column` are set OR flagged in `notes`
    - Script uses `connectors.postgres.get_engine` and `connectors.mysql.get_engine` (grep: `from connectors.postgres import` AND `from connectors.mysql import`)
  </acceptance_criteria>
  <done>Schema lock artifact exists on disk; downstream plans have concrete column names to code against.</done>
</task>

<task type="auto">
  <name>Task 3: docs/rules_canslim.md stub + audit dir</name>
  <read_first>
    - docs/rules_mdm_v2.md (style reference)
    - docs/rules_vsa.md (style reference)
    - .planning/phases/29-vn100-universe-canslim-scorer/29-CONTEXT.md (D-09..D-14 for what the doc must eventually contain)
  </read_first>
  <files>
    docs/rules_canslim.md,
    docs/audits/phase29/.gitkeep
  </files>
  <action>
    Create `docs/rules_canslim.md` following the heading structure of docs/rules_mdm_v2.md:

      # CANSLIM Rules (Phase 29)

      > Status: STUB — filled in by plans 29-02..29-09.
      > Code-Docs Sync Rule: any edit to strategies/canslim/ MUST update this file in the same commit (CLAUDE.md).

      ## 1. Universe
      _TBD — plan 29-03_

      ## 2. Sector Routing
      _TBD — plan 29-04_

      ## 3. CanslimConfig
      _TBD — plan 29-02_

      ## 4. Fundamental Rules (C, C+, A, A+)
      _TBD — plan 29-05_

      ## 5. Technical Rule (N) + RS (L)
      _TBD — plan 29-06_

      ## 6. Flow (I) + Liquidity (Liq)
      _TBD — plan 29-07_

      ## 7. Composite Score
      _TBD — plan 29-08_

      ## 8. Validation vs rank_top_stocks.diem_canslim
      _TBD — plan 29-09_

    Also create `docs/audits/phase29/.gitkeep` (empty file) so the directory is committed.
  </action>
  <verify>
    <automated>test -f docs/rules_canslim.md &amp;&amp; test -f docs/audits/phase29/.gitkeep &amp;&amp; grep -q "Code-Docs Sync Rule" docs/rules_canslim.md &amp;&amp; grep -c "^## " docs/rules_canslim.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/rules_canslim.md` exists with 8 `## ` headings
    - File contains the exact string "Code-Docs Sync Rule"
    - `docs/audits/phase29/.gitkeep` exists
  </acceptance_criteria>
  <done>Docs stub committed; audit dir exists for plan 29-09's validation report.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/canslim --collect-only -q` collects tests from all 10 test files
- `uv run python -c "import strategies.canslim"` succeeds
- `schema_lock.json` exists and contains locked column names (or `notes` entries for ambiguous ones)
- `docs/rules_canslim.md` exists as stub with Code-Docs Sync rule reference
</verification>

<success_criteria>
All Wave 0 checklist items from 29-VALIDATION.md are on disk and version-controlled. Plans 02-09 can begin.
</success_criteria>

<output>
After completion, create `.planning/phases/29-vn100-universe-canslim-scorer/29-01-SUMMARY.md`.
</output>
