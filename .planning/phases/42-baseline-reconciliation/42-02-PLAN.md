---
phase: 42-baseline-reconciliation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/audits/v10_baseline_drift.md
autonomous: true
requirements:
  - BASE-01
must_haves:
  truths:
    - "The exact v6.0 ship commit hash that produced output/v6_combined_validation.txt (truth-of-record) is identified and pinned in the audit doc"
    - "The HEAD commit hash (the 'bad' bisect endpoint) is also pinned"
    - "The count of commits between the v6.0 ship commit and HEAD that touch strategies/mdm_hybrid/ or core/ (the bisect surface area) is recorded"
    - "The audit doc has the Phase 28-29 single-file narrative structure so Wave 2 can append the bisect log table"
  artifacts:
    - path: "docs/audits/v10_baseline_drift.md"
      provides: "Seeded audit doc with v6.0 ship commit + HEAD commit + bisect endpoints + scaffolded sections"
      contains: "## Bisect Endpoints"
  key_links:
    - from: "docs/audits/v10_baseline_drift.md"
      to: "output/v6_combined_validation.txt"
      via: "cited as v6.0 truth-of-record (CAGR 11.5%, MaxDD -28.2%, 124 SELL)"
      pattern: "v6_combined_validation"
    - from: "docs/audits/v10_baseline_drift.md"
      to: "output/v9_ab_comparison.txt"
      via: "cited as measured-today drift (CAGR 10.70%, MaxDD -28.63%, 105 SELL)"
      pattern: "v9_ab_comparison"
---

<objective>
Identify the v6.0 ship commit hash (the "good" bisect anchor) and the HEAD commit hash (the "bad" anchor), then scaffold `docs/audits/v10_baseline_drift.md` with: executive summary, bisect endpoints section, reference-number tables (v6.0 truth-of-record + measured-today drift), placeholder for the bisect log (populated by 42-03), placeholder for root-cause narrative (populated by 42-03), placeholder for reconciliation outcome (populated by 42-04/05), and placeholder for "Reconciled Baseline" section (populated by 42-05).

Purpose: Anchor the forensic audit document before bisect runs, so Wave 2-4 agents append into a known structure rather than invent one. Per D-06, audit doc is single-file (not folder), matching Phase 28-29 narrative-heavy audit precedent.

Output: `docs/audits/v10_baseline_drift.md` with seeded structure and pinned commit hashes.
</objective>

<execution_context>
@C:/Users/trant/projects/mdm/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/trant/projects/mdm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-baseline-reconciliation/42-CONTEXT.md
@.planning/MILESTONES.md
@output/v6_combined_validation.txt
@output/v9_ab_comparison.txt
@docs/audits/phase28-data-audit.md
@docs/audits/phase29-canslim-validation.md

<interfaces>
<!-- Expected commit anchors (verified during planning; executor must re-confirm) -->

v6.0 ship commit (per .planning/MILESTONES.md v6.0 entry + `git log --grep="v6.0"`):
  candidate: 37cfdc2  "chore: complete v6.0 milestone — MDM Fail-Safe & Signal Refinement"
  rationale: This is the commit immediately after v6.0 work merged; preceding commits are individual Phase 23-27 feats. Verify by checking `git show 37cfdc2 --stat` lists MDMV2Config / HybridEngine / position_manager changes consistent with v6.0 scope.

HEAD commit:
  resolve via: `git rev-parse HEAD`
  at time of planning: 11aedc258c39e7b603a27413f88d7b671e7c8be0

Bisect surface commits (between 37cfdc2..HEAD, touching engine-relevant paths):
  resolve via: `git log --oneline 37cfdc2..HEAD -- strategies/mdm_hybrid/ core/indicators.py core/data_loader.py`
  at time of planning: 8 commits (will be higher in bisect itself — bisect checks ~log2(N_total)×runs)

Reference tuples to quote literally in the audit doc:
  v6.0 truth-of-record (from output/v6_combined_validation.txt line 13-16, Fail-Safe column):
    Total Return: +238.8%
    CAGR:         11.5%
    Max Drawdown: -28.2%
    Transitions:  334
    BUY time:     30.4%
    SELL count (per ROADMAP.md line 810 + project memory): 124
    MA50-breakdown share (per ROADMAP.md line 810): 84%

  Measured-today drift (from output/v9_ab_comparison.txt lines 21-22, baseline row):
    Total Return:  +213.4%
    CAGR:          10.70%
    Max Drawdown:  -28.63%
    Sharpe_rf3:    0.461
    Transitions:   296
    BUY%:          29.8%
    CASH%:         38.8%
    SELL%:         31.4%
    SELL count:    105
    MA50-breakdown share: 100.0%  (Note: v9 report shows 100%; v6 ref 84% — this delta itself is part of the drift to explain)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify v6.0 ship commit hash + HEAD + bisect surface, seed docs/audits/v10_baseline_drift.md</name>
  <files>docs/audits/v10_baseline_drift.md</files>
  <read_first>
    - .planning/MILESTONES.md (v6.0 entry, shipped 2026-04-02; v9.0 entry for context)
    - output/v6_combined_validation.txt (lines 1-60 — truth-of-record tuple)
    - output/v9_ab_comparison.txt (lines 1-110 — measured drift tuple)
    - docs/audits/phase28-data-audit.md (format precedent: Executive Summary → sections → companion CSVs)
    - docs/audits/phase29-canslim-validation.md (format precedent: narrative tone, section headers)
    - .planning/phases/42-baseline-reconciliation/42-CONTEXT.md (D-02, D-05, D-06, D-08, D-16 binding)
  </read_first>
  <action>
    Run these three git commands (capture output) to resolve commit anchors:

    1. `git log --oneline --grep="v6.0" -i | head -30` — confirm the v6.0 ship commit. Expected to find `37cfdc2 chore: complete v6.0 milestone — MDM Fail-Safe & Signal Refinement`. If a different commit appears clearly as the ship commit (e.g., a later merge/tag), use that; record the rationale in the doc. Use the full 40-char hash from `git rev-parse 37cfdc2`.
    2. `git rev-parse HEAD` — the "bad" anchor.
    3. `git log --oneline <SHIP_COMMIT>..HEAD -- strategies/mdm_hybrid/ core/indicators.py core/data_loader.py | wc -l` — count of engine-touching commits in the bisect surface.

    Then CREATE `docs/audits/v10_baseline_drift.md` with the following structure (use literal Markdown, exact headers so downstream greps work):

    ```markdown
    # v10.0 Baseline Drift Audit — Phase 42

    **Status:** In Progress (plan 42-02 seed; bisect pending in 42-03)
    **Opened:** <today's date>
    **Closes:** BASE-01 (forensic audit), BASE-02 (fix-forward), BASE-03 (determinism regression test)

    ## Executive Summary

    Between v6.0 ship (CAGR 11.5%, MaxDD -28.2%, 124 SELL per `output/v6_combined_validation.txt`) and HEAD
    (CAGR 10.70%, MaxDD -28.63%, 105 SELL per `output/v9_ab_comparison.txt`), the VN30 2015-2026 backtest drifted.
    This audit identifies the offending commit(s) via `git bisect` with CAGR-only gate (11.4% per D-03),
    applies a fix-forward waterfall (selective revert → v60_strict_mode preset → accept-and-document per D-07),
    and publishes the reconciled baseline tuple that downstream Phases 43-47 consume as the HARD gate reference.

    ## Bisect Endpoints

    | Anchor | Commit Hash | Subject | Reference |
    |--------|-------------|---------|-----------|
    | good (v6.0 ship) | <SHIP_COMMIT_FULL_HASH> | <SUBJECT from git log> | truth-of-record = `output/v6_combined_validation.txt` |
    | bad  (HEAD)      | <HEAD_FULL_HASH>        | <SUBJECT from git log> | drift = `output/v9_ab_comparison.txt` |

    Bisect surface area: <N> commits between anchors touching `strategies/mdm_hybrid/`, `core/indicators.py`, or `core/data_loader.py`.
    Full commit range: <N_total> commits (checked via `git log --oneline <ship>..HEAD | wc -l`).
    Expected bisect iterations: ~log2(N_total) engine runs (per D-01, ~8 for 253 commits).

    ## Reference Numbers

    ### v6.0 Truth-of-Record (from `output/v6_combined_validation.txt`, Fail-Safe column)

    | Metric | Value |
    |--------|-------|
    | Total Return | +238.8% |
    | CAGR | 11.5% |
    | Max Drawdown | -28.2% |
    | Transitions | 334 |
    | BUY time | 30.4% |
    | SELL signal count | 124 |
    | MA50-breakdown SELL share | 84% |

    ### Measured-Today Drift (from `output/v9_ab_comparison.txt`, baseline row)

    | Metric | Value | Delta vs v6.0 |
    |--------|-------|---------------|
    | Total Return | +213.4% | -25.4pp |
    | CAGR | 10.70% | -0.80pp |
    | Max Drawdown | -28.63% | -0.43pp |
    | Sharpe_rf3 | 0.461 | — (not in v6.0 report) |
    | Transitions | 296 | -38 |
    | BUY% / CASH% / SELL% | 29.8% / 38.8% / 31.4% | — |
    | SELL signal count | 105 | -19 |
    | MA50-breakdown SELL share | 100.0% | +16pp |

    ## Bisect Log

    _To be populated by plan 42-03._

    Table will have columns: `commit_hash | date | author | subject | cagr_pct | sell_count | max_dd_pct | verdict`.
    Rows are one per commit tested by `git bisect`, plus a final row with the identified offending commit(s).

    ## Root-Cause Narrative

    _To be populated by plan 42-03._

    Will describe: what the offending commit(s) changed, why the change caused CAGR drift, whether the drift is
    an intentional improvement, a bug, or an untested side effect.

    ## Reconciliation Outcome

    _To be populated by plan 42-04._

    One of: **fixed_by_revert** (selective `git revert` of offending commit), **fixed_by_preset** (added
    `v60_strict_mode: bool = False` to MDMV2Config per D-07 step 2), or **accepted_drift** (drift documented as
    intentional/unsafe-to-revert per D-07 step 3 — if chosen, this section will start with the exact string
    `**Reconciliation Outcome: Accepted drift, not fixed**` so downstream phases grep-detect the exception).

    Parity thresholds (D-09): CAGR within ±0.3pp of 11.5% AND SELL count within ±5 of 124 AND MaxDD within ±1.0pp of -28.2%.

    ## Reconciled Baseline

    _To be populated by plan 42-05 (literal numbers, not dynamic references, per D-16)._

    Will contain the full 18-field canonical tuple (D-12) as a human-readable markdown table,
    numerically identical to `output/v10_reconciled_baseline.json`.

    ## Invariants

    - **D-10:** Data window locked at `2015-01-05` → `2026-03-31` for every run in this audit.
    - **D-11:** No fail-safe logic changes allowed. If bisect lands on a fail-safe-touching commit, that finding
      becomes **accept-and-document** — it is NOT a fix candidate.
    - **D-14:** Downstream Phases 43-47 read from `output/v10_reconciled_baseline.json`, not from literals in this doc.
    - **D-15:** JSON `schema_version: 1` locks the tuple shape.

    ## References

    - `output/v6_combined_validation.txt` — v6.0 truth-of-record
    - `output/v9_ab_comparison.txt` — measured-today drift
    - `.planning/MILESTONES.md` — v6.0 entry, v9.0 entry
    - `.planning/phases/42-baseline-reconciliation/42-CONTEXT.md` — 22 locked decisions (D-01..D-22)
    - `analysis/bisect_v10_baseline.py` — bisect runner (plan 42-01)
    - `analysis/publish_v10_baseline.py` — reconciled-baseline publisher (plan 42-05)
    - `tests/test_baseline_determinism.py` — determinism regression (plan 42-06)
    ```

    Replace `<SHIP_COMMIT_FULL_HASH>`, `<HEAD_FULL_HASH>`, `<SUBJECT from git log>`, `<N>`, `<N_total>`, and `<today's date>` with the actual values from the git commands. Preserve markdown headers exactly — Wave 2/3/4 agents grep for them.
  </action>
  <acceptance_criteria>
    - File `docs/audits/v10_baseline_drift.md` exists
    - `grep "^# v10.0 Baseline Drift Audit" docs/audits/v10_baseline_drift.md` returns 1 match
    - `grep "^## Bisect Endpoints" docs/audits/v10_baseline_drift.md` returns 1 match
    - `grep "^## Bisect Log" docs/audits/v10_baseline_drift.md` returns 1 match
    - `grep "^## Root-Cause Narrative" docs/audits/v10_baseline_drift.md` returns 1 match
    - `grep "^## Reconciliation Outcome" docs/audits/v10_baseline_drift.md` returns 1 match
    - `grep "^## Reconciled Baseline" docs/audits/v10_baseline_drift.md` returns 1 match
    - `grep "37cfdc2\|<actual-v6-ship-full-hash>" docs/audits/v10_baseline_drift.md` returns >= 1 match (the full 40-char hash of the v6.0 ship commit)
    - `grep "good (v6.0 ship)" docs/audits/v10_baseline_drift.md` returns 1 match
    - `grep "bad  (HEAD)" docs/audits/v10_baseline_drift.md` returns 1 match (note two spaces between "bad" and "(HEAD)" to align the table)
    - `grep "11.5%" docs/audits/v10_baseline_drift.md` returns >= 1 match (v6.0 CAGR literal)
    - `grep "10.70%" docs/audits/v10_baseline_drift.md` returns >= 1 match (measured-today CAGR literal)
    - `grep "CAGR 11.4% per D-03" docs/audits/v10_baseline_drift.md` returns >= 1 match OR `grep "11.4% per D-03" docs/audits/v10_baseline_drift.md` returns >= 1 match (gate threshold cited)
    - `grep "D-10" docs/audits/v10_baseline_drift.md` returns >= 1 match (data-window invariant cited)
    - `grep "D-11" docs/audits/v10_baseline_drift.md` returns >= 1 match (no-fail-safe-changes invariant cited)
    - `grep "D-14" docs/audits/v10_baseline_drift.md` returns >= 1 match (downstream JSON-consumption invariant cited)
    - `grep "schema_version: 1" docs/audits/v10_baseline_drift.md` returns >= 1 match (D-15)
    - `grep "2015-01-05" docs/audits/v10_baseline_drift.md` returns >= 1 match (data window start)
    - `grep "2026-03-31" docs/audits/v10_baseline_drift.md` returns >= 1 match (data window end)
  </acceptance_criteria>
  <verify>
    <automated>test -f docs/audits/v10_baseline_drift.md && grep -q "^## Bisect Endpoints" docs/audits/v10_baseline_drift.md && grep -q "^## Bisect Log" docs/audits/v10_baseline_drift.md && grep -q "^## Reconciled Baseline" docs/audits/v10_baseline_drift.md && grep -q "2015-01-05" docs/audits/v10_baseline_drift.md && grep -q "2026-03-31" docs/audits/v10_baseline_drift.md && echo PASS</automated>
  </verify>
  <done>
    `docs/audits/v10_baseline_drift.md` exists with all required section headers, the two commit anchors pinned with full hashes, the v6.0 truth-of-record table and measured-today drift table both populated, and placeholder sections scoped for 42-03/04/05 to append into.
  </done>
</task>

</tasks>

<verification>
- Audit doc has all 6 required `##` section headers (Executive Summary, Bisect Endpoints, Reference Numbers, Bisect Log, Root-Cause Narrative, Reconciliation Outcome, Reconciled Baseline, Invariants, References)
- Both commit anchors resolved to full 40-char hashes
- v6.0 and measured-today reference tables quote literals from the source .txt files
- Downstream plans 42-03, 42-04, 42-05 can grep for the exact section headers and append content
</verification>

<success_criteria>
- Single-file audit doc created at `docs/audits/v10_baseline_drift.md`
- Commit anchors + reference numbers populated from git and the two shipped .txt files
- All placeholder sections present with exact headers for downstream-plan append operations
</success_criteria>

<output>
After completion, create `.planning/phases/42-baseline-reconciliation/42-02-SUMMARY.md`.
</output>
