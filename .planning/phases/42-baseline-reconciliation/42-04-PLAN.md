---
phase: 42-baseline-reconciliation
plan: 04
type: execute
wave: 3
depends_on:
  - "42-03"
files_modified:
  - docs/audits/v10_baseline_drift.md
  - strategies/mdm_hybrid/config.py
  - docs/rules_mdm_hybrid.md
autonomous: true
requirements:
  - BASE-02
must_haves:
  truths:
    - "Running the HybridEngine with v6.0-equivalent config on VN30 2015-2026 at the reconciled-HEAD commit produces CAGR within ±0.3pp of 11.5%, SELL count within ±5 of 124, MaxDD within ±1.0pp of -28.2% (D-09) — OR the Reconciliation Outcome section explicitly documents accept-and-document with justification"
    - "The reconciliation outcome is one of exactly three labels: fixed_by_revert | fixed_by_preset | accepted_drift (D-07, D-08)"
    - "The audit doc contains EXACTLY ONE strict `Label: \\`<label>\\`` line with one of the three literal labels in backticks (no choice-expression leakage)"
    - "No changes to fail-safe logic in strategies/mdm_hybrid/position_manager.py (D-11)"
    - "If preset-flag path (step 2) is taken, MDMV2Config gains a `v60_strict_mode: bool = False` field with clear docstring and branch-guards in the offending code path; and docs/rules_mdm_hybrid.md is updated in the SAME commit per CLAUDE.md Code-Docs Sync Rule"
  artifacts:
    - path: "docs/audits/v10_baseline_drift.md"
      provides: "Populated Reconciliation Outcome section with label + parity metrics"
      contains: "Reconciliation Outcome:"
    - path: "strategies/mdm_hybrid/config.py"
      provides: "MDMV2Config (possibly extended with v60_strict_mode field if preset path taken)"
      contains: "class MDMV2Config"
    - path: "docs/rules_mdm_hybrid.md"
      provides: "(Conditional on fixed_by_preset) updated to document v60_strict_mode semantics per CLAUDE.md Code-Docs Sync Rule"
      contains: "MDM Hybrid"
  key_links:
    - from: "docs/audits/v10_baseline_drift.md"
      to: "strategies/mdm_hybrid/config.py"
      via: "if fixed_by_preset, audit doc cites the new field and its default"
      pattern: "v60_strict_mode"
---

<objective>
Apply the fix-forward waterfall from D-07: (step 1) selective revert → (step 2) `v60_strict_mode` preset flag → (step 3) accept-and-document. Stop at the first step that restores parity per D-09 (CAGR within ±0.3pp of 11.5%, SELL within ±5 of 124, MaxDD within ±1.0pp of -28.2%). Populate the Reconciliation Outcome section of `docs/audits/v10_baseline_drift.md` with the outcome label, parity metrics, and path-specific artifacts (revert commit hash, preset-flag diff, or accept-and-document justification).

Purpose: Close BASE-02 — produce a reconciled-HEAD state where the engine reproduces the shipped v6.0 baseline within tolerance, OR produce a documented rationale for why drift must stand.

Output: Potentially a new commit (revert or preset-flag), updated `config.py` (if preset path), same-commit update to `docs/rules_mdm_hybrid.md` (if preset path, per Code-Docs Sync Rule), and populated Reconciliation Outcome section.
</objective>

<execution_context>
@C:/Users/trant/projects/mdm/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/trant/projects/mdm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-baseline-reconciliation/42-CONTEXT.md
@.planning/phases/42-baseline-reconciliation/42-03-SUMMARY.md
@docs/audits/v10_baseline_drift.md
@analysis/bisect_v10_baseline.py
@strategies/mdm_hybrid/config.py
@strategies/mdm_hybrid/mdm_hybrid_engine.py
@strategies/mdm_hybrid/position_manager.py
@docs/rules_mdm_hybrid.md
@output/v6_combined_validation.txt

<interfaces>
<!-- Parity thresholds (D-09) — encoded as explicit numeric ranges -->

v6.0 reference (from output/v6_combined_validation.txt):
  cagr_pct_ref     = 11.5
  sell_count_ref   = 124
  max_dd_pct_ref   = -28.2

Parity band (D-09):
  cagr_ok:    11.2 <= cagr_measured <= 11.8          (±0.3pp)
  sell_ok:    119  <= sell_measured <= 129           (±5)
  max_dd_ok:  -29.2 <= max_dd_measured <= -27.2      (±1.0pp)

Bisect gate vs parity band (B1 clarification — tied to plan 42-01 design note):
  - Bisect gate (plan 42-01): 11.4 (tight — finds earliest drift)
  - D-09 parity acceptance band: 11.2–11.8 (HEAD tolerance)
  - Sub-spec drift case: if the offending commit's CAGR ∈ [11.2, 11.4] (below bisect gate
    but INSIDE the parity band), HEAD may already satisfy D-09 without a fix.
    → In this case, use label `accepted_drift` with an explicit "sub-spec drift" annotation
      in the Reconciliation Outcome narrative (e.g., "accepted_drift (sub-spec drift within
      D-09 parity band)"). The underlying label literal remains `accepted_drift` — one of
      the three canonical values; no new label is introduced. The JSON schema enum in
      plan 42-05 stays at three values.

Fix-forward waterfall (D-07, D-08):
  step 1: selective git revert of offending commit; STOP if all three parity bands pass
  step 2: add MDMV2Config.v60_strict_mode: bool = False field + targeted branch guards; STOP if all three parity bands pass
  step 3: accept-and-document (audit doc flagged section "**Reconciliation Outcome: Accepted drift, not fixed**")

No-fly zone (D-11, REQUIREMENTS.md Out of Scope):
  - strategies/mdm_hybrid/position_manager.py fail-safe code (lines containing "fail_safe") may NOT be modified
  - If offending commit touched fail-safe, jump directly to step 3 (accept-and-document)

Run-verification command (for each step's parity check):
  `uv run python analysis/bisect_v10_baseline.py` + parse BISECT_RESULT: line
  After a reconciliation change, bisect gate returns exit 0 (CAGR >= 11.4) if fix succeeded.
  Full parity check (CAGR + SELL + MaxDD) requires parsing the BISECT_RESULT line manually.

Label line contract (B2 strict format — binding):
  The audit doc MUST contain EXACTLY ONE line matching the regex:
    ^Label: `(fixed_by_revert|fixed_by_preset|accepted_drift)`$
  The executor writes the line `Label: ` followed by EXACTLY ONE of the three literal
  strings wrapped in backticks. Do NOT write a choice-expression like
  `fixed_by_revert | fixed_by_preset | accepted_drift` (that expression must not appear
  anywhere in the audit doc; plan 42-05 treats that as an error).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Attempt selective revert (step 1); if fails or unsafe, attempt v60_strict_mode preset flag (step 2); else document accept-and-document (step 3). Write outcome.</name>
  <files>strategies/mdm_hybrid/config.py, docs/audits/v10_baseline_drift.md, docs/rules_mdm_hybrid.md</files>
  <read_first>
    - docs/audits/v10_baseline_drift.md (## Bisect Log and ## Root-Cause Narrative — the offending commit hash and file-level impact; ## Bisect Endpoints — the phase-start "bad" anchor hash used for D-11 diff check)
    - strategies/mdm_hybrid/config.py (MDMV2Config dataclass at lines 17-97, VN30_PRESET at line 129)
    - strategies/mdm_hybrid/position_manager.py (lines containing "fail_safe" — confirm the no-fly zone before touching anything)
    - strategies/mdm_hybrid/mdm_hybrid_engine.py (entry point for any preset-flag branch-guards)
    - docs/rules_mdm_hybrid.md (current contents — if preset path taken, must update in same commit per Code-Docs Sync Rule)
    - analysis/bisect_v10_baseline.py (for post-revert re-verification)
    - .planning/phases/42-baseline-reconciliation/42-CONTEXT.md (D-07, D-08, D-09, D-10, D-11 binding)
  </read_first>
  <action>
    Execute the fix-forward waterfall strictly in order. Before each step, check D-11 no-fly zone.

    **PRE-CHECK 0: Capture phase-start anchor hash (for M4 D-11 diff check).**
    Resolve `PHASE_START_HASH` from the audit doc's `## Bisect Endpoints` section (this is the
    HEAD hash plan 42-02 recorded as the "bad" anchor — same hash that HEAD was at when this
    phase started, since plans 42-01/02/03 only modified scripts and docs, not engine code):
    ```bash
    # The bad-anchor row in ## Bisect Endpoints has the phase-start hash as the 40-char hex
    # on the line starting with "| bad  (HEAD)" (two spaces between 'bad' and '(HEAD)'
    # per plan 42-02's literal header).
    PHASE_START_HASH=$(grep "^| bad  (HEAD)" docs/audits/v10_baseline_drift.md | grep -oE "\b[a-f0-9]{40}\b" | head -1)
    # Fallback: if the above is empty, use merge-base with the v6.0 ship commit:
    if [ -z "$PHASE_START_HASH" ]; then
        SHIP=$(grep "^| good (v6.0 ship)" docs/audits/v10_baseline_drift.md | grep -oE "\b[a-f0-9]{40}\b" | head -1)
        PHASE_START_HASH=$(git merge-base HEAD $SHIP)
        echo "PHASE_START_HASH resolved via merge-base fallback: $PHASE_START_HASH"
    fi
    # Sanity check: must be a 40-char hex, else abort the plan with a clear error
    echo "$PHASE_START_HASH" | grep -qE "^[a-f0-9]{40}$" || { echo "ERROR: could not resolve PHASE_START_HASH"; exit 1; }
    ```
    Use `$PHASE_START_HASH` wherever the D-11 compliance check or plan-start diff check appears below.

    **PRE-CHECK 1: No-fly zone (D-11).** Read the offending commit from `docs/audits/v10_baseline_drift.md ## Root-Cause Narrative`. Run `git show --stat <offending_full_hash>` and `git show <offending_full_hash> -- strategies/mdm_hybrid/position_manager.py`. If the diff touches any line containing `fail_safe`, `fail-safe`, or `FailSafe` → **skip directly to STEP 3 (accept-and-document)**. Write in the outcome section: "Offending commit touches fail-safe; D-11 forbids modification; step 1 and step 2 skipped."

    ---

    **STEP 1: Selective revert.**

    1. `git revert --no-edit <offending_full_hash>` (use `--no-commit` if you want to inspect first, then `git commit -m "revert(42-04): revert <offending_short_hash> to restore v6.0 baseline CAGR (BASE-02)"`).
    2. If `git revert` produces merge conflicts, abort: `git revert --abort`. Revert is not self-contained → proceed to STEP 2.
    3. If revert succeeds cleanly, re-run `uv run python analysis/bisect_v10_baseline.py` and capture the BISECT_RESULT line.
    4. Parse cagr, sell, max_dd. Check parity band:
       - `cagr_ok    = 11.2 <= cagr <= 11.8`
       - `sell_ok    = 119  <= sell <= 129`
       - `max_dd_ok  = -29.2 <= max_dd <= -27.2`
    5. If ALL THREE parity bands pass: STOP. Reconciliation outcome = `fixed_by_revert`. Skip to WRITE OUTCOME.
    6. If CAGR passes but SELL or MaxDD fails (D-09 "parity-suspect"): `git revert HEAD` (undo the revert) and proceed to STEP 2. Document that pure revert was numerically incomplete.
    7. If CAGR still fails (revert didn't help): `git revert HEAD` and proceed to STEP 2.

    ---

    **STEP 2: `v60_strict_mode` preset flag.**

    1. Edit `strategies/mdm_hybrid/config.py`. In the `MDMV2Config` dataclass body, add a new field after the existing `name: str = "default"` line:
       ```python
           # v6.0 Strict Mode (Phase 42 BASE-02, D-07 step 2)
           # When True, restores v6.0-shipped semantics by branch-guarding the
           # drift-introducing code path identified in docs/audits/v10_baseline_drift.md.
           # Default False preserves current v7/v8/v9 behavior; v10.0 macro baseline runs
           # set this True. See 42-CONTEXT.md D-07 for rationale.
           v60_strict_mode: bool = False
       ```

    2. In the offending code (identified by bisect — file + line range from `git show <offending_hash>`), add branch-guards that restore v6.0 semantics when `v60_strict_mode=True`. Example shape (adapt to actual code):
       ```python
       if cfg.v60_strict_mode:
           # v6.0 pre-drift path
           <original logic>
       else:
           # post-drift path (current default)
           <new logic>
       ```
       Keep the diff minimal — one branch-guard per affected function. Do NOT touch unrelated logic. Do NOT touch `position_manager.py` fail-safe code (D-11).

       **CLAUDE.md Code-Docs Sync Rule (m1):** If the branch-guarded code lives in
       `strategies/mdm_hybrid/`, you MUST ALSO update `docs/rules_mdm_hybrid.md` in the SAME
       commit to document `v60_strict_mode` semantics. Add a subsection like:
       ```markdown
       ## v60_strict_mode (Phase 42 BASE-02)

       `MDMV2Config.v60_strict_mode: bool` (default `False`).

       When `True`, restores v6.0-shipped semantics by branch-guarding the drift-introducing
       code path identified in `docs/audits/v10_baseline_drift.md` (Phase 42 bisect).
       `VN30_PRESET` leaves this `False`; v10.0 macro baseline runs and the Phase 42
       determinism regression test set it `True`.

       - Affected code: `<list the file(s) with branch-guards>`
       - Pre-drift (strict) path: `<short description>`
       - Post-drift (default) path: `<short description>`
       ```
       Stage both files in the same commit: `git add strategies/mdm_hybrid/config.py docs/rules_mdm_hybrid.md <any branch-guarded file>`.

    3. Update `VN30_PRESET` at line 129 of `config.py` to add `v60_strict_mode=False` explicitly (so the preset is self-documenting).

    4. In `analysis/bisect_v10_baseline.py`, update the preset-building line to pass `v60_strict_mode=True` when running the reconciliation check (this is the only legal modification to the bisect script; comment the change with "Phase 42-04: set True to exercise v6.0-strict path"). NOTE: Plan 42-01's bisect script used `replace(VN30_PRESET, atr_buffer_enabled=False, refined_dd_enabled=False)` — extend that call to include `v60_strict_mode=True`.

    5. Re-run `uv run python analysis/bisect_v10_baseline.py` and capture BISECT_RESULT line.

    6. Parse cagr, sell, max_dd. Check parity band (same as step 1).
    7. If ALL THREE parity bands pass: STOP. Reconciliation outcome = `fixed_by_preset`. Commit with message `feat(42-04): add v60_strict_mode flag to restore v6.0 baseline (BASE-02)` — the commit must include `strategies/mdm_hybrid/config.py`, any branch-guarded file(s), AND `docs/rules_mdm_hybrid.md` (Code-Docs Sync Rule). Skip to WRITE OUTCOME.
    8. If parity fails: revert the config.py + rules_mdm_hybrid.md + branch-guard changes (`git checkout strategies/mdm_hybrid/config.py analysis/bisect_v10_baseline.py docs/rules_mdm_hybrid.md <branch-guarded files>`), proceed to STEP 3.

    ---

    **STEP 3: Accept-and-document.**

    1. Do NOT commit any code changes. The drift is accepted as the new baseline.
    2. In the audit-doc outcome section (see WRITE OUTCOME below), begin with the exact string:
       `**Reconciliation Outcome: Accepted drift, not fixed**`
    3. Explain: what changed, why reverting is infeasible (conflicts, wired into v7/v8), why preset-flag is infeasible (would break v7+ tests, touches fail-safe, etc.), what metric gap remains, and why the drifted baseline is the correct reference going forward.
    4. **Sub-spec drift sub-case (B1):** if the offending-commit CAGR or HEAD CAGR lies in
       `[11.2, 11.4]` (inside the D-09 parity band but below the bisect gate), annotate the
       narrative explicitly: "Sub-spec drift: CAGR is BELOW the 11.4 bisect gate but INSIDE
       the D-09 parity acceptance band [11.2, 11.8]. No fix required for D-09 compliance;
       drift documented for Phase 43-47 reference." The Label line still uses the literal
       `accepted_drift` (one of the three canonical values — do NOT introduce a new label).

    ---

    **WRITE OUTCOME (all three paths converge here).**

    Replace the `_To be populated by plan 42-04._` line under `## Reconciliation Outcome` in `docs/audits/v10_baseline_drift.md` with a populated section. Structure:

    ```markdown
    ## Reconciliation Outcome

    **Reconciliation Outcome: <Fixed by revert | Fixed by preset | Accepted drift, not fixed>**

    Label: `<EXACTLY_ONE_OF: fixed_by_revert OR fixed_by_preset OR accepted_drift>`

    ### Path Taken

    <Narrative explaining which step of D-07 waterfall succeeded, or why all three failed and step 3 was the outcome. If sub-spec drift applies (B1), annotate explicitly as described in STEP 3 step 4.>

    ### Parity Check (D-09 thresholds)

    | Metric | Reference (v6.0) | Measured (reconciled-HEAD) | Delta | Band (D-09) | Pass? |
    |--------|------------------|----------------------------|-------|-------------|-------|
    | CAGR (%) | 11.5 | <measured> | <delta> | ±0.3pp → [11.2, 11.8] | <PASS/FAIL> |
    | SELL count | 124 | <measured> | <delta> | ±5 → [119, 129] | <PASS/FAIL> |
    | MaxDD (%) | -28.2 | <measured> | <delta> | ±1.0pp → [-29.2, -27.2] | <PASS/FAIL> |

    ### Artifacts Produced

    <If fixed_by_revert: the revert commit hash, `git show <revert_hash> --stat` output.>
    <If fixed_by_preset: the commit hash, the `v60_strict_mode` field diff, the branch-guard diff, AND the `docs/rules_mdm_hybrid.md` update diff (all in one commit per Code-Docs Sync Rule).>
    <If accepted_drift: no artifacts; downstream phases read the drifted measured baseline as the reference.>

    ### D-11 Compliance Statement

    Verified: no changes to `strategies/mdm_hybrid/position_manager.py` fail-safe logic in this plan's commits.
    Check: `git diff <PHASE_START_HASH>..HEAD -- strategies/mdm_hybrid/position_manager.py | grep -i fail_safe` returns empty, where `<PHASE_START_HASH>` = `<the literal 40-char hash resolved in PRE-CHECK 0 above>`.
    ```

    **CRITICAL label-line contract (B2):** the `Label:` line MUST be written EXACTLY ONCE, using
    ONE of the three literal strings inside backticks — NOT the choice-expression shown in the
    template. For example:
    - CORRECT: `Label: \`fixed_by_preset\``
    - WRONG (choice-expression leak): `Label: \`fixed_by_revert | fixed_by_preset | accepted_drift\``
    - WRONG (multiple labels): `Label: \`fixed_by_revert\` \`accepted_drift\``
    Plan 42-05 treats ANY of the wrong shapes as a ValueError.

    Fill in every `<...>` placeholder with the actual values (including the PHASE_START_HASH literal resolved in PRE-CHECK 0).

    **If fixed_by_preset:** leave the config.py `v60_strict_mode` field, branch-guards, and `docs/rules_mdm_hybrid.md` update in place — plan 42-06 determinism test will exercise the field. Commit message: `feat(42-04): add v60_strict_mode flag to restore v6.0 baseline (BASE-02)`. The commit MUST list both `strategies/mdm_hybrid/config.py` and `docs/rules_mdm_hybrid.md` as changed files (Code-Docs Sync Rule).

    **If fixed_by_revert:** the revert commit is already in the git log. Commit message style: `revert(42-04): revert <offending_short_hash> to restore v6.0 baseline CAGR (BASE-02)`.

    **If accepted_drift:** no code commits in this plan; only the audit-doc update.

    **D-11 final check:** Before completing, run:
    ```bash
    git diff $PHASE_START_HASH..HEAD -- strategies/mdm_hybrid/position_manager.py
    ```
    Verify the diff does NOT contain any line matching `fail_safe` (case-insensitive). If it does → revert the offending changes in position_manager.py and re-run parity check. If the offending commit was fail-safe related, you should have taken STEP 3 from the start.
  </action>
  <acceptance_criteria>
    - `grep "^## Reconciliation Outcome$" docs/audits/v10_baseline_drift.md` returns 1 match
    - `grep "^\*\*Reconciliation Outcome:" docs/audits/v10_baseline_drift.md` returns 1 match
    - `grep "_To be populated by plan 42-04._" docs/audits/v10_baseline_drift.md` returns 0 matches
    - **B2 strict Label line (exactly one):** `grep -cE "^Label: \`(fixed_by_revert|fixed_by_preset|accepted_drift)\`$" docs/audits/v10_baseline_drift.md` returns exactly `1` (not 0, not >1)
    - **B2 no choice-expression leak:** `grep -c "fixed_by_revert | fixed_by_preset" docs/audits/v10_baseline_drift.md` returns `0` (guards against the template choice-expression leaking in)
    - `grep "### Parity Check (D-09 thresholds)" docs/audits/v10_baseline_drift.md` returns 1 match
    - `grep "### D-11 Compliance Statement" docs/audits/v10_baseline_drift.md` returns 1 match
    - Running `uv run python analysis/bisect_v10_baseline.py` at current HEAD returns:
      - If outcome is `fixed_by_revert` or `fixed_by_preset`: exit code 0 AND BISECT_RESULT cagr value parsed as float satisfies `11.2 <= cagr <= 11.8`
      - If outcome is `accepted_drift`: exit code 1 is acceptable (parity not achieved, documented as such)
    - **M4 D-11 compliance with resolved hash:** the audit doc's `### D-11 Compliance Statement` section contains the literal 40-char `PHASE_START_HASH` (NOT the placeholder `<phase-start-hash>`), verifiable by `grep -E "git diff [a-f0-9]{40}\.\.HEAD -- strategies/mdm_hybrid/position_manager.py" docs/audits/v10_baseline_drift.md` returns >= 1 match AND `grep "<phase-start-hash>" docs/audits/v10_baseline_drift.md` returns 0 matches
    - **M4 D-11 runtime check:** `PSH=$(grep -oE "git diff [a-f0-9]{40}" docs/audits/v10_baseline_drift.md | head -1 | awk '{print $3}'); git diff $PSH..HEAD -- strategies/mdm_hybrid/position_manager.py | grep -i fail_safe` returns empty (no fail-safe diffs)
    - If outcome is `fixed_by_preset`:
      - `grep "v60_strict_mode: bool = False" strategies/mdm_hybrid/config.py` returns 1 match
      - `grep "v60_strict_mode=False" strategies/mdm_hybrid/config.py` returns >= 1 match (VN30_PRESET explicit value)
      - `grep "v60_strict_mode=True" analysis/bisect_v10_baseline.py` returns 1 match (the bisect script's reconciliation-check path)
      - **m1 Code-Docs Sync:** `grep "v60_strict_mode" docs/rules_mdm_hybrid.md` returns >= 1 match (docs updated in the preset commit)
      - **m1 Same-commit check:** the commit touching `strategies/mdm_hybrid/config.py` with the `v60_strict_mode` addition ALSO touches `docs/rules_mdm_hybrid.md`: `PRESET_COMMIT=$(git log -n1 --format=%H -- strategies/mdm_hybrid/config.py); git show --stat $PRESET_COMMIT | grep -q "docs/rules_mdm_hybrid.md" && git show --stat $PRESET_COMMIT | grep -q "strategies/mdm_hybrid/config.py"` passes (both files appear in the commit's stat output)
    - If outcome is `accepted_drift`:
      - `grep "^\*\*Reconciliation Outcome: Accepted drift, not fixed\*\*" docs/audits/v10_baseline_drift.md` returns 1 match (exact literal for downstream grep)
      - No new commits on `strategies/mdm_hybrid/config.py` or `strategies/mdm_hybrid/position_manager.py` since plan start (verify with `git log --oneline $PHASE_START_HASH..HEAD -- strategies/mdm_hybrid/config.py strategies/mdm_hybrid/position_manager.py` returns 0 lines)
      - **B1 sub-spec drift annotation (conditional):** if the offending-commit CAGR or HEAD CAGR ∈ [11.2, 11.4], `grep "sub-spec drift" docs/audits/v10_baseline_drift.md` returns >= 1 match (the narrative explicitly flags this sub-case)
  </acceptance_criteria>
  <verify>
    <automated>OUTCOME=$(grep -oE "fixed_by_revert|fixed_by_preset|accepted_drift" docs/audits/v10_baseline_drift.md | head -1); test -n "$OUTCOME" || { echo FAIL no-outcome; exit 1; }; LABEL_COUNT=$(grep -cE "^Label: \`(fixed_by_revert|fixed_by_preset|accepted_drift)\`$" docs/audits/v10_baseline_drift.md); [ "$LABEL_COUNT" = "1" ] || { echo "FAIL: expected exactly 1 strict Label line, got $LABEL_COUNT"; exit 1; }; CHOICE_LEAK=$(grep -c "fixed_by_revert | fixed_by_preset" docs/audits/v10_baseline_drift.md); [ "$CHOICE_LEAK" = "0" ] || { echo "FAIL: choice-expression leaked into audit doc"; exit 1; }; grep -q "^## Reconciliation Outcome$" docs/audits/v10_baseline_drift.md && grep -q "### D-11 Compliance Statement" docs/audits/v10_baseline_drift.md && ! grep -q "_To be populated by plan 42-04._" docs/audits/v10_baseline_drift.md && ! grep -q "<phase-start-hash>" docs/audits/v10_baseline_drift.md && grep -qE "git diff [a-f0-9]{40}\.\.HEAD -- strategies/mdm_hybrid/position_manager.py" docs/audits/v10_baseline_drift.md && echo "PASS outcome=$OUTCOME label_count=$LABEL_COUNT"</automated>
  </verify>
  <done>
    Fix-forward waterfall executed per D-07; outcome label recorded as EXACTLY ONE of fixed_by_revert / fixed_by_preset / accepted_drift on a single strict `Label:` line (B2); parity-check table populated with reference vs measured vs delta vs band vs pass; D-11 compliance verified with RESOLVED 40-char phase-start hash (M4); if preset path taken, docs/rules_mdm_hybrid.md updated in the SAME commit (m1); if sub-spec drift case applies, narrative explicitly annotates it (B1); no fail-safe code changes.
  </done>
</task>

</tasks>

<verification>
- Reconciliation outcome label is exactly one of `fixed_by_revert`, `fixed_by_preset`, `accepted_drift` (written as a literal on a single strict Label line per B2)
- No choice-expression `fixed_by_revert | fixed_by_preset | accepted_drift` leaks anywhere in the audit doc
- Parity check table populated with live-measured values, not placeholders
- D-11 compliance grep passes (no fail_safe diffs) using the RESOLVED 40-char phase-start hash
- If preset path taken: `v60_strict_mode` exists in MDMV2Config with default False, VN30_PRESET explicitly sets False, offending code path has a branch-guard using this field, and `docs/rules_mdm_hybrid.md` is updated in the SAME commit
- If sub-spec drift case applies (offending CAGR ∈ [11.2, 11.4]): narrative annotates "sub-spec drift" explicitly while keeping the label literal as `accepted_drift`
</verification>

<success_criteria>
- BASE-02 closed: either HybridEngine at HEAD matches v6.0 within D-09 band, OR the accepted-drift path is documented with explicit justification (including sub-spec drift annotation if applicable)
- Reconciled-HEAD state is the stable input for plans 42-05 (publish) and 42-06 (determinism test)
- Zero changes to fail-safe logic verified
- Code-Docs Sync Rule honored for preset-flag path
</success_criteria>

<output>
After completion, create `.planning/phases/42-baseline-reconciliation/42-04-SUMMARY.md`.
</output>
