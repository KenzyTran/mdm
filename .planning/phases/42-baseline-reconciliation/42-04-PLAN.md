---
phase: 42-baseline-reconciliation
plan: 04
type: execute
wave: 3
depends_on:
  - "42-03"
files_modified:
  - docs/audits/v10_baseline_drift.md
  - strategies/mdm_hybrid/config.py
autonomous: true
requirements:
  - BASE-02
must_haves:
  truths:
    - "Running the HybridEngine with v6.0-equivalent config on VN30 2015-2026 at the reconciled-HEAD commit produces CAGR within ±0.3pp of 11.5%, SELL count within ±5 of 124, MaxDD within ±1.0pp of -28.2% (D-09) — OR the Reconciliation Outcome section explicitly documents accept-and-document with justification"
    - "The reconciliation outcome is one of exactly three labels: fixed_by_revert | fixed_by_preset | accepted_drift (D-07, D-08)"
    - "No changes to fail-safe logic in strategies/mdm_hybrid/position_manager.py (D-11)"
    - "If preset-flag path (step 2) is taken, MDMV2Config gains a `v60_strict_mode: bool = False` field with clear docstring and branch-guards in the offending code path"
  artifacts:
    - path: "docs/audits/v10_baseline_drift.md"
      provides: "Populated Reconciliation Outcome section with label + parity metrics"
      contains: "Reconciliation Outcome:"
    - path: "strategies/mdm_hybrid/config.py"
      provides: "MDMV2Config (possibly extended with v60_strict_mode field if preset path taken)"
      contains: "class MDMV2Config"
  key_links:
    - from: "docs/audits/v10_baseline_drift.md"
      to: "strategies/mdm_hybrid/config.py"
      via: "if fixed_by_preset, audit doc cites the new field and its default"
      pattern: "v60_strict_mode"
---

<objective>
Apply the fix-forward waterfall from D-07: (step 1) selective revert → (step 2) `v60_strict_mode` preset flag → (step 3) accept-and-document. Stop at the first step that restores parity per D-09 (CAGR within ±0.3pp of 11.5%, SELL within ±5 of 124, MaxDD within ±1.0pp of -28.2%). Populate the Reconciliation Outcome section of `docs/audits/v10_baseline_drift.md` with the outcome label, parity metrics, and path-specific artifacts (revert commit hash, preset-flag diff, or accept-and-document justification).

Purpose: Close BASE-02 — produce a reconciled-HEAD state where the engine reproduces the shipped v6.0 baseline within tolerance, OR produce a documented rationale for why drift must stand.

Output: Potentially a new commit (revert or preset-flag), updated `config.py` (if preset path), and populated Reconciliation Outcome section.
</objective>

<execution_context>
@C:/Users/trant/projects/mdm/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/trant/projects/mdm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-baseline-reconciliation/42-CONTEXT.md
@.planning/phases/42-baseline-reconciliation/42-03-SUMMARY.md
@docs/audits/v10_baseline_drift.md
@analysis/bisect_v10_baseline.py
@strategies/mdm_hybrid/config.py
@strategies/mdm_hybrid/mdm_hybrid_engine.py
@strategies/mdm_hybrid/position_manager.py
@output/v6_combined_validation.txt

<interfaces>
<!-- Parity thresholds (D-09) — encoded as explicit numeric ranges -->

v6.0 reference (from output/v6_combined_validation.txt):
  cagr_pct_ref     = 11.5
  sell_count_ref   = 124
  max_dd_pct_ref   = -28.2

Parity band (D-09):
  cagr_ok:    11.2 <= cagr_measured <= 11.8          (±0.3pp)
  sell_ok:    119  <= sell_measured <= 129           (±5)
  max_dd_ok:  -29.2 <= max_dd_measured <= -27.2      (±1.0pp)

Fix-forward waterfall (D-07, D-08):
  step 1: selective git revert of offending commit; STOP if all three parity bands pass
  step 2: add MDMV2Config.v60_strict_mode: bool = False field + targeted branch guards; STOP if all three parity bands pass
  step 3: accept-and-document (audit doc flagged section "**Reconciliation Outcome: Accepted drift, not fixed**")

No-fly zone (D-11, REQUIREMENTS.md Out of Scope):
  - strategies/mdm_hybrid/position_manager.py fail-safe code (lines containing "fail_safe") may NOT be modified
  - If offending commit touched fail-safe, jump directly to step 3 (accept-and-document)

Run-verification command (for each step's parity check):
  `uv run python analysis/bisect_v10_baseline.py` + parse BISECT_RESULT: line
  After a reconciliation change, bisect gate returns exit 0 (CAGR >= 11.4) if fix succeeded.
  Full parity check (CAGR + SELL + MaxDD) requires parsing the BISECT_RESULT line manually.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Attempt selective revert (step 1); if fails or unsafe, attempt v60_strict_mode preset flag (step 2); else document accept-and-document (step 3). Write outcome.</name>
  <files>strategies/mdm_hybrid/config.py, docs/audits/v10_baseline_drift.md</files>
  <read_first>
    - docs/audits/v10_baseline_drift.md (## Bisect Log and ## Root-Cause Narrative — the offending commit hash and file-level impact)
    - strategies/mdm_hybrid/config.py (MDMV2Config dataclass at lines 17-97, VN30_PRESET at line 129)
    - strategies/mdm_hybrid/position_manager.py (lines containing "fail_safe" — confirm the no-fly zone before touching anything)
    - strategies/mdm_hybrid/mdm_hybrid_engine.py (entry point for any preset-flag branch-guards)
    - analysis/bisect_v10_baseline.py (for post-revert re-verification)
    - .planning/phases/42-baseline-reconciliation/42-CONTEXT.md (D-07, D-08, D-09, D-10, D-11 binding)
  </read_first>
  <action>
    Execute the fix-forward waterfall strictly in order. Before each step, check D-11 no-fly zone.

    **PRE-CHECK: No-fly zone (D-11).** Read the offending commit from `docs/audits/v10_baseline_drift.md ## Root-Cause Narrative`. Run `git show --stat <offending_full_hash>` and `git show <offending_full_hash> -- strategies/mdm_hybrid/position_manager.py`. If the diff touches any line containing `fail_safe`, `fail-safe`, or `FailSafe` → **skip directly to STEP 3 (accept-and-document)**. Write in the outcome section: "Offending commit touches fail-safe; D-11 forbids modification; step 1 and step 2 skipped."

    ---

    **STEP 1: Selective revert.**

    1. `git revert --no-edit <offending_full_hash>` (use `--no-commit` if you want to inspect first, then `git commit -m "revert(42-04): revert <offending_short_hash> to restore v6.0 baseline CAGR (BASE-02)"`).
    2. If `git revert` produces merge conflicts, abort: `git revert --abort`. Revert is not self-contained → proceed to STEP 2.
    3. If revert succeeds cleanly, re-run `uv run python analysis/bisect_v10_baseline.py` and capture the BISECT_RESULT line.
    4. Parse cagr, sell, max_dd. Check parity band:
       - `cagr_ok    = 11.2 <= cagr <= 11.8`
       - `sell_ok    = 119  <= sell <= 129`
       - `max_dd_ok  = -29.2 <= max_dd <= -27.2`
    5. If ALL THREE parity bands pass: STOP. Reconciliation outcome = `fixed_by_revert`. Skip to WRITE OUTCOME.
    6. If CAGR passes but SELL or MaxDD fails (D-09 "parity-suspect"): `git revert HEAD` (undo the revert) and proceed to STEP 2. Document that pure revert was numerically incomplete.
    7. If CAGR still fails (revert didn't help): `git revert HEAD` and proceed to STEP 2.

    ---

    **STEP 2: `v60_strict_mode` preset flag.**

    1. Edit `strategies/mdm_hybrid/config.py`. In the `MDMV2Config` dataclass body, add a new field after the existing `name: str = "default"` line:
       ```python
           # v6.0 Strict Mode (Phase 42 BASE-02, D-07 step 2)
           # When True, restores v6.0-shipped semantics by branch-guarding the
           # drift-introducing code path identified in docs/audits/v10_baseline_drift.md.
           # Default False preserves current v7/v8/v9 behavior; v10.0 macro baseline runs
           # set this True. See 42-CONTEXT.md D-07 for rationale.
           v60_strict_mode: bool = False
       ```

    2. In the offending code (identified by bisect — file + line range from `git show <offending_hash>`), add branch-guards that restore v6.0 semantics when `v60_strict_mode=True`. Example shape (adapt to actual code):
       ```python
       if cfg.v60_strict_mode:
           # v6.0 pre-drift path
           <original logic>
       else:
           # post-drift path (current default)
           <new logic>
       ```
       Keep the diff minimal — one branch-guard per affected function. Do NOT touch unrelated logic. Do NOT touch `position_manager.py` fail-safe code (D-11).

    3. Update `VN30_PRESET` at line 129 of `config.py` to add `v60_strict_mode=False` explicitly (so the preset is self-documenting).

    4. In `analysis/bisect_v10_baseline.py`, update the preset-building line to pass `v60_strict_mode=True` when running the reconciliation check (this is the only legal modification to the bisect script; comment the change with "Phase 42-04: set True to exercise v6.0-strict path"). NOTE: Plan 42-01's bisect script used `replace(VN30_PRESET, atr_buffer_enabled=False, refined_dd_enabled=False)` — extend that call to include `v60_strict_mode=True`.

    5. Re-run `uv run python analysis/bisect_v10_baseline.py` and capture BISECT_RESULT line.

    6. Parse cagr, sell, max_dd. Check parity band (same as step 1).
    7. If ALL THREE parity bands pass: STOP. Reconciliation outcome = `fixed_by_preset`. Commit with message `feat(42-04): add v60_strict_mode flag to restore v6.0 baseline (BASE-02)`. Skip to WRITE OUTCOME.
    8. If parity fails: revert the config.py changes (`git checkout strategies/mdm_hybrid/config.py analysis/bisect_v10_baseline.py`), proceed to STEP 3.

    ---

    **STEP 3: Accept-and-document.**

    1. Do NOT commit any code changes. The drift is accepted as the new baseline.
    2. In the audit-doc outcome section (see WRITE OUTCOME below), begin with the exact string:
       `**Reconciliation Outcome: Accepted drift, not fixed**`
    3. Explain: what changed, why reverting is infeasible (conflicts, wired into v7/v8), why preset-flag is infeasible (would break v7+ tests, touches fail-safe, etc.), what metric gap remains, and why the drifted baseline is the correct reference going forward.

    ---

    **WRITE OUTCOME (all three paths converge here).**

    Replace the `_To be populated by plan 42-04._` line under `## Reconciliation Outcome` in `docs/audits/v10_baseline_drift.md` with a populated section. Structure:

    ```markdown
    ## Reconciliation Outcome

    **Reconciliation Outcome: <Fixed by revert | Fixed by preset | Accepted drift, not fixed>**

    Label: `<fixed_by_revert | fixed_by_preset | accepted_drift>` (this label is stored verbatim in output/v10_reconciled_baseline.json by plan 42-05)

    ### Path Taken

    <Narrative explaining which step of D-07 waterfall succeeded, or why all three failed and step 3 was the outcome.>

    ### Parity Check (D-09 thresholds)

    | Metric | Reference (v6.0) | Measured (reconciled-HEAD) | Delta | Band (D-09) | Pass? |
    |--------|------------------|----------------------------|-------|-------------|-------|
    | CAGR (%) | 11.5 | <measured> | <delta> | ±0.3pp → [11.2, 11.8] | <PASS/FAIL> |
    | SELL count | 124 | <measured> | <delta> | ±5 → [119, 129] | <PASS/FAIL> |
    | MaxDD (%) | -28.2 | <measured> | <delta> | ±1.0pp → [-29.2, -27.2] | <PASS/FAIL> |

    ### Artifacts Produced

    <If fixed_by_revert: the revert commit hash, `git show <revert_hash> --stat` output.>
    <If fixed_by_preset: the commit hash, the `v60_strict_mode` field diff, and the branch-guard diff.>
    <If accepted_drift: no artifacts; downstream phases read the drifted measured baseline as the reference.>

    ### D-11 Compliance Statement

    Verified: no changes to `strategies/mdm_hybrid/position_manager.py` fail-safe logic in this plan's commits.
    Check: `git diff <start>..HEAD -- strategies/mdm_hybrid/position_manager.py | grep -i fail_safe` returns empty.
    ```

    Fill in every `<...>` placeholder with the actual values.

    **If fixed_by_preset:** leave the config.py `v60_strict_mode` field and branch-guards in place — plan 42-06 determinism test will exercise them. Commit message: `feat(42-04): add v60_strict_mode flag to restore v6.0 baseline (BASE-02)`.

    **If fixed_by_revert:** the revert commit is already in the git log. Commit message style: `revert(42-04): revert <offending_short_hash> to restore v6.0 baseline CAGR (BASE-02)`.

    **If accepted_drift:** no code commits in this plan; only the audit-doc update.

    **D-11 final check:** Before completing, run:
    ```bash
    # If fixed_by_revert: compare from before-revert to HEAD
    # If fixed_by_preset: compare from pre-plan to HEAD
    git diff <plan_start_hash>..HEAD -- strategies/mdm_hybrid/position_manager.py
    ```
    Verify the diff does NOT contain any line matching `fail_safe` (case-insensitive). If it does → revert the offending changes in position_manager.py and re-run parity check. If the offending commit was fail-safe related, you should have taken STEP 3 from the start.
  </action>
  <acceptance_criteria>
    - `grep "^## Reconciliation Outcome$" docs/audits/v10_baseline_drift.md` returns 1 match
    - `grep "^\*\*Reconciliation Outcome:" docs/audits/v10_baseline_drift.md` returns 1 match
    - `grep "_To be populated by plan 42-04._" docs/audits/v10_baseline_drift.md` returns 0 matches
    - `grep "Label: \`\(fixed_by_revert\|fixed_by_preset\|accepted_drift\)\`" docs/audits/v10_baseline_drift.md` returns 1 match (exactly one of the three labels present as literal in backticks)
    - `grep "### Parity Check (D-09 thresholds)" docs/audits/v10_baseline_drift.md` returns 1 match
    - `grep "### D-11 Compliance Statement" docs/audits/v10_baseline_drift.md` returns 1 match
    - Running `uv run python analysis/bisect_v10_baseline.py` at current HEAD returns:
      - If outcome is `fixed_by_revert` or `fixed_by_preset`: exit code 0 AND BISECT_RESULT cagr value parsed as float satisfies `11.2 <= cagr <= 11.8`
      - If outcome is `accepted_drift`: exit code 1 is acceptable (parity not achieved, documented as such)
    - D-11 compliance: `git diff <phase-start-hash>..HEAD -- strategies/mdm_hybrid/position_manager.py | grep -i fail_safe` returns empty
    - If outcome is `fixed_by_preset`:
      - `grep "v60_strict_mode: bool = False" strategies/mdm_hybrid/config.py` returns 1 match
      - `grep "v60_strict_mode=False" strategies/mdm_hybrid/config.py` returns >= 1 match (VN30_PRESET explicit value)
      - `grep "v60_strict_mode=True" analysis/bisect_v10_baseline.py` returns 1 match (the bisect script's reconciliation-check path)
    - If outcome is `accepted_drift`:
      - `grep "^\*\*Reconciliation Outcome: Accepted drift, not fixed\*\*" docs/audits/v10_baseline_drift.md` returns 1 match (exact literal for downstream grep)
      - No new commits on `strategies/mdm_hybrid/config.py` or `strategies/mdm_hybrid/position_manager.py` since plan start (verify with `git log --oneline <phase-start>..HEAD -- strategies/mdm_hybrid/config.py strategies/mdm_hybrid/position_manager.py` returns 0 lines)
  </acceptance_criteria>
  <verify>
    <automated>OUTCOME=$(grep -oE "fixed_by_revert|fixed_by_preset|accepted_drift" docs/audits/v10_baseline_drift.md | head -1); test -n "$OUTCOME" && grep -q "^## Reconciliation Outcome$" docs/audits/v10_baseline_drift.md && grep -q "### D-11 Compliance Statement" docs/audits/v10_baseline_drift.md && ! grep -q "_To be populated by plan 42-04._" docs/audits/v10_baseline_drift.md && echo "PASS outcome=$OUTCOME"</automated>
  </verify>
  <done>
    Fix-forward waterfall executed per D-07; outcome label recorded as one of fixed_by_revert / fixed_by_preset / accepted_drift; parity-check table populated with reference vs measured vs delta vs band vs pass; D-11 compliance verified with grep; no fail-safe code changes.
  </done>
</task>

</tasks>

<verification>
- Reconciliation outcome label is exactly one of `fixed_by_revert`, `fixed_by_preset`, `accepted_drift`
- Parity check table populated with live-measured values, not placeholders
- D-11 compliance grep passes (no fail_safe diffs)
- If preset path taken: `v60_strict_mode` exists in MDMV2Config with default False, VN30_PRESET explicitly sets False, and the offending code path has a branch-guard using this field
</verification>

<success_criteria>
- BASE-02 closed: either HybridEngine at HEAD matches v6.0 within D-09 band, OR the accepted-drift path is documented with explicit justification
- Reconciled-HEAD state is the stable input for plans 42-05 (publish) and 42-06 (determinism test)
- Zero changes to fail-safe logic verified
</success_criteria>

<output>
After completion, create `.planning/phases/42-baseline-reconciliation/42-04-SUMMARY.md`.
</output>
