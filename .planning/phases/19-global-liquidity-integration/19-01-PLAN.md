---
phase: 19-global-liquidity-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - strategies/mdm_v2/liquidity.py
  - strategies/mdm_v2/config.py
  - tests/test_liquidity.py
autonomous: true
requirements: [LIQ-01, LIQ-03]

must_haves:
  truths:
    - "Weekly global_liquidity.csv loads and forward-fills to daily trading dates via merge_asof"
    - "Publication lag offset shifts liquidity dates forward by configurable days to prevent look-ahead bias"
    - "Pre-2007 dates produce neutral regime (qe_floor=0) with no crashes"
    - "MDMV2Config has qe_floor_enabled=False by default for backward compatibility"
  artifacts:
    - path: "strategies/mdm_v2/liquidity.py"
      provides: "LiquidityLoader class and LiquidityRegime enum"
      exports: ["LiquidityLoader", "LiquidityRegime"]
    - path: "strategies/mdm_v2/config.py"
      provides: "Three new config fields for QE floor"
      contains: "qe_floor_enabled"
    - path: "tests/test_liquidity.py"
      provides: "Unit tests for loader, lag, NaN handling"
      min_lines: 80
  key_links:
    - from: "strategies/mdm_v2/liquidity.py"
      to: "data/global_liquidity.csv"
      via: "pd.read_csv + pd.merge_asof"
      pattern: "merge_asof.*direction.*backward"
    - from: "strategies/mdm_v2/config.py"
      to: "strategies/mdm_v2/liquidity.py"
      via: "config fields consumed by loader"
      pattern: "qe_floor_enabled.*bool.*False"
---

<objective>
Create the LiquidityLoader data pipeline and config additions for Phase 19.

Purpose: Establish the data layer that loads weekly global liquidity CSV, applies publication lag offset to prevent look-ahead bias, and merges to daily trading dates. This is the foundation for SELL suppression in Plan 02.
Output: `strategies/mdm_v2/liquidity.py` with LiquidityLoader class, updated `config.py` with QE floor fields, and comprehensive unit tests.
</objective>

<execution_context>
@C:/Users/trant/projects/mdm/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/trant/projects/mdm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-global-liquidity-integration/19-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From strategies/mdm_v2/config.py:
```python
@dataclass
class MDMV2Config:
    # ... existing fields ...
    stop_loss_pct: float = 0.015
    name: str = "default"
    # Fields end here -- new fields go BEFORE name field
```

From data/global_liquidity.csv (columns):
```
date,WALCL,fed_net,ECB_USD,BOJ_USD,global_liquidity,liquidity_roc_20w,qe_floor
```
- 987 rows, 2007-05-02 to 2026-03-25, all Wednesdays
- `qe_floor`: 1 = expanding (suppress SELL), 0 = not
- First 20 rows have NaN in `liquidity_roc_20w` but `qe_floor` is 0
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create LiquidityLoader and LiquidityRegime</name>
  <files>strategies/mdm_v2/liquidity.py, tests/test_liquidity.py</files>
  <read_first>
    - strategies/mdm_v2/config.py (current config dataclass pattern)
    - data/global_liquidity.csv (first 5 rows to confirm column names)
    - strategies/mdm_v2/mdm_v2_engine.py (to understand how daily_df is structured -- has 'date' column)
    - tests/test_data_loader.py (existing test patterns in project)
  </read_first>
  <behavior>
    - Test 1 (load_csv): LiquidityLoader reads data/global_liquidity.csv successfully, returns DataFrame with 987 rows and columns [date, global_liquidity, liquidity_roc_20w, qe_floor]
    - Test 2 (merge_to_daily): Given a daily DataFrame with business days 2010-01-01 to 2010-12-31, load_and_merge returns same number of rows with added liquidity columns, no NaN in qe_floor
    - Test 3 (publication_lag): For every daily row in the merged result, the matched liquidity observation date (before lag shift) is at least publication_lag_days before the daily date -- test with lag=7 and lag=14
    - Test 4 (pre_2007_nan): Given daily DataFrame with dates 2000-01-03 to 2005-12-30 (before any liquidity data), merged result has qe_floor=0 for all rows (NaN filled to 0)
    - Test 5 (regime_enum): LiquidityRegime has EXPANDING, NEUTRAL, CONTRACTING values
    - Test 6 (merge_preserves_index): Merged DataFrame has same number of rows and same date column as input daily_df (no rows added or removed)
  </behavior>
  <action>
    Create `strategies/mdm_v2/liquidity.py` with:

    1. `LiquidityRegime` enum with three values: EXPANDING="EXPANDING", NEUTRAL="NEUTRAL", CONTRACTING="CONTRACTING"

    2. `LiquidityLoader` class:
       - `__init__(self, csv_path: str = "data/global_liquidity.csv")`
       - `load_and_merge(self, daily_df: pd.DataFrame, publication_lag_days: int = 7) -> pd.DataFrame`:
         - Read CSV with `pd.read_csv(self.csv_path, parse_dates=['date'])`
         - Select columns: `['date', 'global_liquidity', 'liquidity_roc_20w', 'qe_floor']`
         - Shift dates forward: `liq['date'] = liq['date'] + pd.Timedelta(days=publication_lag_days)`
         - Sort both DataFrames by date
         - Merge: `pd.merge_asof(daily_sorted, liq, on='date', direction='backward')`
         - Fill NaN: `merged['qe_floor'] = merged['qe_floor'].fillna(0).astype(int)`
         - Return merged DataFrame

    Create `tests/test_liquidity.py` with pytest tests implementing all 6 behaviors above.

    For test_publication_lag: Load raw CSV, for each merged row with non-NaN global_liquidity, find the original CSV row with matching value, assert `(daily_date - original_date).days >= publication_lag_days`.

    Do NOT interpolate between weekly points. Do NOT use `pd.date_range` for reindex. Use `merge_asof` only.
  </action>
  <verify>
    <automated>cd C:/Users/trant/projects/mdm && uv run pytest tests/test_liquidity.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - strategies/mdm_v2/liquidity.py contains `class LiquidityLoader`
    - strategies/mdm_v2/liquidity.py contains `class LiquidityRegime(Enum)`
    - strategies/mdm_v2/liquidity.py contains `merge_asof`
    - strategies/mdm_v2/liquidity.py contains `pd.Timedelta(days=publication_lag_days)`
    - strategies/mdm_v2/liquidity.py contains `fillna(0).astype(int)`
    - tests/test_liquidity.py contains `def test_publication_lag`
    - tests/test_liquidity.py contains `def test_pre_2007` or `def test_pre2007`
    - tests/test_liquidity.py contains at least 6 test functions
    - `uv run pytest tests/test_liquidity.py -x -v` exits 0
  </acceptance_criteria>
  <done>LiquidityLoader loads CSV, merges to daily with lag offset, handles pre-2007 NaN, all 6 unit tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Add QE floor config fields to MDMV2Config</name>
  <files>strategies/mdm_v2/config.py</files>
  <read_first>
    - strategies/mdm_v2/config.py (current full file -- MUST read before editing)
  </read_first>
  <action>
    Add three new fields to `MDMV2Config` dataclass, placed AFTER `stop_loss_pct` and BEFORE `name`:

    ```python
    # Global Liquidity / QE Floor (v5.0, LIQ-03)
    qe_floor_enabled: bool = False              # Master switch, default OFF
    publication_lag_days: int = 7                # Days to offset liquidity data
    liquidity_csv_path: str = "data/global_liquidity.csv"
    ```

    Add validation in `__post_init__`:
    ```python
    assert self.publication_lag_days >= 0, "Publication lag must be non-negative"
    ```

    Do NOT change any existing fields or their default values. The `MDMConfig = MDMV2Config` alias at the bottom must remain.
  </action>
  <verify>
    <automated>cd C:/Users/trant/projects/mdm && uv run python -c "from strategies.mdm_v2.config import MDMV2Config; c = MDMV2Config(); assert c.qe_floor_enabled == False; assert c.publication_lag_days == 7; assert c.liquidity_csv_path == 'data/global_liquidity.csv'; print('Config OK')"</automated>
  </verify>
  <acceptance_criteria>
    - strategies/mdm_v2/config.py contains `qe_floor_enabled: bool = False`
    - strategies/mdm_v2/config.py contains `publication_lag_days: int = 7`
    - strategies/mdm_v2/config.py contains `liquidity_csv_path: str = "data/global_liquidity.csv"`
    - strategies/mdm_v2/config.py contains `publication_lag_days >= 0`
    - strategies/mdm_v2/config.py still contains `MDMConfig = MDMV2Config` at the bottom
    - `MDMV2Config()` instantiates without error with all defaults
  </acceptance_criteria>
  <done>MDMV2Config has three new QE floor fields with safe defaults, existing fields unchanged, backward compatible</done>
</task>

</tasks>

<verification>
- All tests in test_liquidity.py pass: `uv run pytest tests/test_liquidity.py -x -v`
- MDMV2Config instantiates with defaults and new fields: `uv run python -c "from strategies.mdm_v2.config import MDMV2Config; c = MDMV2Config(); print(c.qe_floor_enabled, c.publication_lag_days)"`
- Existing tests still pass: `uv run pytest tests/ -x --timeout=120`
</verification>

<success_criteria>
- LiquidityLoader merges weekly CSV to daily with configurable publication lag
- Pre-2007 dates produce qe_floor=0 (no crash, no false suppression)
- Publication lag test proves no look-ahead bias
- Config has qe_floor_enabled=False default (backward compatible)
- All unit tests green
</success_criteria>

<output>
After completion, create `.planning/phases/19-global-liquidity-integration/19-01-SUMMARY.md`
</output>
