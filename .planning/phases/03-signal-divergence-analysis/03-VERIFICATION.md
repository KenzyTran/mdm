---
phase: 03-signal-divergence-analysis
verified: 2026-03-28T10:00:00Z
status: passed
score: 8/8 must-haves verified
---

# Phase 3: Signal Divergence Analysis — Verification Report

**Phase Goal:** The project can measure exactly where and how classic MDM rules diverge from Dr. K's published post-2019 signals
**Verified:** 2026-03-28
**Status:** PASSED
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Signal comparison engine scores model-generated signals against published signals with match rate, timing offset, and per-signal type breakdown | VERIFIED | `compare_signals()` in `core/signal_comparator.py` returns `{match_rate, total_published, total_matched, per_type}`. 28/28 unit+integration tests pass. |
| 2 | MDM classic rules running on NASDAQ data produce a complete Buy/Sell/Cash signal list for the full 2017-2026 period | VERIFIED | `extract_model_signals()` maps state column via `STATE_TO_SIGNAL`. Integration tests confirm engine runs on real NASDAQ data (2017-01-01 start) and produces 87 signal transitions spanning into 2024. |
| 3 | Divergence report lists every date where classic output differs from published post-2019 signals, classified by type (TIMING, STRUCTURAL, THRESHOLD, IRREPRODUCIBLE) | VERIFIED | `output/divergence_report.csv` generated by `analysis/signal_divergence.py` with columns `date,published_signal,model_signal,divergence_type,context`. 64 divergences classified. `test_divergence_types_valid` confirms all types valid. |
| 4 | Visual overlay chart shows NASDAQ price with both model signals and published signals plotted, making divergence patterns visually apparent | VERIFIED | `output/signal_overlay.png` is 158 KB (well above 10 KB threshold). Three-panel chart (GridSpec 3,1 height_ratios=[4,1,1]) confirmed in source. `test_chart_exists` passes. `test_script_produces_png` passes via subprocess. |
| 5 (plan 01) | Signal comparator takes two DataFrames and returns match rate, per-type match rates, and aligned comparison DataFrame | VERIFIED | `compare_signals()` returns dict with `match_rate`, `total_published`, `total_matched`, `per_type`. `align_signals()` returns `[date, published, model]` outer-merged DataFrame. All 5 test classes pass. |
| 6 (plan 01) | State-to-signal mapping: HOLDING->Buy, SHORT->Sell, CASH->Cash, WAITING_SELL->Cash | VERIFIED | `STATE_TO_SIGNAL` dict defined at module level. `test_extract_waiting_sell_maps_to_cash` passes. |
| 7 (plan 01) | Exact date matching only (no tolerance window) for compare_signals | VERIFIED | `compare_signals` iterates aligned_df checking `pub == mod` only on rows with matching dates. `test_compare_exact_date_only` passes. |
| 8 (plan 01) | classify_divergences checks TIMING before STRUCTURAL | VERIFIED | Code priority: (1) TIMING via `_find_nearby_same_type`, (2) STRUCTURAL only if no nearby same-type, (3) THRESHOLD, (4) IRREPRODUCIBLE. `test_classify_timing_before_structural` passes. |

**Score:** 8/8 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `core/signal_comparator.py` | Reusable comparison engine; exports extract_model_signals, align_signals, compare_signals, classify_divergences, generate_divergence_context | VERIFIED | 318 lines. All 5 public functions present. `STATE_TO_SIGNAL` dict at module level. No stubs or TODOs. |
| `tests/test_signal_comparison.py` | Unit tests for comparison engine, min 80 lines | VERIFIED | 334 lines, 21 test functions across 5 test classes. All pass. |
| `tests/test_signal_generation.py` | Integration tests for NASDAQ MDM signal extraction, min 40 lines | VERIFIED | 129 lines, 7 test functions (3 engine, 3 extraction, 1 comparison). All pass. |
| `analysis/signal_divergence.py` | Analysis script generating all output files, min 100 lines | VERIFIED | 363 lines. Has `def main():`, `GridSpec(3, 1, height_ratios=[4, 1, 1])`, `from core.signal_comparator import`, path anchoring via `PROJECT_ROOT`. |
| `output/divergence_report.csv` | Per-divergence detail rows; header: date,published_signal,model_signal,divergence_type,context | VERIFIED | Header confirmed exact. 64 divergence rows. Generated by test and script execution. |
| `output/divergence_summary.txt` | Human-readable summary; contains "Match rate:" | VERIFIED | Contains "Match rate: 4.5%". Full breakdown: per-type rates, divergence type counts, longest streak. |
| `output/signal_overlay.png` | High-res chart, >10 KB | VERIFIED | 158,029 bytes (154 KB). Far exceeds threshold. |
| `tests/test_divergence_report.py` | Tests for report generation and chart output, min 30 lines | VERIFIED | 88 lines, 6 test functions including `test_script_produces_png`. All pass. |
| `notebooks/signal_overlay.ipynb` | Interactive exploration notebook | VERIFIED | 10 cells. Valid JSON. Contains `from core.signal_comparator import` and `%matplotlib inline`. |
| `tests/test_signal_chart.py` | Tests for notebook structure, min 15 lines | VERIFIED | 41 lines, 3 test functions. All pass. Does NOT contain `test_script_produces_png` (correctly scoped). |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `core/signal_comparator.py` | `strategies/mdm_classic/mdm_engine.py` | `extract_model_signals` processes MDMEngine.run() output; wired in `analysis/signal_divergence.py` line 149-153 | WIRED | Integration tests confirm full round-trip. Engine results flow into `extract_model_signals(results)`. |
| `core/signal_comparator.py` | `core/signal_loader.py` | `compare_signals` accepts output of `load_signal_fixture`; wired in `analysis/signal_divergence.py` line 144 and `test_signal_generation.py` line 14 | WIRED | `load_signal_fixture` called in both analysis script and integration test; output passed directly to `compare_signals`. |
| `analysis/signal_divergence.py` | `core/signal_comparator.py` | imports compare_signals, extract_model_signals, classify_divergences | WIRED | Line 45: `from core.signal_comparator import (extract_model_signals, align_signals, compare_signals, classify_divergences, generate_divergence_context, STATE_TO_SIGNAL)` |
| `analysis/signal_divergence.py` | `output/divergence_report.csv` | writes CSV with divergence rows | WIRED | Line 186-187: `csv_path = os.path.join(OUTPUT_DIR, 'divergence_report.csv')` + `csv_df.to_csv(csv_path, index=False)` |
| `analysis/signal_divergence.py` | `output/signal_overlay.png` | saves matplotlib figure | WIRED | Line 354-355: `chart_path = os.path.join(OUTPUT_DIR, 'signal_overlay.png')` + `plt.savefig(chart_path, ...)` |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|--------------|--------|-------------------|--------|
| `analysis/signal_divergence.py` | `metrics['match_rate']`, `classified` (divergence DataFrame) | `DataLoader('nasdaq').load(start_date='2017-01-01')` → `MDMEngine().run()` → `extract_model_signals()` → `compare_signals()` → `classify_divergences()` | Yes — real NASDAQ price data from CSV; engine computes state transitions on 2000+ bars; 67 published signals loaded from fixture | FLOWING |
| `output/divergence_report.csv` | 64 classified divergence rows | Full pipeline above; each row sourced from `classified[classified['divergence_type'].notna()]` | Yes — types: THRESHOLD=34, TIMING=8, STRUCTURAL=14, IRREPRODUCIBLE=8 | FLOWING |
| `output/divergence_summary.txt` | Match rate 4.5%, per-type rates | `metrics` dict from `compare_signals()` | Yes — real computation: 3/67 published signals matched | FLOWING |
| `notebooks/signal_overlay.ipynb` | Chart and divergence table | Same pipeline as analysis script (notebook cells 3-4) | Yes — cells contain real data loading and computation, not hardcoded values | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| signal_comparator importable with all public functions | `uv run python -c "from core.signal_comparator import extract_model_signals, align_signals, compare_signals, classify_divergences; print('OK')"` | OK | PASS |
| Full test suite: 28 Plan-01 tests | `uv run python -m pytest tests/test_signal_comparison.py tests/test_signal_generation.py -v` | 28 passed in 1.23s | PASS |
| Full test suite: 9 Plan-02 tests | `uv run python -m pytest tests/test_divergence_report.py tests/test_signal_chart.py -v` | 9 passed in 6.77s | PASS |
| Complete test suite: no regressions | `uv run python -m pytest tests/ -v` | 86 passed, 4 skipped in 8.63s | PASS |
| Output CSV has correct header | head -1 output/divergence_report.csv | `date,published_signal,model_signal,divergence_type,context` | PASS |
| Summary contains match rate | grep "Match rate:" output/divergence_summary.txt | `Match rate: 4.5%` | PASS |
| PNG chart exists and is substantive | ls -la output/signal_overlay.png | 158,029 bytes | PASS |
| Script produces PNG via subprocess | `test_script_produces_png` in test suite | PASSED | PASS |

The 4 skipped tests (`test_vsa_regression.py`) are a pre-existing condition: they require VN30.csv which is gitignored and absent from the worktree. This is documented in both SUMMARY files and is not a regression from Phase 3.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|---------|
| SIG-01 | 03-01-PLAN.md | Signal comparison engine scores model-generated signals against published signal history | SATISFIED | `core/signal_comparator.py` implements `compare_signals()` returning match_rate and per-type breakdown. 21 unit tests + 7 integration tests confirm correct operation. |
| SIG-02 | 03-01-PLAN.md | MDM classic rules run on NASDAQ data and produce Buy/Sell/Cash signal list | SATISFIED | `extract_model_signals()` maps engine state output. Integration tests confirm 87 transitions produced from 2017 NASDAQ data with all values in {Buy, Sell, Cash}. |
| SIG-03 | 03-02-PLAN.md | Divergence report identifies dates, signal types, and durations where classic rules differ from published post-2019 signals | SATISFIED | `output/divergence_report.csv` with 64 rows, columns: date, published_signal, model_signal, divergence_type, context. All divergence types valid. |
| SIG-04 | 03-02-PLAN.md | Visual signal overlay shows price chart with model signals and published signals side by side | SATISFIED | `output/signal_overlay.png` (158 KB). Three-panel GridSpec chart: NASDAQ price + published signal track + model signal track with divergence shading. |

All 4 required requirements are SATISFIED. No orphaned requirements detected — REQUIREMENTS.md traceability table maps SIG-01 through SIG-04 exclusively to Phase 3, and all 4 are claimed by Phase 3 plans.

### Anti-Patterns Found

None detected. Scan results:
- No TODO/FIXME/PLACEHOLDER markers in `core/signal_comparator.py` or `analysis/signal_divergence.py`
- No empty returns (return null / return [] / return {}) that flow to user-visible output
- No hardcoded empty data in rendering paths
- All file output paths use `os.path.join(OUTPUT_DIR, ...)` — no bare relative paths

### Human Verification Required

**1. Match Rate Plausibility (Research Quality)**

**Test:** Review the divergence summary. Match rate is 4.5% (3/67 signals match). This is the expected result given classic MDM rules predating 2019 rule changes.
**Expected:** Low match rate is acceptable — Phase 3's goal is to measure divergence, not achieve a high match rate. Phase 4 will address improving the match rate via MDM v2.
**Why human:** Requires domain judgment on whether 4.5% is a plausible baseline for pre-2019 classic rules vs post-2019 published signals.

**2. Chart Visual Quality**

**Test:** Open `output/signal_overlay.png` and verify the three-panel layout is legible — NASDAQ price trend is visible, signal color bands distinguish Buy/Sell/Cash states, and divergence shading marks disagreement periods.
**Expected:** Three distinct panels with clear signal-to-color mapping and visible divergence shading.
**Why human:** Visual layout and legibility cannot be verified programmatically.

**3. Notebook Interactivity**

**Test:** Open `notebooks/signal_overlay.ipynb` in Jupyter, run all cells sequentially, verify that the chart renders and the divergence filter cell (`filter_type = 'STRUCTURAL'`) correctly filters the table.
**Expected:** All 10 cells run without error; chart displays inline; divergence table filters correctly.
**Why human:** Notebook cell execution requires a running Jupyter kernel.

### Gaps Summary

No gaps found. All artifacts exist, are substantive, are wired, and data flows through the full pipeline. The full test suite (86 passing) confirms end-to-end correctness including subprocess execution of the standalone analysis script.

---

_Verified: 2026-03-28_
_Verifier: Claude (gsd-verifier)_
