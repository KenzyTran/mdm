---
phase: 260423-nl3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - README-01
must_haves:
  truths:
    - "Anyone landing on the repo root can read README.md and understand: what this project is, which model is in production today, and how to run a backtest."
    - "Milestone table v1.0 through v10.0 is present with explicit SHIPPED/REJECTED/RETAIN verdicts."
    - "Current best production model (v6.0 HybridEngine + fail-safe, +238.8% total / CAGR 11.5% / MaxDD −28.2% on VN30 2015-2026) is stated prominently."
    - "Quick-start cites Python 3.10+, uv, and scripts/run_hybrid_backtest.py and scripts/run_backtest.py as actual entry points."
    - "Repo layout section points to strategies/ subpackages, analysis/, core/, tests/, data/, output/, docs/, and at least one docs/rules_*.md."
    - "Drill-down links to docs/portfolio_system_overview.md, docs/strategy_v7.md, docs/data_dictionary.md, and a docs/rules_*.md file all resolve."
    - "Constraints section mentions: research/backtest only (no live trading), US data scaled ~1000x, VN microstructure (T+2.5, 7% limit)."
    - "README contains no emoji and no mention of Claude, AI, or GSD."
  artifacts:
    - path: "README.md"
      provides: "Project-root human-readable overview, milestone history, quick-start, repo layout"
      contains: "MDM Reverse-Engineering"
      min_lines: 60
  key_links:
    - from: "README.md"
      to: "docs/portfolio_system_overview.md"
      via: "relative markdown link"
      pattern: "docs/portfolio_system_overview\\.md"
    - from: "README.md"
      to: "docs/strategy_v7.md"
      via: "relative markdown link"
      pattern: "docs/strategy_v7\\.md"
    - from: "README.md"
      to: "docs/data_dictionary.md"
      via: "relative markdown link"
      pattern: "docs/data_dictionary\\.md"
    - from: "README.md"
      to: "docs/rules_mdm_hybrid.md"
      via: "relative markdown link"
      pattern: "docs/rules_mdm_hybrid\\.md"
---

<objective>
Create a single README.md at the project root so a new reader (human, not AI) can, within one page, understand: (1) what this project is, (2) the current best production model, (3) the milestone journey v1.0–v10.0 with verdicts, (4) how to run a backtest, (5) where each module lives, and (6) the hard constraints.

Purpose: There is no README.md at the repo root today. Every new contributor or auditor has to read .planning/PROJECT.md (which is GSD-internal) to orient. A public-facing README fixes this without duplicating decision history.

Output: `README.md` at `C:/Users/trant/projects/mdm/README.md`.
</objective>

<execution_context>
@C:/Users/trant/projects/mdm/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@./CLAUDE.md
@docs/portfolio_system_overview.md
@docs/strategy_v7.md
@docs/data_dictionary.md
@docs/rules_mdm_hybrid.md
@scripts/run_backtest.py
@scripts/run_hybrid_backtest.py
@pyproject.toml

<facts_locked_from_context>
These numbers MUST appear verbatim in README.md. Do not round or restate from memory — copy from here:

- v6.0 best production (VN30 2015-2026): total return +238.8%, CAGR 11.5%, MaxDD −28.2%.
  (Reconciled measurement at Phase 42: CAGR 11.47%, SELL 124, MaxDD −28.17%, total return +238.78%. Either the shipped headline or the reconciled measurement is acceptable — prefer shipped headline in the top-of-README summary, reconciled in the milestone table if listed separately.)
- v7.0 CANSLIM+MDM (VN100, OOS 2019-2025): CAGR 10.18%, Sharpe_rf3 0.813, MaxDD −16.31%.
- v8.0 RS Momentum (OOS 2019-2025): CAGR 10.0%, Sharpe_rf3 0.645, MaxDD −24.9%. Verdict: REJECTED (trails v7.0).
- v9.0 VN30 whipsaw reduction (ATR Buffer + Refined DD): all three scenarios fail CAGR ≥ 11.5% gate on 2015-2026; walk-forward degradation +67% to +96%. Verdict: REJECTED, v6.0 retained.
- v10.0 walk-forward grid search: 0 of 39 combos accepted (median degradation 0.538, range 0.410–0.645 vs 0.30 gate). Verdict: retain v6.0 for Phase 46.
- Python 3.10+, uv package manager.
- Entry-point scripts live under `scripts/`, NOT at project root: `scripts/run_backtest.py` (original MDM classic) and `scripts/run_hybrid_backtest.py` (Hybrid engine). Do not write "python run_backtest.py" at the repo root — it does not exist there.
- Dashboard: http://mdm-trading-dashboard.s3-website-ap-southeast-1.amazonaws.com

Packages under `strategies/` (confirmed via directory listing):
mdm_classic, mdm_v2, mdm_hybrid, vsa, canslim, entry, portfolio, momentum.

Top-level dirs (confirmed): analysis, connectors, core, dashboard, data, docs, notebooks, output, scripts, strategies, tests.

docs/rules_*.md files (confirmed): rules_mdm_classic.md, rules_mdm_v2.md, rules_mdm_hybrid.md, rules_vsa.md, rules_canslim.md, rules_canslim_mdm.md, rules_entry.md.
</facts_locked_from_context>

<hard_dont_do>
- DO NOT write any emoji character (U+1F300..U+1FAFF, U+2600..U+27BF, etc.). No check-marks via emoji, no arrows via emoji. Plain ASCII arrows like `->` are fine.
- DO NOT mention "Claude", "AI", "LLM", "GSD", "/gsd:", "get-shit-done", or describe the planning workflow in the README body. This file is for human readers approaching the repo cold.
- DO NOT copy ROADMAP.md tables wholesale. Summarize.
- DO NOT invent numbers. If a metric is not in `<facts_locked_from_context>`, do not state it.
- DO NOT reference `run_backtest.py` at project root — the correct path is `scripts/run_backtest.py`.
- DO NOT create a multi-page README. Target ~80-140 lines when rendered, single-page feel.
- DO NOT use h1 more than once. Only one `# ...` header (the title).
</hard_dont_do>

</context>

<tasks>

<task type="auto">
  <name>Task 1: Write README.md at project root</name>
  <files>README.md</files>
  <action>
Create the file `README.md` at the repo root (`C:/Users/trant/projects/mdm/README.md`) with the exact seven sections below, in this order. Use GitHub-flavored markdown. Vietnamese prose is acceptable where it reads more naturally; keep technical terms in English. No emoji anywhere.

### Section layout

1. **Title + one-paragraph "what this is"** (1 `#` header, ~4-6 sentences)
   - Title: `# MDM Reverse-Engineering & VN30 Market Timing`
   - Paragraph must cover: reverse-engineering Dr. K's post-2019 MDM from public signal history (962 NASDAQ signals 1974-2026), then adapting the discovered rules to VN30 index timing, and combining with CANSLIM stock selection on VN100 for a long-only multi-stock portfolio. Mention that VSA is maintained as an independent strategy.

2. **Current Production Model** (`## Current Production Model`, ~6-10 lines)
   - State: **v6.0 HybridEngine + fail-safe** is the production baseline on VN30 2015-2026.
   - Numbers verbatim from `<facts_locked_from_context>`: total return +238.8%, CAGR 11.5%, MaxDD −28.2%.
   - One sentence noting the v7.0 CANSLIM+MDM stack exists on VN100 with different OOS metrics (CAGR 10.18%, Sharpe 0.813, MaxDD −16.31%) — link `docs/strategy_v7.md` and `docs/portfolio_system_overview.md` here inline.
   - Link `docs/rules_mdm_hybrid.md` for the full spec of the v6.0 engine.

3. **Milestone History** (`## Milestone History`, markdown table)
   - Columns: `Milestone | Scope | Status | Key result`.
   - Rows (exactly these, in this order):
     - `v1.0–v5.0` | Foundation: reverse-engineering pipeline, Hybrid engine, QE/banding filters | SHIPPED | 962-signal dataset + Propose-Filter-Decide pipeline
     - `v6.0` | Fail-safe + signal refinement on VN30 | SHIPPED (production) | +238.8% / CAGR 11.5% / MaxDD −28.2% (2015-2026)
     - `v7.0` | CANSLIM + MDM gate on VN100 portfolio | SHIPPED | OOS CAGR 10.18%, Sharpe 0.813, MaxDD −16.31% (2019-2025)
     - `v8.0` | RS Momentum stock selection on VN100 | REJECTED | OOS Sharpe 0.645 trails v7.0 (0.813)
     - `v9.0` | VN30 whipsaw reduction (ATR Buffer + Refined DD) | REJECTED | Walk-forward degradation +67% to +96%; v6.0 retained
     - `v10.0` | VN macro filter (DXY/EEM/SBV) + walk-forward grid | IN PROGRESS (retain-v6.0 branch) | 0/39 combos accepted in Phase 45 grid; v6.0 retained for Phase 46

4. **Quick Start** (`## Quick Start`, shell block)
   - List Python 3.10+ requirement and uv package manager (cite `pyproject.toml`).
   - Show two commands users can actually run, both using forward slashes:
     ```
     uv sync
     uv run python scripts/run_backtest.py           # MDM classic on NASDAQ
     uv run python scripts/run_hybrid_backtest.py    # v6.0 HybridEngine
     ```
   - One line about data expectations: CSV OHLCV panels for VN30 and NASDAQ under `data/`. Link `docs/data_dictionary.md` for connector schemas and column contracts.

5. **Repo Layout** (`## Repo Layout`, bullet list)
   - Bullet each top-level directory that a contributor needs to know:
     - `strategies/` — `mdm_classic`, `mdm_v2`, `mdm_hybrid`, `vsa`, `canslim`, `entry`, `portfolio`, `momentum`
     - `core/` — indicator engine, data loader, feature snapshot helpers
     - `analysis/` — hypothesis testing, rule discovery, A/B and walk-forward validators
     - `connectors/` — Postgres (TA) and MySQL (fundamentals) connectors used by v7.0 VN100 stack
     - `scripts/` — entry-point scripts (backtests, diagnostics, dashboard export)
     - `tests/` — pytest suite (regression tests marked `@pytest.mark.regression`)
     - `data/` — CSV inputs (VN30, NASDAQ, S&P500, signals, liquidity proxies)
     - `output/` — generated reports, equity curves, sweep results
     - `docs/` — human-facing specs: `rules_mdm_classic.md`, `rules_mdm_v2.md`, `rules_mdm_hybrid.md`, `rules_vsa.md`, `rules_canslim*.md`, `rules_entry.md`, plus overviews
     - `.planning/` — one line only: "planning & milestone artifacts (internal workflow metadata)". No further elaboration.
   - End with: "Per-strategy trading rules live under `docs/rules_*.md` and MUST stay in sync with code changes in the corresponding `strategies/` package (see `CLAUDE.md` Code-Docs Sync Rule)."

6. **Constraints** (`## Constraints`, bullet list, ~4 bullets)
   - Research & backtest only — no live trading, no real-time signal generation.
   - US market OHLCV (NASDAQ, S&P500) is scaled ~1000x at source and normalized in the loader before analysis.
   - VN30 microstructure: T+2.5 settlement, 7% daily price limit, derivative expiry effects — reflected in the portfolio engine's fill logic and stop placement.
   - Signal validation uses publicly delayed Dr. K history (up to 2 months lag for non-members); exact replication of the proprietary model is not a goal.

7. **Further Reading** (`## Further Reading`, bullet list of links)
   - `docs/portfolio_system_overview.md` — end-to-end signal → portfolio flow diagram.
   - `docs/strategy_v7.md` — v7.0 CANSLIM + MDM + RS partial liquidation spec.
   - `docs/data_dictionary.md` — connector schemas, CANSLIM column contracts, adjustment helpers.
   - `docs/rules_mdm_hybrid.md` — v6.0 HybridEngine production rules.
   - `docs/rules_vsa.md` — independent VSA strategy (maintained separately from MDM).

### Format & style constraints

- Use only ONE `#` h1 (the title). All other section headers are `##`.
- Markdown tables use `|` separators with an alignment row.
- Negative signs on drawdowns: use the Unicode minus sign `−` (U+2212) OR ASCII `-`; pick one and be consistent (either is fine for human readers).
- Relative links only. No absolute filesystem paths. No URLs other than the dashboard URL already cited if you choose to include it (optional — the dashboard is still live but not required content).
- Tone: technical, concise, no marketing language. Prose can be Vietnamese where natural (per user preference); keep section headers and code blocks in English.
- Final line: `*Last updated: 2026-04-23*`

### Self-audit before you finish writing

Run through this checklist mentally and fix before saving:
- [ ] Zero emoji characters anywhere in the file.
- [ ] Zero occurrences of "Claude", "AI", "LLM", "GSD", "/gsd:", "get-shit-done".
- [ ] All four required drill-down links (`portfolio_system_overview.md`, `strategy_v7.md`, `data_dictionary.md`, `rules_mdm_hybrid.md`) appear as clickable markdown links.
- [ ] Both quick-start commands reference `scripts/run_backtest.py` and `scripts/run_hybrid_backtest.py` (with the `scripts/` prefix).
- [ ] Milestone table has exactly 6 rows in the order specified.
- [ ] All numbers (+238.8%, CAGR 11.5%, MaxDD −28.2%, Sharpe 0.813, Sharpe 0.645, 0/39, +67-96%) are present verbatim.
- [ ] File has exactly one `#` header (title). Every other heading is `##`.
- [ ] Rendered length feels single-page (roughly 80-140 lines including blanks).
  </action>
  <verify>
    <automated>python -c "import re,sys; t=open('README.md',encoding='utf-8').read(); emoji=re.findall(r'[\U0001F300-\U0001FAFF☀-➿]', t); banned=[w for w in ['Claude','GSD','/gsd:','get-shit-done',' AI ',' LLM '] if w in t]; required=['docs/portfolio_system_overview.md','docs/strategy_v7.md','docs/data_dictionary.md','docs/rules_mdm_hybrid.md','scripts/run_backtest.py','scripts/run_hybrid_backtest.py','v6.0','v7.0','v8.0','v9.0','v10.0','238.8','11.5','0.813','0.645','0/39']; missing=[r for r in required if r not in t]; h1=len(re.findall(r'(?m)^# [^#]', t)); errs=[]; errs.append(f'emoji={emoji}') if emoji else None; errs.append(f'banned={banned}') if banned else None; errs.append(f'missing={missing}') if missing else None; errs.append(f'h1_count={h1}') if h1!=1 else None; print('OK' if not errs else 'FAIL: '+'; '.join(errs)); sys.exit(0 if not errs else 1)"</automated>
  </verify>
  <done>
    - `README.md` exists at project root.
    - Automated verify command prints `OK` and exits 0.
    - Manual eyeball: table renders, links resolve, document feels ~single-page.
  </done>
</task>

</tasks>

<verification>
After Task 1 completes:

1. Run the automated gate shown in `<verify>` — it must print `OK`.
2. Open `README.md` in a markdown renderer (or `cat README.md | head -80`) and confirm:
   - Title is a single `#` line.
   - Milestone table has 6 rows in declared order with SHIPPED / REJECTED / IN PROGRESS labels visible.
   - Four drill-down links are present and point at files that exist under `docs/`.
   - No emoji, no mention of Claude/AI/GSD.
3. Resolve each drill-down link:
   ```
   test -f docs/portfolio_system_overview.md && test -f docs/strategy_v7.md && test -f docs/data_dictionary.md && test -f docs/rules_mdm_hybrid.md && echo LINKS_OK
   ```
   Expect: `LINKS_OK`.
</verification>

<success_criteria>
- [ ] `README.md` created at `C:/Users/trant/projects/mdm/README.md`.
- [ ] Automated verify prints `OK` (all required tokens present, zero emoji, zero banned words, exactly one h1).
- [ ] All four drill-down doc links resolve to existing files.
- [ ] Milestone table includes v1.0-v5.0, v6.0, v7.0, v8.0, v9.0, v10.0 rows with correct verdicts.
- [ ] Quick-start cites `scripts/run_backtest.py` and `scripts/run_hybrid_backtest.py` (not bare filenames at root).
- [ ] File committed via the standard GSD commit flow.
</success_criteria>

<output>
After completion, create `.planning/quick/260423-nl3-create-readme-md-at-project-root-with-pr/260423-nl3-SUMMARY.md` with:
- Final line count of README.md
- Confirmation that automated verify passed
- List of any deviations from the plan (expected: none)
</output>
