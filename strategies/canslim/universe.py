"""VN100 universe loader — owning requirements UNIV-01, UNIV-02, UNIV-03.

Stub: filled in by plan 29-03.
"""
from __future__ import annotations


def load_vn100_universe(as_of_date=None):
    """Return the static VN100 ticker list (stubbed).

    Args:
        as_of_date: Date for point-in-time membership (currently ignored;
            VN100 is treated as static per 29-CONTEXT.md D-02).

    Returns:
        list[str]: Ticker symbols.

    Raises:
        NotImplementedError: Filled in by plan 29-03.
    """
    raise NotImplementedError("Filled in by plan 29-03")
